---
quick_id: 260423-0s2
slug: implementing-task-3-of-the-a0-lesson-1-p
description: "Implementing Task 3 of the A0 Lesson 1 plan: CompletionStore + AsyncStorage impl (TDD)."
date: 2026-04-23
status: complete
---

# Quick Task 260423-0s2: CompletionStore + AsyncStorage impl (TDD)

## Task

Create the `CompletionStore` interface and `asyncStorageCompletionStore` implementation backed by `@react-native-async-storage/async-storage`, using TDD (red → green).

## Plan

### Task 1: Write failing test

**File:** `src/__tests__/curriculum/completion-store.test.ts`

Mock `@react-native-async-storage/async-storage` with an in-memory `Map`. Four test cases:
1. `getCompletion` returns `false` when key absent
2. `markCompleted` + `getCompletion` round-trips under namespaced key (`tila.lesson-completion.{id}`)
3. `getCompletion` returns `false` for non-`"true"` stored values
4. `clearAll` removes only completion-namespaced keys (leaves unrelated keys intact)

### Task 2: Implement

**File:** `src/curriculum/runtime/completion-store.ts`

- Export `CompletionStore` interface with `markCompleted`, `getCompletion`, `clearAll`
- Export `asyncStorageCompletionStore` singleton
- Key prefix: `tila.lesson-completion.`
- `getCompletion` returns `true` only when stored value is exactly `"true"`
- `clearAll` uses `getAllKeys` + `multiRemove`, filtering to prefix-matching keys only
- All methods wrapped in try/catch; errors suppressed with `__DEV__` console.warn guard (matches Task 2 pattern)

### Task 3: Commit

`feat(curriculum): AsyncStorage completion store`
