---
type: quick
tasks: 3
autonomous: true
files_modified:
  - src/sync/provider.tsx
  - src/sync/tables.ts
  - src/sync/types.ts
  - src/sync/service.ts
  - src/components/home/LessonGrid.tsx
  - app/(tabs)/index.tsx
  - app/(tabs)/progress.tsx
  - app/lesson/[id].tsx
  - src/__tests__/sync-service.test.ts
---

<objective>
Fix 7 issues found in Phase 8 code review: SyncProvider infinite loop (blocker), sync PK/local_id mismatch (blocker), premium flash on home screen, restore analytics wrong function, AccountPrompt missing declined_at check, partial useAppState adoption, and sync test coverage gaps.

Purpose: Close blockers that would cause infinite sync loops and data corruption, plus fix 5 quality issues.
Output: All 7 issues resolved, tests passing.
</objective>

<context>
@.planning/STATE.md
@src/sync/provider.tsx
@src/sync/tables.ts
@src/sync/types.ts
@src/sync/service.ts
@src/sync/migration.sql
@src/components/home/LessonGrid.tsx
@src/monetization/hooks.ts
@app/(tabs)/progress.tsx
@app/(tabs)/index.tsx
@app/lesson/[id].tsx
@src/db/schema.ts
@src/state/hooks.ts
@src/state/provider.tsx
@src/__tests__/sync-service.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix sync blockers — infinite loop and PK mismatch</name>
  <files>
    src/sync/provider.tsx
    src/sync/tables.ts
    src/sync/types.ts
    src/sync/service.ts
  </files>
  <action>
**Issue 1 — SyncProvider infinite loop (BLOCKER):**

In `src/sync/provider.tsx`:
1. Add a `lastSyncedAtRef` using `useRef<Date | null>(null)` at line ~23 (after syncingRef).
2. In the `triggerSync` useCallback (line 24), in the catch block (line 61-69), replace `syncState.lastSyncedAt` with `lastSyncedAtRef.current`.
3. In the success paths (lines 42-59), after setting syncState, also update `lastSyncedAtRef.current = new Date()`.
4. Remove `syncState.lastSyncedAt` from the dependency array on line 73. Final deps: `[db, user, isAnonymous]`.

**Issue 2 — Sync PK/local_id mismatch (BLOCKER):**

In `src/sync/types.ts`:
- Add optional field `remoteKeyColumn?: string` to `TableSyncConfig` — when set, the local primaryKey value is mapped to this column name on the remote.
- Add optional field `onConflictColumns?: string[]` to `TableSyncConfig` — overrides the default `user_id,{primaryKey}` conflict target.

In `src/sync/tables.ts`:
- `lesson_attempts` config: keep `primaryKey: 'id'`, add `remoteKeyColumn: 'local_id'`, add `onConflictColumns: ['user_id', 'local_id']`.
- `question_attempts` config: same — add `remoteKeyColumn: 'local_id'`, add `onConflictColumns: ['user_id', 'local_id']`.
- `user_profile` config: add `onConflictColumns: ['user_id']` (single-row-per-user, no PK sent to remote).
- `habit` config: add `onConflictColumns: ['user_id']` (single-row-per-user, no PK sent to remote).

In `src/sync/service.ts`, in the push section (around line 93-127):
1. When building the remote record (line 104-111), check `config.remoteKeyColumn`:
   - If set: `record[config.remoteKeyColumn] = localRow[config.primaryKey]` instead of `record[config.primaryKey] = localRow[config.primaryKey]`.
   - For tables with `onConflictColumns` containing only `['user_id']` (user_profile, habit): do NOT include the local PK in the remote record at all.
2. When upserting (line 117-122), use `config.onConflictColumns ? config.onConflictColumns.join(',') : \`user_id,\${config.primaryKey}\`` as the onConflict value.
3. In the remote lookup (line 86-90): for tables with `remoteKeyColumn`, key the map by `remoteKeyColumn` value. For tables with `onConflictColumns: ['user_id']`, there's only one row — key by a sentinel like `'__single'` and match local rows to it unconditionally.
4. In the pull section (line 136-160): for `remoteKeyColumn` tables, match remote rows to local rows using the `remoteKeyColumn` -> local `primaryKey` mapping. For single-row tables (onConflictColumns=['user_id']), always match to the single local row.
  </action>
  <verify>
    <automated>cd "C:/Users/Nasif/Desktop/Iqra AI/tila-mobile" && npm run typecheck</automated>
  </verify>
  <done>
    - SyncProvider triggerSync deps no longer include syncState.lastSyncedAt — no re-render loop
    - lesson_attempts and question_attempts sync with local_id mapping to remote local_id column
    - user_profile and habit sync with user_id-only conflict (single row per user)
    - TypeScript compiles clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix UI/UX issues — premium flash, restore analytics, account prompt, useAppState</name>
  <files>
    src/components/home/LessonGrid.tsx
    app/(tabs)/index.tsx
    app/(tabs)/progress.tsx
    app/lesson/[id].tsx
  </files>
  <action>
**Issue 3 — Premium false-lock flash on home screen:**

In `src/components/home/LessonGrid.tsx`:
1. Add `subscriptionLoading?: boolean` to the `LessonGridProps` interface (line 118-127).
2. In the component destructuring (line 130-137), add `subscriptionLoading`.
3. On line 206, change the premium lock check: replace `!isPremiumActive` with `!isPremiumActive && !subscriptionLoading`. This matches `useCanAccessLesson` logic — while loading, treat as accessible.

In `app/(tabs)/index.tsx`:
1. On line 299, destructure `loading` from `useSubscription()`: `const { isPremiumActive, stage, trialDaysRemaining, showPaywall, loading: subLoading } = useSubscription();`
2. Pass `subscriptionLoading={subLoading}` to the `<LessonGrid>` component (around line 603).

**Issue 4 — Restore analytics using wrong function:**

In `app/(tabs)/progress.tsx`:
1. On line 28, add `trackRestoreFailed` to the import: `import { trackRestoreCompleted, trackRestoreFailed } from "../../src/monetization/analytics";`
2. On line 105 (the catch block), replace `trackRestoreCompleted({ success: false, entitlements_restored: 0 })` with `trackRestoreFailed({})`.

**Issue 5 — AccountPrompt ignores declined_at:**

In `app/lesson/[id].tsx`, in the account prompt useEffect (line 101-111):
1. Make the effect async (or use an inner async function).
2. Before setting `setShowAccountPrompt(true)`, query the DB: `const row = await db.getFirstAsync<{ account_prompt_declined_at: string | null }>('SELECT account_prompt_declined_at FROM user_profile WHERE id = 1');`
3. If `row?.account_prompt_declined_at` exists, parse it and check if it's within the last 7 days: `const declined = new Date(row.account_prompt_declined_at); const daysSince = (Date.now() - declined.getTime()) / (1000 * 60 * 60 * 24); if (daysSince < 7) return;`
4. Only call `setShowAccountPrompt(true)` if not declined within 7 days.
5. Add `account_prompt_declined_at` to the user_profile sync columns array in `src/sync/tables.ts` (in the user_profile config, add `'account_prompt_declined_at'` to the columns array).

**Issue 6 — Partial useAppState adoption:**

This issue is LOWER priority. Looking at the actual code:
- `app/(tabs)/index.tsx` uses `useProgress()` and `useHabit()` separately because it needs fine-grained access to `progress.updateProfile`, `progress.completeLesson`, etc. that `useAppState()` does not expose. The `AppStateProvider` wraps `useProgress` and `useHabit` internally but only exposes a read-only `progress` object.
- `app/(tabs)/progress.tsx` similarly needs `progress.completedLessonIds`, `progress.mastery`, etc. directly.
- Replacing these with `useAppState()` would require either: (a) expanding AppStateContextValue to expose all mutation methods, or (b) using both `useAppState()` AND individual hooks, which is worse.

**Decision: Skip Issue 6.** The current architecture is correct — individual hooks are the right choice when mutation methods are needed. `useAppState()` is designed for read-only consumers. Document this as intentional in a code comment in `src/state/hooks.ts`:
- Add a JSDoc comment above `useAppState`: `/** Read-only aggregate of progress + habit state. For screens that need mutation methods (completeLesson, updateProfile, recordPractice), use useProgress() and useHabit() directly. */`
  </action>
  <verify>
    <automated>cd "C:/Users/Nasif/Desktop/Iqra AI/tila-mobile" && npm run typecheck</automated>
  </verify>
  <done>
    - LessonGrid shows lessons as accessible while subscription is loading (no premium flash)
    - Restore failure path uses trackRestoreFailed (not trackRestoreCompleted with success:false)
    - AccountPrompt respects 7-day cooldown after decline
    - account_prompt_declined_at added to sync config
    - Issue 6 documented as intentional (useAppState is read-only; screens need mutation hooks)
  </done>
</task>

<task type="auto">
  <name>Task 3: Add sync test coverage for lesson_attempts and user_profile configs</name>
  <files>
    src/__tests__/sync-service.test.ts
  </files>
  <action>
Update the vi.mock for `../../src/sync/tables` to include three configs instead of one:
1. Keep existing `mastery_entities` config (no changes).
2. Add `lesson_attempts` config with: `localTable: 'lesson_attempts'`, `remoteTable: 'lesson_attempts'`, `primaryKey: 'id'`, `remoteKeyColumn: 'local_id'`, `onConflictColumns: ['user_id', 'local_id']`, `columns: ['lesson_id', 'accuracy', 'passed', 'duration_seconds', 'attempted_at']`, `timestampColumn: 'attempted_at'`, `hasAutoIncrement: true`.
3. Add `user_profile` config with: `localTable: 'user_profile'`, `remoteTable: 'user_profiles'`, `primaryKey: 'id'`, `onConflictColumns: ['user_id']`, `columns: ['onboarded', 'name', 'daily_goal']`, `timestampColumn: 'updated_at'`, `hasAutoIncrement: false`.

Also create matching TEST_CONFIG constants for each.

Add new describe block `'syncTable — lesson_attempts (local_id mapping)'`:
- Test: "maps local id to remote local_id on push". Create local row `{id: 42, lesson_id: 3, accuracy: 0.85, passed: 1, attempted_at: '2026-04-01T12:00:00Z'}`, empty remote. Assert pushed=1. Assert the upserted remote record has `local_id: 42` (not `id: 42`). Assert onConflict used `user_id,local_id`.
- Test: "pulls remote row and matches by local_id". Create remote row `{local_id: 42, lesson_id: 3, accuracy: 0.9, attempted_at: '2026-04-02T00:00:00Z'}`, local row `{id: 42, lesson_id: 3, accuracy: 0.85, attempted_at: '2026-04-01T12:00:00Z'}`. Assert pulled=1.

Add new describe block `'syncTable — user_profile (single-row upsert)'`:
- Test: "pushes single row with user_id-only conflict". Create local row `{id: 1, onboarded: 1, name: 'Ali', daily_goal: 5, updated_at: '2026-04-01T12:00:00Z'}`, empty remote. Assert pushed=1. Assert upserted record does NOT contain key `id`. Assert onConflict used `user_id` only.
- Test: "pulls remote row to single local row". Remote has `{user_id: 'user-1', onboarded: 1, name: 'Updated', updated_at: '2026-04-02T00:00:00Z'}`, local has `{id: 1, onboarded: 1, name: 'Old', updated_at: '2026-04-01T00:00:00Z'}`. Assert pulled=1.

To verify the onConflict and record shape, update `createMockSupabase` to capture the upsert options: store the last call's `options` arg so tests can assert `options.onConflict`.
  </action>
  <verify>
    <automated>cd "C:/Users/Nasif/Desktop/Iqra AI/tila-mobile" && npm test -- --run</automated>
  </verify>
  <done>
    - Tests cover lesson_attempts local_id mapping (push maps id->local_id, pull matches by local_id)
    - Tests cover user_profile single-row upsert (push excludes local id, onConflict is user_id only)
    - All existing tests still pass
    - New tests verify the exact onConflict column values
  </done>
</task>

</tasks>

<verification>
1. `npm run typecheck` — zero errors
2. `npm test -- --run` — all tests pass including new sync coverage
3. `npm run validate` — lint + typecheck clean
</verification>

<success_criteria>
- SyncProvider no longer re-renders infinitely (syncState.lastSyncedAt removed from triggerSync deps)
- lesson_attempts/question_attempts sync using local_id mapping with correct onConflict
- user_profile/habit sync as single-row-per-user with user_id-only conflict
- Home screen LessonGrid does not flash premium-locked during subscription loading
- Restore failure uses trackRestoreFailed, not trackRestoreCompleted
- AccountPrompt checks declined_at and skips if within 7 days
- Issue 6 documented as intentional (useAppState is read-only aggregate)
- 4 new test cases covering risky sync configs
</success_criteria>
