# Tila — Back Up To Speed (2026-04-20)

Returning after ~9 days. The last real commit was 2026-04-09. `.planning/STATE.md` is stale — it still points at the old UI overhaul (Phase 02 Quiz Experience). Your actual work has pivoted entirely to a different track: **Curriculum V2 / Lesson Quality Redesign**.

---

## Where the code actually is

- **You're on:** `main`
- **Active work branch:** `feature/curriculum-v2` ← this is where the last 3 weeks of work lives
- **Worktree:** `.worktrees/curriculum-v2` (per memory)
- **Architecture:** v2 runs in parallel with v1, gated by `CurriculumProvider` + feature flag. v1 is untouched except 3 integration seams (`_layout.tsx`, `lesson/[id].tsx`, `(tabs)/index.tsx`).

Untracked in working tree: new curriculum blueprint PDF + docx in `curriculum/`, plus a screenshot from 2026-04-08.

---

## What V2 is and why it exists

**Problem:** The first 18 lessons were pure quizzes — generic generators, random distractors, no teaching moment before testing. Felt like flashcards, not lessons.

**Solution:** Hybrid teach/practice model. Each `LessonV2` now has:
- `teachingSequence` — hand-authored intro (uses new `present` type + authored quiz items). No scoring on `present`.
- `exercisePlan` — generators (practice, review, checkpoints).
- `exitSequence` — hand-authored decode gate.

**Flow:** introduce → contrast → guided practice → decode.

Spec: `docs/superpowers/specs/2026-04-09-lesson-quality-redesign.md`
Plan: `docs/superpowers/plans/2026-04-09-lesson-quality-redesign.md`

---

## Status by layer

**Infrastructure — DONE:**
- v2 schema, validation, mastery state machine, scoring, SRS, phase unlocks, remediation
- Authoring toolkit + templates (`docs/curriculum-v2-authoring/`)
- Validator (`scripts/validate-v2.ts`) + 290+ engine tests
- `present` exercise type shipped (`PresentExercise.tsx`)
- Hybrid sequencing in `useLessonQuizV2` + scoring excludes `present`
- Authored item validation rules

**Content — DONE (structurally):**
- L1–L18 all authored in hybrid format
  - L1 (orientation) · L2 (early new-letter) · L3–L7 (Phase 1) · L8–L16 (Phase 2) · L17 (sukun) · L18 (checkpoint 2)

**Content — POLISH IN FLIGHT (last session):**
- L1 iterated: RTL letter order fix, 4 labeled audio buttons on side-by-side, no auto-play, 8-question quiz with 3 question types
- L2 iterated twice: explain harakat concept before testing, then rewrote from authored spec (fatha with ba and alif)

---

## The real blocker: teaching *quality*, not task completion

Per the memory from 2026-04-08: **infrastructure is strong, teaching quality is placeholder.** Distractors random, prompts quiz-like, build/read/fix flows don't teach.

**Decision made:** Golden-path mode. Don't author more content. Make 6 critical lessons genuinely teachable first, then define reusable quality patterns, then resume scaling.

**The 6 golden-path lessons:**

| # | Lesson | Template | Polished? |
|---|---|---|---|
| L1 | Arabic Starts Here (orientation) | 0A | ✅ heavy iteration last session |
| L2 | First decode (fatha + ba + alif) | 1A | ✅ heavy iteration last session |
| L5 | Decoding sprint | 3A | ⬜ not yet |
| L7 | Checkpoint 1 | 5 | ⬜ not yet |
| L13 | First connected reading | 4A | ⬜ not yet |
| L14 | Chain-breaking | 4B | ⬜ not yet |
| L18 | Checkpoint 2 + decode gate | 5 | ⬜ not yet |

(Note: your memory lists 6 lessons; the plan lists 7 golden paths. L2, L5, L7, L13, L14, L18 are the original six.)

---

## Priorities — do in this order

### 1. Verify L1 and L2 on device (this is literally where you left off)
Boot `feature/curriculum-v2`, run the vertical slice, do L1 and L2 end-to-end. Either sign them off as golden, or capture the next round of iteration notes. You had a screenshot from 2026-04-08 sitting in the repo root — worth reviewing before you touch anything.

### 2. Process the new blueprint
`curriculum/tila_curriculum_blueprint.pdf` + `Tila_Curriculum_Blueprint_v3.docx` are untracked. These landed after the last plan was written. Skim them — they may change L3–L6 authoring decisions before you keep iterating. Decide: ignore, fold in, or rewrite plan.

### 3. Move through remaining golden-path lessons
For each of L5, L7, L13, L14, L18: inspect on device → hand-author the weak moments → fix distractor pools → tighten prompts/feedback → sign off. Apply lessons learned from L1/L2 (no auto-play, labeled audio, teach before test).

### 4. Extract the quality pattern
Once 6 lessons feel right, codify the reusable pattern per exercise type. Then — and only then — resume scaling to the rest of the curriculum.

---

## Known deferreds (don't forget)

- `fix(join)` exercise type needs UI redesign (chain-breaking can't be expressed)
- Audio sourcing decision unresolved (letter sounds work; combos/chunks still placeholder)
- `EXPO_PUBLIC_CURRICULUM_OVERRIDE` env var flag didn't work in worktree — needs debugging
- Some v1 UI strings still say "Letter Recognition" as phase title
- `.planning/STATE.md` is stale and needs rewriting to reflect the V2 pivot

---

## Strategic frame (don't lose sight of)

From the master plan and product roadmap memories, V2 golden-path work is part of **Block 3: Make It Retain** (teaching quality improvements). But the real strategic gate is **Block 2: Convert** — paywall at L7, monetization polish. The current V2 thread is justifying the monetization pitch: if L1–L7 don't feel like real teaching, the paywall at L7 won't convert. So golden-path quality on L2, L5, L7 is directly on the revenue critical path, not just polish.

Revenue before polish applies here *in reverse*: the polish on the first 7 lessons *is* the revenue work.

---

## Suggested next command

```
git checkout feature/curriculum-v2
npm start   # then run L1 and L2 on a device
```

Then either `/gsd-verify-work` to walk L1/L2 UAT, or jump straight into L5 when L1/L2 feel done.
