---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/design/tokens.ts
  - src/design/animations.ts
  - src/design/components/ArabicText.tsx
  - src/__tests__/arabic-typography.test.ts
  - src/__tests__/animations.test.ts
autonomous: true
requirements: [FOUN-01, FOUN-03]

must_haves:
  truths:
    - "Arabic text with full diacritics renders without clipping at every size tier (display, quizHero, large, body)"
    - "A new quizHero Arabic size tier (52px) exists for quiz contexts"
    - "Animation tokens include breathing (4.5s cycle), drift (18-24s), and settle (600ms) tiers"
    - "Existing interaction animation timers are unchanged"
  artifacts:
    - path: "src/design/tokens.ts"
      provides: "Updated Arabic typography with lineHeight ratios >= 2.0x and new arabicQuizHero tier"
      contains: "arabicQuizHero"
    - path: "src/design/animations.ts"
      provides: "Breathing, drift, and settle animation tier tokens"
      exports: ["breathing", "drift", "settle"]
    - path: "src/design/components/ArabicText.tsx"
      provides: "quizHero size option and overflow visible"
      contains: "quizHero"
    - path: "src/__tests__/arabic-typography.test.ts"
      provides: "Typography token verification tests"
    - path: "src/__tests__/animations.test.ts"
      provides: "Extended animation tier tests"
  key_links:
    - from: "src/design/components/ArabicText.tsx"
      to: "src/design/tokens.ts"
      via: "typography.arabicQuizHero import"
      pattern: "typography\\.arabicQuizHero"
---

<objective>
Fix Arabic typography clipping and add animation tier tokens to the design system.

Purpose: Arabic text currently clips diacritics (lineHeight 1.39-1.50x vs needed 2.0-2.2x), and no breathing/drift animation tiers exist for the ambient system. These are the foundational tokens every subsequent plan depends on.
Output: Updated tokens.ts with correct lineHeight ratios + arabicQuizHero, updated ArabicText component with quizHero tier + overflow:visible, extended animations.ts with breathing/drift/settle tiers, and tests for both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-RESEARCH.md

@src/design/tokens.ts
@src/design/animations.ts
@src/design/components/ArabicText.tsx
@src/__tests__/animations.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/design/tokens.ts (current Arabic typography -- to be updated):
```typescript
arabicDisplay: { fontFamily: "Amiri_400Regular", fontSize: 72, lineHeight: 100 }, // 1.39x CLIPS
arabicLarge:   { fontFamily: "Amiri_400Regular", fontSize: 36, lineHeight: 54 },  // 1.50x CLIPS
arabicBody:    { fontFamily: "Amiri_400Regular", fontSize: 24, lineHeight: 36 },  // 1.50x CLIPS
```

From src/design/animations.ts (current exports -- to be extended):
```typescript
export const springs = { press, bouncy, gentle, snap };
export const durations = { fast: 150, micro: 200, normal: 300, slow: 400, dramatic: 600 };
export const staggers = { fast, normal, dramatic };
export const easings = { contentReveal, entrance, exit, smooth };
export const screenTransitions = { slideUp: 400, fade: 300, push: 350, feedback: 200 };
export const pressScale = { normal: 0.97, subtle: 0.98, bouncy: 0.95 };
```

From src/design/components/ArabicText.tsx (current type -- to be extended):
```typescript
type ArabicSize = "display" | "large" | "body";
const SIZE_MAP: Record<ArabicSize, { fontFamily: string; fontSize: number; lineHeight: number }>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix Arabic typography tokens and add quizHero tier</name>
  <files>src/design/tokens.ts, src/design/components/ArabicText.tsx, src/__tests__/arabic-typography.test.ts</files>
  <read_first>
    - src/design/tokens.ts (current broken lineHeight values)
    - src/design/components/ArabicText.tsx (current SIZE_MAP and ArabicSize type)
    - .planning/phases/01-foundation/01-UI-SPEC.md (target lineHeight values)
  </read_first>
  <behavior>
    - Test 1: typography.arabicDisplay has fontSize 72 and lineHeight 158 (ratio 2.20x) — per D-05
    - Test 2: typography.arabicQuizHero has fontSize 52 and lineHeight 114 (ratio 2.20x) — per D-07
    - Test 3: typography.arabicLarge has fontSize 36 and lineHeight 72 (ratio 2.00x) — per D-05
    - Test 4: typography.arabicBody has fontSize 24 and lineHeight 48 (ratio 2.00x) — per D-05
    - Test 5: All Arabic tiers have lineHeight >= 2.0 * fontSize
    - Test 6: ArabicText SIZE_MAP includes 'quizHero' key — per D-07
    - Test 7: ArabicText renders with overflow 'visible' in its style — per D-06
  </behavior>
  <action>
1. Create test file `src/__tests__/arabic-typography.test.ts`:
   - Import `typography` from `../design/tokens`
   - Test each Arabic tier has the exact fontSize and lineHeight from UI-SPEC:
     - arabicDisplay: fontSize 72, lineHeight 158
     - arabicQuizHero: fontSize 52, lineHeight 114
     - arabicLarge: fontSize 36, lineHeight 72
     - arabicBody: fontSize 24, lineHeight 48
   - Test all Arabic tiers (keys starting with "arabic") have lineHeight >= 2.0 * fontSize
   - Test that ArabicText source file contains `overflow` and `visible` (source audit pattern)
   - Test that ArabicText source file exports a type or SIZE_MAP that includes `quizHero`

2. Run tests (RED — they will fail because tokens still have old values).

3. Update `src/design/tokens.ts` typography object:
   - Change arabicDisplay lineHeight from 100 to 158
   - Add arabicQuizHero entry after arabicDisplay:
     ```
     arabicQuizHero: { fontFamily: fontFamilies.arabicRegular, fontSize: 52, lineHeight: 114 },
     ```
   - Change arabicLarge lineHeight from 54 to 72
   - Change arabicBody lineHeight from 36 to 48
   - Do NOT change any non-Arabic typography entries (per D-11, keep existing unchanged)

4. Update `src/design/components/ArabicText.tsx`:
   - Change ArabicSize type to: `type ArabicSize = "display" | "quizHero" | "large" | "body"`
   - Add to SIZE_MAP: `quizHero: typography.arabicQuizHero,`
   - Add `overflow: 'visible' as const` to the style array in the Text component, right after the writingDirection property (per D-06)

5. Run tests (GREEN).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/arabic-typography.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "arabicQuizHero" src/design/tokens.ts` returns >= 1
    - `grep "lineHeight: 158" src/design/tokens.ts` matches (arabicDisplay)
    - `grep "lineHeight: 114" src/design/tokens.ts` matches (arabicQuizHero)
    - `grep "lineHeight: 72" src/design/tokens.ts` matches (arabicLarge)
    - `grep "lineHeight: 48" src/design/tokens.ts` matches (arabicBody)
    - `grep "quizHero" src/design/components/ArabicText.tsx` matches in both type and SIZE_MAP
    - `grep "overflow" src/design/components/ArabicText.tsx` matches with 'visible'
    - `npx vitest run src/__tests__/arabic-typography.test.ts -x` exits 0
    - `npm run validate` exits 0
  </acceptance_criteria>
  <done>All four Arabic typography tiers have lineHeight >= 2.0x fontSize, arabicQuizHero (52px/114px) exists in tokens and ArabicText, overflow:visible applied to ArabicText, all tests pass, typecheck passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add breathing, drift, and settle animation tiers</name>
  <files>src/design/animations.ts, src/__tests__/animations.test.ts</files>
  <read_first>
    - src/design/animations.ts (current animation tokens)
    - src/__tests__/animations.test.ts (existing test structure to extend)
    - .planning/phases/01-foundation/01-UI-SPEC.md (exact animation values)
  </read_first>
  <behavior>
    - Test 1: breathing.inhale === 2000 — per D-09
    - Test 2: breathing.hold === 500 — per D-09
    - Test 3: breathing.exhale === 2000 — per D-09
    - Test 4: breathing.cycle === 4500 — per D-09
    - Test 5: breathing.opacityMin === 0.08 and opacityMax === 0.25
    - Test 6: breathing.scaleMin === 1.0 and scaleMax === 1.06
    - Test 7: drift.slow === 24000 — per D-10
    - Test 8: drift.normal === 18000 — per D-10
    - Test 9: drift.rangeX and drift.rangeY exist with min/max
    - Test 10: settle.duration === 600
    - Test 11: Existing springs, durations, staggers, easings unchanged
  </behavior>
  <action>
1. Extend `src/__tests__/animations.test.ts` with new describe blocks AFTER the existing tests (do not modify existing tests):

   Add `import { breathing, drift, settle }` to the existing import statement.

   Add describe("breathing tier"):
   - it("has inhale 2000ms"): expect(breathing.inhale).toBe(2000)
   - it("has hold 500ms"): expect(breathing.hold).toBe(500)
   - it("has exhale 2000ms"): expect(breathing.exhale).toBe(2000)
   - it("has cycle 4500ms"): expect(breathing.cycle).toBe(4500)
   - it("has opacity range 0.08-0.25"): expect(breathing.opacityMin).toBe(0.08); expect(breathing.opacityMax).toBe(0.25)
   - it("has scale range 1.0-1.06"): expect(breathing.scaleMin).toBe(1.0); expect(breathing.scaleMax).toBe(1.06)

   Add describe("drift tier"):
   - it("has slow 24000ms"): expect(drift.slow).toBe(24000)
   - it("has normal 18000ms"): expect(drift.normal).toBe(18000)
   - it("has rangeX min 20 max 40"): expect(drift.rangeX).toEqual({ min: 20, max: 40 })
   - it("has rangeY min 10 max 25"): expect(drift.rangeY).toEqual({ min: 10, max: 25 })

   Add describe("settle tier"):
   - it("has duration 600ms"): expect(settle.duration).toBe(600)

2. Run tests (RED — breathing/drift/settle not exported yet).

3. Add to `src/design/animations.ts` AFTER the existing `pressScale` export (per D-11 — keep existing unchanged):

   ```typescript
   // ── Ambient Tiers (Phase 1) ──

   export const breathing = {
     inhale: 2000,
     hold: 500,
     exhale: 2000,
     cycle: 4500,
     opacityMin: 0.08,
     opacityMax: 0.25,
     scaleMin: 1.0,
     scaleMax: 1.06,
   } as const;

   export const drift = {
     slow: 24000,
     normal: 18000,
     rangeX: { min: 20, max: 40 },
     rangeY: { min: 10, max: 25 },
   } as const;

   export const settle = {
     duration: 600,
   } as const;
   ```

4. Run tests (GREEN — all existing + new tests pass).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/animations.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep "breathing" src/design/animations.ts` returns matches for the export
    - `grep "drift" src/design/animations.ts` returns matches for the export
    - `grep "settle" src/design/animations.ts` returns matches for the export
    - `grep "inhale: 2000" src/design/animations.ts` matches
    - `grep "cycle: 4500" src/design/animations.ts` matches
    - `grep "slow: 24000" src/design/animations.ts` matches
    - `grep "normal: 18000" src/design/animations.ts` matches (drift.normal)
    - Existing springs.press still has stiffness 400 (unchanged per D-11)
    - `npx vitest run src/__tests__/animations.test.ts -x` exits 0
    - `npm run validate` exits 0
  </acceptance_criteria>
  <done>animations.ts exports breathing (4.5s cycle with opacity/scale ranges), drift (18-24s with X/Y displacement ranges), and settle (600ms) tiers. All existing interaction-tier tokens unchanged. All tests pass, typecheck passes.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/__tests__/arabic-typography.test.ts src/__tests__/animations.test.ts -x` — all pass
2. `npm run validate` — zero lint/type errors
3. `npm test` — full suite green (no regressions)
</verification>

<success_criteria>
- Arabic lineHeight ratios are 2.20x (display, quizHero) and 2.00x (large, body) in tokens.ts
- arabicQuizHero tier (52px/114px) exists in tokens and ArabicText component
- ArabicText renders with overflow:'visible'
- breathing, drift, settle exported from animations.ts with exact UI-SPEC values
- All existing animation tokens unchanged
- All tests pass, typecheck passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
