---
phase: 05-launch-ops-checklist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/privacy/index.html
  - app/(tabs)/progress.tsx
  - .planning/phases/05-launch-ops-checklist/PRIVACY-QUESTIONNAIRE.md
autonomous: true
requirements:
  - LAUNCH-01
  - LAUNCH-02

must_haves:
  truths:
    - "Privacy policy page exists as a standalone HTML file ready for GitHub Pages deployment"
    - "Privacy policy covers PostHog (consent-gated), Sentry (always-on crash reporting), and RevenueCat (purchase data)"
    - "Progress tab shows a tappable Privacy Policy link that opens the policy URL in the system browser"
    - "App Store Connect privacy questionnaire answers are documented with exact data types per SDK"
  artifacts:
    - path: "docs/privacy/index.html"
      provides: "Privacy policy page styled with Tila branding"
      contains: "PostHog"
    - path: "app/(tabs)/progress.tsx"
      provides: "In-app privacy policy link"
      contains: "PRIVACY_POLICY_URL"
    - path: ".planning/phases/05-launch-ops-checklist/PRIVACY-QUESTIONNAIRE.md"
      provides: "App Store Connect privacy nutrition label answers"
      contains: "Data Used to Track You"
  key_links:
    - from: "app/(tabs)/progress.tsx"
      to: "https://tila-app.github.io/privacy/"
      via: "Linking.openURL"
      pattern: "Linking\\.openURL.*privacy"
---

<objective>
Create the privacy policy, add an in-app link to it, and document App Store Connect privacy questionnaire answers.

Purpose: LAUNCH-01 requires a privacy policy URL accessible in-app and ready for App Store Connect. LAUNCH-02 requires documented privacy nutrition label answers. These are submission blockers.
Output: Privacy policy HTML file, in-app link in Progress tab, privacy questionnaire document.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-launch-ops-checklist/05-CONTEXT.md
@.planning/phases/05-launch-ops-checklist/05-RESEARCH.md

<interfaces>
<!-- From app/(tabs)/progress.tsx — relevant imports and structure -->
<!-- The privacy link goes between the Restore Purchases button (line ~312) and Reset Progress button (line ~329) -->

From app/(tabs)/progress.tsx:
```typescript
import { Pressable, Text, Linking } from "react-native";
import { useColors } from "../../src/design/theme";
import { typography, spacing } from "../../src/design/tokens";

// Existing styles to match:
// restoreButton: { alignSelf: "center", paddingVertical: spacing.md, paddingHorizontal: spacing.xl, borderWidth: 1, borderRadius: 8, marginTop: spacing.xxxl, minHeight: 44 }
// resetButton: { marginTop: spacing.xxxxl, paddingVertical: spacing.md, alignItems: "center", borderTopWidth: 1 }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create privacy policy HTML and privacy questionnaire document</name>
  <files>docs/privacy/index.html, .planning/phases/05-launch-ops-checklist/PRIVACY-QUESTIONNAIRE.md</files>
  <read_first>
    - src/analytics/posthog.ts
    - src/analytics/sentry.ts
    - src/monetization/revenuecat.ts
    - .planning/phases/05-launch-ops-checklist/05-RESEARCH.md
  </read_first>
  <action>
    **Create `docs/privacy/index.html`** (per D-01, D-02, D-03):

    A single-file HTML page with inline CSS, styled with Tila branding:
    - Background: `#F8F6F0` (warm cream)
    - Text color: `#163323` (dark green)
    - Fonts: Inter (body) and Lora (headings) via Google Fonts CDN
    - Max-width: 720px centered, responsive
    - Last updated date: 2026-04-01

    Sections (per research Privacy Policy Structure):
    1. **Information We Collect** — three subsections:
       - Analytics Data (PostHog): event names, event properties (lesson completion, quiz scores, navigation), anonymous user ID (UUID), device type, OS version, app version. Consent-gated — only collected when user opts in.
       - Crash Reports (Sentry): stack traces, device info (model, OS version), app state at crash time. Always on in production. No PII collected (sendDefaultPii is false by default).
       - Purchase Data (RevenueCat): purchase history, subscription status. Processed by RevenueCat and Apple. No credit card data touches our servers.
    2. **How We Use Your Information** — improve app quality, fix crashes, process subscriptions
    3. **Third-Party Services** — PostHog (analytics), Sentry (crash reporting), RevenueCat (subscriptions), Apple (payments). Link to each service's privacy policy.
    4. **Data Retention** — analytics data retained per PostHog defaults, crash data per Sentry defaults, purchase data per RevenueCat/Apple
    5. **Your Choices** — analytics consent toggle (PostHog only collected with consent), subscription management via Apple Settings
    6. **Children's Privacy** — app is designed for users aged 13 and older, does not knowingly collect data from children under 13 (per research COPPA section)
    7. **Contact Information** — privacy@tila.app (or similar placeholder email for user to update)
    8. **Changes to This Policy** — updates posted to this page with revised date

    Add `<meta>` tags: viewport for mobile, charset utf-8, title "Privacy Policy - Tila".

    **Create `.planning/phases/05-launch-ops-checklist/PRIVACY-QUESTIONNAIRE.md`** (per D-05, D-06, D-07):

    Document the exact App Store Connect privacy nutrition label answers:

    ```
    ## Data Used to Track You
    None — no IDFA collection, no cross-app tracking (per D-07, PostHog SDK verified no IDFA)

    ## Data Linked to You
    - Purchase History (RevenueCat) — Purpose: App Functionality
    - Usage Data: Product Interaction (PostHog, consent-gated) — Purpose: Analytics

    ## Data Not Linked to You
    - Crash Data (Sentry) — Purpose: App Functionality
    - Performance Data (Sentry) — Purpose: App Functionality

    ## App Store Connect Answers
    - "Does your app collect data?" → Yes
    - "Does your app track users?" → No
    - Category: each data type listed above with purpose and linkage
    ```

    Include the exact PostHog IDFA finding: "PostHog React Native SDK v4.39.0 contains zero references to IDFA, advertisingId, or ATTrackingManager — verified by source inspection."
  </action>
  <verify>
    <automated>test -f docs/privacy/index.html && grep -q "PostHog" docs/privacy/index.html && grep -q "Sentry" docs/privacy/index.html && grep -q "RevenueCat" docs/privacy/index.html && grep -q "13" docs/privacy/index.html && test -f .planning/phases/05-launch-ops-checklist/PRIVACY-QUESTIONNAIRE.md && grep -q "Data Used to Track You" .planning/phases/05-launch-ops-checklist/PRIVACY-QUESTIONNAIRE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/privacy/index.html` exists and contains the strings "PostHog", "Sentry", "RevenueCat", "13" (age), "consent"
    - File has `<meta name="viewport"` tag for mobile responsiveness
    - Background color `#F8F6F0` appears in CSS
    - `.planning/phases/05-launch-ops-checklist/PRIVACY-QUESTIONNAIRE.md` exists
    - Questionnaire doc contains "Data Used to Track You", "None", "Data Linked to You", "Purchase History", "Crash Data"
  </acceptance_criteria>
  <done>Privacy policy HTML is ready for GitHub Pages deployment. Privacy questionnaire document contains exact App Store Connect answers for all three SDKs.</done>
</task>

<task type="auto">
  <name>Task 2: Add privacy policy link to Progress tab</name>
  <files>app/(tabs)/progress.tsx</files>
  <read_first>
    - app/(tabs)/progress.tsx
  </read_first>
  <action>
    **Add in-app privacy policy link** (per D-04 — Claude's Discretion: Progress tab near Restore Purchases):

    1. Add `Linking` to the `react-native` import at the top of `app/(tabs)/progress.tsx` (line 1-8 already imports from react-native — add Linking to the destructured imports).

    2. Add a constant near the top of the component (or outside it):
       ```typescript
       const PRIVACY_POLICY_URL = 'https://tila-app.github.io/privacy/';
       ```

    3. Add a Privacy Policy link between the Restore Purchases section (ends at line ~326) and the Reset Progress button (starts at line ~329). Insert this JSX:
       ```tsx
       {/* Privacy Policy */}
       <Pressable
         onPress={() => Linking.openURL(PRIVACY_POLICY_URL)}
         style={styles.privacyLink}
       >
         <Text style={[typography.bodySmall, { color: colors.textMuted }]}>
           Privacy Policy
         </Text>
       </Pressable>
       ```

    4. Add `privacyLink` style to the StyleSheet (place it between `restoreButton` and `resetButton`):
       ```typescript
       privacyLink: {
         alignSelf: "center",
         paddingVertical: spacing.sm,
         marginTop: spacing.lg,
       },
       ```

    The link appears for ALL users (not gated behind subscription status) — Apple requires the privacy policy to be accessible from within the app at all times.

    5. Run `npm run validate` to confirm no lint or typecheck errors.
  </action>
  <verify>
    <automated>cd "C:\Users\Nasif\Desktop\Iqra AI\tila-mobile" && grep -q "PRIVACY_POLICY_URL" app/\(tabs\)/progress.tsx && grep -q "Linking.openURL" app/\(tabs\)/progress.tsx && grep -q "privacyLink" app/\(tabs\)/progress.tsx && npm run validate 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `app/(tabs)/progress.tsx` contains `const PRIVACY_POLICY_URL = 'https://tila-app.github.io/privacy/'`
    - File contains `Linking.openURL(PRIVACY_POLICY_URL)`
    - File contains `privacyLink` style definition with `alignSelf: "center"`
    - `Linking` is imported from `react-native`
    - `npm run validate` exits with 0 (no lint or typecheck errors)
  </acceptance_criteria>
  <done>Progress tab shows a tappable "Privacy Policy" link visible to all users. Link opens the GitHub Pages privacy policy URL in the system browser. Lint and typecheck pass.</done>
</task>

</tasks>

<verification>
- `docs/privacy/index.html` exists with all required sections covering PostHog, Sentry, RevenueCat
- `.planning/phases/05-launch-ops-checklist/PRIVACY-QUESTIONNAIRE.md` contains exact nutrition label answers
- `app/(tabs)/progress.tsx` has privacy policy link using `Linking.openURL`
- `npm run validate` passes with zero errors
</verification>

<success_criteria>
- Privacy policy HTML file is complete, branded, and ready for GitHub Pages deployment
- Privacy questionnaire document maps every SDK to its App Store Connect data type declaration
- In-app privacy link is accessible from the Progress tab for all users
- All code changes pass lint and typecheck
</success_criteria>

<output>
After completion, create `.planning/phases/05-launch-ops-checklist/05-01-SUMMARY.md`
</output>
