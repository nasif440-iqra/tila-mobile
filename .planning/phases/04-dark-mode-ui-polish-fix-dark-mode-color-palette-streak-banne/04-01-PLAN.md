---
phase: 04-dark-mode-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/design/tokens.ts
  - src/design/atmosphere/AtmosphereBackground.tsx
  - src/components/onboarding/ProgressBar.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-08

must_haves:
  truths:
    - "Dark mode backgrounds use warm charcoal-brown tones, not cold green"
    - "Dark mode text uses warm cream tones, not cold grays"
    - "Gold accent (#C4A464) remains unchanged in dark mode"
    - "AtmosphereBackground dark presets use warm brown gradients"
    - "Onboarding progress bar is a thin, barely visible line"
  artifacts:
    - path: "src/design/tokens.ts"
      provides: "Warm earth tone dark color palette"
      contains: "darkColors"
    - path: "src/design/atmosphere/AtmosphereBackground.tsx"
      provides: "Updated DARK_PRESETS with warm brown gradients"
      contains: "DARK_PRESETS"
    - path: "src/components/onboarding/ProgressBar.tsx"
      provides: "Minimal thin progress bar"
      contains: "height"
  key_links:
    - from: "src/design/theme.ts"
      to: "src/design/tokens.ts"
      via: "imports darkColors"
      pattern: "darkColors"
    - from: "src/design/atmosphere/AtmosphereBackground.tsx"
      to: "src/design/tokens.ts"
      via: "uses colors from useColors()"
      pattern: "useColors"
---

<objective>
Rewrite the dark mode color palette from cold green to warm earth tones, update atmosphere dark presets to match, and restyle the onboarding progress bar to be minimal.

Purpose: Dark mode currently feels like a sci-fi terminal with cold green tones. It should feel like a dimly lit mosque at night -- warm, contemplative, inviting. The progress bar should blend into the atmosphere rather than demanding attention.

Output: Updated darkColors tokens, updated DARK_PRESETS, restyled ProgressBar component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dark-mode-ui-polish-fix-dark-mode-color-palette-streak-banne/04-CONTEXT.md

<interfaces>
<!-- Key types the executor needs -->

From src/design/tokens.ts:
```typescript
export const lightColors = {
  bg: "#F8F6F0",
  bgWarm: "#F2EADE",
  bgCard: "#FFFFFF",
  primary: "#163323",
  primaryLight: "#255038",
  primarySoft: "#E8F0EB",
  primaryDark: "#0F2419",
  accent: "#C4A464",
  accentLight: "#F5EDDB",
  accentGlow: "rgba(196, 164, 100, 0.3)",
  danger: "#BD524D",
  dangerLight: "#FCE6E5",
  dangerDark: "#7A2E2B",
  text: "#163323",
  textSoft: "#52545C",
  textMuted: "#6B6760",
  border: "#EBE6DC",
  brown: "#3D2B1F",
  brownLight: "#5C4033",
  brownSoft: "#F5EDE4",
  white: "#FFFFFF",
  black: "#000000",
} as const;

export type ColorTokens = typeof lightColors;
```

From src/design/atmosphere/AtmosphereBackground.tsx:
```typescript
export type AtmospherePreset = "home" | "quiz" | "sacred" | "celebration" | "loading" | "onboarding";

interface PresetConfig {
  linearColors: [string, string];
  glowPositionX: string;
  glowPositionY: string;
  glowColor: string;
  glowOpacity: number;
  glowRadius: number;
  floatingLetters: boolean;
  floatingLetterSpeed: "normal" | "slow";
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite darkColors to warm earth tone palette</name>
  <files>src/design/tokens.ts</files>
  <read_first>src/design/tokens.ts, src/design/theme.ts</read_first>
  <action>
Replace the entire `darkColors` object in `src/design/tokens.ts` with warm earth tones per D-01. The palette should evoke "a dimly lit mosque at night" -- deep charcoal-brown backgrounds, warm cream text, gold accents.

New darkColors values:
```
bg: "#1A1613"           // deep warm charcoal-brown (was #0F1A14 cold green)
bgWarm: "#211D18"       // slightly lighter warm brown (was #142019 cold green)
bgCard: "#2A2420"       // card surface, warm brown (was #1C2A22 cold green)
primary: "#E8DCC8"      // warm cream for primary actions (was #A8D5BA mint green)
primaryLight: "#C4B89C" // muted warm cream (was #7ABF95 green)
primarySoft: "#2A2420"  // same as card bg for soft containers (was #1E3328 green)
primaryDark: "#F2EBE0"  // brightest cream for emphasis (was #D4EAE0 green)
accent: "#C4A464"       // UNCHANGED per D-02 -- gold stays gold
accentLight: "#2A2218"  // dark gold tint container (was #2A2418)
accentGlow: "rgba(196, 164, 100, 0.2)"  // unchanged
danger: "#E07672"       // keep warm red (unchanged)
dangerLight: "#2A1A1A"  // keep dark red container (unchanged)
dangerDark: "#F0A8A6"   // keep light danger text (unchanged)
text: "#EDE8E0"         // warm cream text (was #E8E4DC slightly cooler)
textSoft: "#A89E94"     // warm muted text (was #A0A4AC cold gray)
textMuted: "#8A8078"    // warm muted (was #8A8680 neutral gray)
border: "#332C26"       // warm brown border (was #2A3028 green-gray)
brown: "#D4C4B0"        // keep (unchanged)
brownLight: "#B8A898"   // keep (unchanged)
brownSoft: "#2A2420"    // warm brown soft (was #2A2420 same)
white: "#FFFFFF"        // keep
black: "#000000"        // keep
```

Also update `darkShadows` to use warm shadow colors:
- Change all `shadowColor` from "#000000" to "#0D0A08" (very dark warm brown instead of pure black).

DO NOT change lightColors, typography, spacing, radii, borderWidths, or the light shadows object.
  </action>
  <verify>
    <automated>cd "C:\Users\Nasif\Desktop\Iqra AI\tila-mobile" && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - darkColors.bg is "#1A1613" (grep: `bg: "#1A1613"`)
    - darkColors.bgWarm is "#211D18" (grep: `bgWarm: "#211D18"`)
    - darkColors.primary is "#E8DCC8" (grep: `primary: "#E8DCC8"`)
    - darkColors.accent remains "#C4A464" (grep: accent unchanged per D-02)
    - darkColors.text is "#EDE8E0" (grep: `text: "#EDE8E0"`)
    - darkColors.textSoft is "#A89E94" (grep: `textSoft: "#A89E94"`)
    - darkColors.border is "#332C26" (grep: `border: "#332C26"`)
    - darkShadows shadowColor uses "#0D0A08" not "#000000"
    - lightColors object is completely unchanged
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>darkColors uses warm earth tones (charcoal-brown bg, cream text, gold accent preserved). No green tones remain. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Update AtmosphereBackground DARK_PRESETS with warm brown gradients</name>
  <files>src/design/atmosphere/AtmosphereBackground.tsx</files>
  <read_first>src/design/atmosphere/AtmosphereBackground.tsx, src/design/tokens.ts</read_first>
  <action>
Update the `DARK_PRESETS` object in AtmosphereBackground.tsx per D-03. Replace all cold green gradient colors with warm brown gradients that match the new darkColors palette.

New DARK_PRESETS linearColors (replacing cold green #0F1A14 / #142019):
- home: ["#1A1613", "#211D18"] (matches new bg/bgWarm)
- quiz: ["#1A1613", "#211D18"]
- sacred: ["#211D18", "#1A1613"] (reversed for depth variation)
- celebration: ["#1A1613", "#211D18"]
- loading: ["#1A1613", "#1A1613"] (uniform)
- onboarding: ["#211D18", "#1A1613"] (reversed)

Keep all other preset properties unchanged (glow positions, radii, floatingLetters, speeds).
The glowColor values ("rgba(196, 164, 100, 0.2)") should remain unchanged since gold glow works with warm earth tones.

DO NOT modify the light PRESETS object at all.
  </action>
  <verify>
    <automated>cd "C:\Users\Nasif\Desktop\Iqra AI\tila-mobile" && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - DARK_PRESETS.home.linearColors contains "#1A1613" (grep: `"#1A1613"`)
    - DARK_PRESETS contains no instances of "#0F1A14" or "#142019" (old cold green colors gone)
    - Light PRESETS object still contains "#F8F6F0" (unchanged)
    - All 6 presets still exist in DARK_PRESETS (home, quiz, sacred, celebration, loading, onboarding)
    - TypeScript compiles cleanly
  </acceptance_criteria>
  <done>DARK_PRESETS uses warm brown gradients matching new darkColors. No cold green gradients remain. Light presets unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle onboarding ProgressBar to thin minimal line</name>
  <files>src/components/onboarding/ProgressBar.tsx</files>
  <read_first>src/components/onboarding/ProgressBar.tsx, src/components/onboarding/OnboardingFlow.tsx</read_first>
  <action>
Per D-08, restyle ProgressBar to be a thin, barely-there line that blends into the atmosphere.

Changes to ProgressBar.tsx:
1. Reduce track height from 2.5 to 1.5 (thinner line)
2. Reduce fill border radius to match (1)
3. Lower the opacity of the track background: change `backgroundColor: colors.border` to use an rgba equivalent at 40% opacity. Do this by wrapping in a View and applying `opacity: 0.4` to the track style.
4. Change gap from 3 to 2 (tighter segments)
5. The fill color should remain `colors.primary` but at reduced opacity -- add `opacity: 0.6` to the fill style.

The component keeps the same props interface and same segmented behavior (one segment per step). It just becomes visually quieter.

DO NOT change OnboardingFlow.tsx -- only the ProgressBar component itself.
  </action>
  <verify>
    <automated>cd "C:\Users\Nasif\Desktop\Iqra AI\tila-mobile" && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Track height is 1.5 (grep: `height: 1.5`)
    - Gap is 2 (grep: `gap: 2`)
    - Track has opacity: 0.4 applied
    - Fill has opacity: 0.6 applied
    - Component still exports ProgressBar function
    - TypeScript compiles cleanly
  </acceptance_criteria>
  <done>ProgressBar is a thin, barely-visible line. Tracks and fills have reduced opacity. Blends into atmosphere rather than demanding attention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected. This plan modifies visual tokens and UI components only. No data flow, auth, or external service changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | I (Information Disclosure) | tokens.ts | accept | Color tokens are static visual values with no sensitive data |
</threat_model>

<verification>
1. `npm run typecheck` passes with zero errors
2. Dark mode colors contain no green tones (#0F1A14, #142019, #A8D5BA, #7ABF95 all gone from darkColors and DARK_PRESETS)
3. Gold accent #C4A464 unchanged in both light and dark modes
4. ProgressBar renders thinner than before (height 1.5 vs 2.5)
5. Light mode colors and PRESETS completely unmodified
</verification>

<success_criteria>
- Dark mode palette feels warm (charcoal-brown, cream, gold) instead of cold (green, mint)
- AtmosphereBackground dark gradients match the new warm palette
- Onboarding progress bar is thin and unobtrusive
- Zero TypeScript errors, zero runtime regressions in light mode
</success_criteria>

<output>
After completion, create `.planning/phases/04-dark-mode-ui-polish-fix-dark-mode-color-palette-streak-banne/04-01-SUMMARY.md`
</output>
