---
phase: 07-engine-typescript-migration
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/engine/questions/shared.ts
  - src/engine/questions/recognition.ts
  - src/engine/questions/sound.ts
  - src/engine/questions/contrast.ts
  - src/engine/questions/harakat.ts
  - src/engine/questions/checkpoint.ts
  - src/engine/questions/connectedForms.ts
  - src/engine/questions/connectedReading.ts
  - src/engine/questions/review.ts
  - src/engine/questions/explanations.ts
  - src/engine/questions/index.ts
autonomous: true
requirements: [RET-01]

must_haves:
  truths:
    - "All 11 question generator .js files migrated to .ts"
    - "No `any` in exported function signatures"
    - "npm run typecheck error count does not increase from baseline (~24 lines)"
    - "npm test passes with all existing tests"
    - "questions/index.d.ts deleted after index.js becomes index.ts"
  artifacts:
    - path: "src/engine/questions/shared.ts"
      provides: "Typed shared question utilities"
    - path: "src/engine/questions/index.ts"
      provides: "Typed question dispatcher"
---

<objective>
Migrate all 11 question generator files (shared, recognition, sound, contrast, harakat, checkpoint, connectedForms, connectedReading, review, explanations, index) from JavaScript to TypeScript with full type annotations.

Purpose: Complete the question layer migration per the leaf-first strategy (D-08 waves 2-4).
Output: 11 .ts files replacing their .js originals, index.d.ts deleted.
</objective>

<tasks>
<task id="1" type="auto">
  <name>Migrate shared.js to TypeScript</name>
  <action>Rename shared.js to shared.ts and add type annotations to all 16 exported functions/constants. Use Question, QuestionOption from types/question.ts and ArabicLetter from types/engine.ts. Type SOUND_CONFUSION_MAP as Record<number, number[]>, SOUND_PROMPTS with proper string array types.</action>
  <done>shared.ts exists with typed exports, shared.js deleted, typecheck passes, tests pass</done>
</task>

<task id="2" type="auto">
  <name>Migrate recognition.js, sound.js, contrast.js to TypeScript</name>
  <action>Rename all three files to .ts and add Lesson parameter types and Question[] return types. These are simple generators that depend on shared.ts.</action>
  <done>All three .ts files exist with typed exports, .js files deleted, typecheck passes, tests pass</done>
</task>

<task id="3" type="auto">
  <name>Migrate harakat.js, checkpoint.js, explanations.js to TypeScript</name>
  <action>Rename all three files to .ts. harakat.js uses Harakah/HarakatCombo from types/engine.ts. checkpoint.js uses progress parameter typing. explanations.js returns strings.</action>
  <done>All three .ts files exist with typed exports, .js files deleted, typecheck passes, tests pass</done>
</task>

<task id="4" type="auto">
  <name>Migrate connectedForms.js, connectedReading.js, review.js to TypeScript</name>
  <action>Rename all three files to .ts. connectedForms.js is the largest (497 lines) with many internal helper functions. connectedReading.js and review.js use similar patterns.</action>
  <done>All three .ts files exist with typed exports, .js files deleted, typecheck passes, tests pass</done>
</task>

<task id="5" type="auto">
  <name>Migrate index.js to TypeScript and delete index.d.ts</name>
  <action>Rename index.js to index.ts with proper type annotations on generateLessonQuestions and generateHybridExercises. Delete index.d.ts since the .ts file now provides its own types.</action>
  <done>index.ts exists with typed exports, index.js and index.d.ts deleted, typecheck passes, all tests pass</done>
</task>
</tasks>

<verification>
- npm run typecheck: error count <= 24 (baseline)
- npm test: all existing tests pass
- No `any` in exported function signatures across all migrated files
</verification>
