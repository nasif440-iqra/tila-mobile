---
phase: 02-onboarding-wow-factor
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/onboarding-flow.test.ts
  - src/__tests__/bismillah.test.ts
  - src/__tests__/warm-glow.test.ts
  - src/__tests__/onboarding-animations.test.ts
autonomous: true
requirements: [ONB-01, ONB-02, ONB-03, ONB-04, MIND-01, MIND-02]
must_haves:
  truths:
    - "Test stubs exist for all Phase 2 requirements before implementation begins"
    - "Each test file has at least one describe block and one placeholder test"
    - "npm test passes with all stubs (tests may be skipped/todo but not failing)"
  artifacts:
    - path: "src/__tests__/onboarding-flow.test.ts"
      provides: "Test stubs for ONB-01, ONB-02, ONB-03"
      exports: []
    - path: "src/__tests__/bismillah.test.ts"
      provides: "Test stubs for MIND-01, MIND-02"
      exports: []
    - path: "src/__tests__/warm-glow.test.ts"
      provides: "Test stubs for ONB-04"
      exports: []
    - path: "src/__tests__/onboarding-animations.test.ts"
      provides: "Test stubs for ONB-03 animation constants"
      exports: []
  key_links: []
---

<objective>
Create test stub files for all Phase 2 requirements so that implementation plans have verification targets from the start.

Purpose: The RESEARCH.md Validation Architecture defines 4 test files that must exist before implementation. Creating them in Wave 0 ensures every subsequent plan can run `npm test` against real test files, and executors know what behaviors to satisfy.
Output: Four test stub files with describe blocks and todo/placeholder tests covering ONB-01 through ONB-04 and MIND-01 through MIND-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-onboarding-wow-factor/02-CONTEXT.md
@.planning/phases/02-onboarding-wow-factor/02-RESEARCH.md

<interfaces>
<!-- From RESEARCH.md Validation Architecture — these are the test files to create -->

Test file mapping:
- src/__tests__/onboarding-flow.test.ts — covers ONB-01, ONB-02, ONB-03
- src/__tests__/bismillah.test.ts — covers MIND-01, MIND-02
- src/__tests__/warm-glow.test.ts — covers ONB-04
- src/__tests__/onboarding-animations.test.ts — covers ONB-03

From existing test patterns (src/__tests__/animations.test.ts):
```typescript
import { describe, it, expect } from "vitest";
// Tests import from source and assert exports/values
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create onboarding-flow and bismillah test stubs</name>
  <files>src/__tests__/onboarding-flow.test.ts, src/__tests__/bismillah.test.ts</files>
  <read_first>
    - src/__tests__/animations.test.ts (pattern reference for test structure)
    - src/__tests__/haptics.test.ts (pattern reference)
  </read_first>
  <action>
    **Create src/__tests__/onboarding-flow.test.ts:**

    Use `describe`/`it`/`it.todo` from Vitest. Structure:

    ```typescript
    import { describe, it, expect } from "vitest";

    describe("OnboardingFlow", () => {
      describe("ONB-01: Flow renders all 9 steps", () => {
        it.todo("exports STEP constant with 9 named entries");
        it.todo("STEP_NAMES array has 9 entries including 'bismillah' at index 4");
        it.todo("TOTAL_STEPS equals 9");
      });

      describe("ONB-02: LetterReveal sacred moment", () => {
        it.todo("LetterReveal imports STILLNESS_BEAT_DURATION from animations");
        it.todo("hapticMilestone is called during letter reveal");
      });

      describe("ONB-03: Step transitions use correct presets", () => {
        it.todo("all step renders use STEP.* constants, not raw numbers");
        it.todo("FloatingLettersLayer visible condition uses STEP.STARTING_POINT");
      });
    });
    ```

    Note: These stubs reference STEP and STEP_NAMES which do not exist yet (created in Plan 02). The todo tests will not attempt to import or fail. Add a comment at the top: `// Forward-looking stubs — STEP constants created in Plan 02-02`.

    **Create src/__tests__/bismillah.test.ts:**

    ```typescript
    import { describe, it, expect } from "vitest";

    describe("BismillahMoment", () => {
      describe("MIND-01: Renders with correct text and auto-advances", () => {
        it.todo("renders Bismillah Arabic text");
        it.todo("calls hapticSelection on mount");
        it.todo("auto-advances after BISMILLAH_DISPLAY_DURATION");
        it.todo("has no button or skip mechanism");
      });
    });

    describe("BismillahOverlay", () => {
      describe("MIND-02: Session detection works correctly", () => {
        it.todo("shouldShowBismillah returns true on first call");
        it.todo("shouldShowBismillah returns false after markBismillahShown");
        it.todo("overlay auto-fades after 2500ms with 500ms fade duration");
        it.todo("BismillahOverlay renders with bgWarm background at 0.97 opacity");
      });
    });
    ```

    Added stub for overlay opacity per review suggestion (catches Pitfall 4).

    Use `it.todo(...)` for all tests — these are stubs that will be filled in during or after implementation. The point is to establish the test file structure and verify `npm test` does not fail.
  </action>
  <verify>
    <automated>cd "C:/Users/Nasif/Desktop/Iqra AI/tila-mobile" && npm test && npx vitest run src/__tests__/onboarding-flow.test.ts src/__tests__/bismillah.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Both test files exist with describe blocks and todo tests
    - onboarding-flow.test.ts covers ONB-01, ONB-02, ONB-03
    - bismillah.test.ts covers MIND-01, MIND-02
    - bismillah.test.ts includes overlay opacity stub (Pitfall 4 guard)
    - npm test does not fail
  </acceptance_criteria>
  <done>Both test files exist with describe blocks and todo tests covering ONB-01, ONB-02, ONB-03, MIND-01, MIND-02. npm test does not fail.</done>
</task>

<task type="auto">
  <name>Task 2: Create warm-glow and onboarding-animations test stubs</name>
  <files>src/__tests__/warm-glow.test.ts, src/__tests__/onboarding-animations.test.ts</files>
  <read_first>
    - src/__tests__/animations.test.ts (pattern reference)
    - src/components/onboarding/animations.ts (current exports to verify)
  </read_first>
  <action>
    **Create src/__tests__/warm-glow.test.ts:**

    ```typescript
    import { describe, it, expect } from "vitest";

    describe("WarmGlow", () => {
      describe("ONB-04: Animated variant", () => {
        it.todo("exports WarmGlow function component");
        it.todo("animated prop defaults to false");
        it.todo("accepts color, pulseMin, pulseMax props");
        it.todo("static mode (animated=false) does not use Animated.View");
      });
    });
    ```

    Added backward-compatibility stub per review suggestion (Pitfall 5 guard).

    **Create src/__tests__/onboarding-animations.test.ts:**

    This file should have ONE real test (not todo) that verifies the existing animation constants are exported, plus todo stubs for the new constants that Plan 01 will add.

    ```typescript
    import { describe, it, expect } from "vitest";
    import {
      STAGGER_BASE,
      STAGGER_DURATION,
      SPLASH_STAGGER_BASE,
      SPLASH_STAGGER_DURATION,
      CTA_DELAY_OFFSET,
      CTA_DURATION,
    } from "../components/onboarding/animations";

    describe("Onboarding Animation Constants", () => {
      describe("ONB-03: Existing constants preserved", () => {
        it("exports STAGGER_BASE as a number", () => {
          expect(typeof STAGGER_BASE).toBe("number");
          expect(STAGGER_BASE).toBe(150);
        });

        it("exports STAGGER_DURATION as a number", () => {
          expect(typeof STAGGER_DURATION).toBe("number");
          expect(STAGGER_DURATION).toBe(500);
        });

        it("exports SPLASH_STAGGER_BASE as a number", () => {
          expect(typeof SPLASH_STAGGER_BASE).toBe("number");
          expect(SPLASH_STAGGER_BASE).toBe(250);
        });

        it("exports SPLASH_STAGGER_DURATION as a number", () => {
          expect(typeof SPLASH_STAGGER_DURATION).toBe("number");
          expect(SPLASH_STAGGER_DURATION).toBe(700);
        });

        it("exports CTA_DELAY_OFFSET as a number", () => {
          expect(typeof CTA_DELAY_OFFSET).toBe("number");
          expect(CTA_DELAY_OFFSET).toBe(200);
        });

        it("exports CTA_DURATION as a number", () => {
          expect(typeof CTA_DURATION).toBe("number");
          expect(CTA_DURATION).toBe(500);
        });
      });

      describe("ONB-03: New Phase 2 constants", () => {
        it.todo("exports BISMILLAH_DISPLAY_DURATION = 2500");
        it.todo("exports STILLNESS_BEAT_DURATION = 1200");
        it.todo("exports LETTER_REVEAL_HAPTIC_DELAY derived from SPLASH_STAGGER_DURATION + STILLNESS_BEAT_DURATION");
      });
    });
    ```

    The real tests for existing constants serve as a regression guard -- if Plan 01 or Plan 02 accidentally removes or changes a stagger constant, this test catches it immediately (addresses ONB-03 stagger preservation).
  </action>
  <verify>
    <automated>cd "C:/Users/Nasif/Desktop/Iqra AI/tila-mobile" && npm test && npx vitest run src/__tests__/warm-glow.test.ts src/__tests__/onboarding-animations.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Both test files exist
    - onboarding-animations.test.ts has real assertions for existing STAGGER/SPLASH constants
    - warm-glow.test.ts includes backward-compatibility stub (Pitfall 5 guard)
    - Test stubs cover all 6 requirement IDs: ONB-01, ONB-02, ONB-03, ONB-04, MIND-01, MIND-02
    - npm test passes
  </acceptance_criteria>
  <done>Both test files exist. onboarding-animations.test.ts has real assertions for existing stagger constants (regression guard for ONB-03) plus todo stubs for new Phase 2 constants. warm-glow.test.ts has todo stubs for ONB-04 including backward-compat guard. npm test passes.</done>
</task>

</tasks>

<verification>
- `npm test` passes with no failures (todo tests are counted but not failures)
- All 4 test files exist in src/__tests__/
- onboarding-animations.test.ts has real assertions for existing STAGGER/SPLASH constants
- Test stubs cover all 6 requirement IDs: ONB-01, ONB-02, ONB-03, ONB-04, MIND-01, MIND-02
</verification>

<success_criteria>
- Four test stub files created matching RESEARCH.md Validation Architecture
- Existing animation constants have regression tests (not just stubs)
- npm test passes cleanly
- Implementation plans (02-01, 02-02, 02-03) can reference these test files in their verify steps
</success_criteria>

<output>
After completion, create `.planning/phases/02-onboarding-wow-factor/02-00-SUMMARY.md`
</output>
