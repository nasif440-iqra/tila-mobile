---
phase: 02-onboarding-wow-factor
plan: 02
type: execute
wave: 2
depends_on: ["02-00", "02-01"]
files_modified:
  - src/components/onboarding/steps/BismillahMoment.tsx
  - src/components/onboarding/steps/Welcome.tsx
  - src/components/onboarding/steps/Tilawat.tsx
  - src/components/onboarding/steps/Hadith.tsx
  - src/components/onboarding/OnboardingFlow.tsx
autonomous: true
requirements: [ONB-01, ONB-03, ONB-04, MIND-01, MIND-02]
must_haves:
  truths:
    - "Onboarding flow has exactly 9 steps in order: Welcome, Tilawat, Hadith, StartingPoint, Bismillah, LetterReveal, LetterAudio, LetterQuiz, Finish"
    - "Bismillah step auto-advances after 2.5 seconds with no skip button"
    - "Bismillah displays Arabic calligraphy with English translation and warm gold glow"
    - "Welcome screen uses BrandedLogo instead of inline LogoMark"
    - "Welcome, Tilawat, and Hadith screens use animated WarmGlow variant"
    - "FloatingLettersLayer is visible on steps 0-3 (extended from 0-2)"
    - "Step transitions use staggered content entrances from shared presets"
    - "Tilawat CTA says 'Begin Reading', Hadith CTA says 'Continue Journey'"
  artifacts:
    - path: "src/components/onboarding/steps/BismillahMoment.tsx"
      provides: "Sacred breathing pause step with Bismillah calligraphy"
      exports: ["BismillahMoment"]
    - path: "src/components/onboarding/OnboardingFlow.tsx"
      provides: "9-step orchestrator with named step constants"
      exports: ["OnboardingFlow"]
    - path: "src/components/onboarding/steps/Welcome.tsx"
      provides: "Elevated welcome with BrandedLogo and animated WarmGlow"
      exports: ["Welcome"]
  key_links:
    - from: "src/components/onboarding/OnboardingFlow.tsx"
      to: "src/components/onboarding/steps/BismillahMoment.tsx"
      via: "import and render at step 4"
      pattern: "step === STEP.BISMILLAH.*BismillahMoment"
    - from: "src/components/onboarding/steps/Welcome.tsx"
      to: "src/components/onboarding/BrandedLogo.tsx"
      via: "import BrandedLogo, replaces LogoMark"
      pattern: "import.*BrandedLogo"
    - from: "src/components/onboarding/steps/BismillahMoment.tsx"
      to: "src/components/onboarding/animations.ts"
      via: "BISMILLAH_DISPLAY_DURATION constant"
      pattern: "BISMILLAH_DISPLAY_DURATION"
---

<objective>
Create the BismillahMoment step, rewrite OnboardingFlow for 9 steps with named constants, and elevate Welcome/Tilawat/Hadith screens with enhanced visuals.

Purpose: This is the core orchestration change — inserting the sacred Bismillah breathing moment into the flow and upgrading the first four screens that set the spiritual and branded tone. The OnboardingFlow rewrite with named step constants prevents the off-by-one pitfall documented in RESEARCH.md.
Output: New BismillahMoment component, rewritten OnboardingFlow with STEP constants, elevated Welcome/Tilawat/Hadith screens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-onboarding-wow-factor/02-CONTEXT.md
@.planning/phases/02-onboarding-wow-factor/02-UI-SPEC.md
@.planning/phases/02-onboarding-wow-factor/02-RESEARCH.md
@.planning/phases/02-onboarding-wow-factor/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (must exist before this plan runs) -->

From src/components/onboarding/WarmGlow.tsx (enhanced):
```typescript
export function WarmGlow({
  size?: number,       // default 340
  opacity?: number,    // default 0.12 (static mode only)
  animated?: boolean,  // default false
  color?: string,      // optional override color
  pulseMin?: number,   // default 0.08
  pulseMax?: number,   // default 0.25
}): JSX.Element;
```

From src/components/onboarding/BrandedLogo.tsx (new):
```typescript
export function BrandedLogo({ width?: number, height?: number }): JSX.Element;
```

From src/components/onboarding/FloatingLettersLayer.tsx (enhanced):
```typescript
export function FloatingLettersLayer({ color, tint }: { color: string; tint?: 'primary' | 'accent' }): JSX.Element;
```

From src/components/onboarding/animations.ts (enhanced):
```typescript
export { durations, staggers } from "../../design/animations";
export const STAGGER_BASE = 150;
export const STAGGER_DURATION = 500;
export const SPLASH_STAGGER_BASE = 250;
export const SPLASH_STAGGER_DURATION = 700;
export const CTA_DELAY_OFFSET = 200;
export const CTA_DURATION = 500;
export const BISMILLAH_DISPLAY_DURATION = 2500;
export const STILLNESS_BEAT_DURATION = 1200;
export const LETTER_REVEAL_HAPTIC_DELAY = 1900; // SPLASH_STAGGER_DURATION + STILLNESS_BEAT_DURATION
```

From src/design/haptics.ts:
```typescript
export function hapticSelection(): void;
export function hapticMilestone(): void;
export function hapticSuccess(): void;
```

From src/components/onboarding/OnboardingStepLayout.tsx:
```typescript
export function OnboardingStepLayout({ variant, fadeInDuration, footer, children }): JSX.Element;
// variant: 'splash' | 'card'
```

From src/design/tokens.ts:
```typescript
export const spacing = { xs: 4, sm: 8, md: 12, lg: 16, xl: 24, xxl: 32, xxxl: 48 };
export const fontFamilies = { arabicRegular, bodyRegular, bodySemiBold, bodyMedium, headingRegular, headingSemiBold, headingBold, headingItalic };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BismillahMoment step + rewrite OnboardingFlow to 9 steps</name>
  <files>src/components/onboarding/steps/BismillahMoment.tsx, src/components/onboarding/OnboardingFlow.tsx</files>
  <read_first>
    - src/components/onboarding/OnboardingFlow.tsx
    - src/components/onboarding/animations.ts
    - src/components/onboarding/OnboardingStepLayout.tsx
    - src/design/animations.ts
    - src/design/haptics.ts
    - src/design/tokens.ts
    - src/design/theme.ts
    - src/components/onboarding/steps/Welcome.tsx (to see existing step pattern)
  </read_first>
  <action>
    **Create BismillahMoment.tsx (per D-07, D-09, D-10):**

    Create `src/components/onboarding/steps/BismillahMoment.tsx`. This is the sacred breathing pause step.

    Props: `{ onNext: () => void }`

    Component body:
    1. Call `useColors()` for color tokens.
    2. In `useEffect` on mount: call `hapticSelection()`, set `setTimeout(onNext, BISMILLAH_DISPLAY_DURATION)` (2500ms), return cleanup clearing the timeout.
    3. Render `OnboardingStepLayout` with `variant="splash"` and `fadeInDuration={durations.dramatic}` (600ms).
    4. Inside layout:
       - `<WarmGlow size={280} opacity={0.18} animated color={colors.accent} pulseMin={0.12} pulseMax={0.25} />`
       - Bismillah Arabic text wrapped in `Animated.View` with `entering={FadeIn.delay(200).duration(800)}`:
         - Use `ArabicText` component (from `src/design/components`) with `size="display"` and `color={colors.primaryDark}`.
         - Override style: `{ fontSize: 40, lineHeight: 60, textAlign: 'center', zIndex: 1 }`.
         - Text content: `بِسْمِ اللّٰهِ الرَّحْمٰنِ الرَّحِيمِ`
         - Add `accessibilityLabel="Bismillah ir-Rahman ir-Raheem. In the name of God, the Most Gracious, the Most Merciful."`
       - English translation in `Animated.Text` with `entering={FadeIn.delay(600).duration(600)}`:
         - Style: `fontFamily: fontFamilies.bodyRegular, fontSize: 13, lineHeight: 18, color: colors.textMuted, textAlign: 'center', fontStyle: 'italic', marginTop: spacing.lg, zIndex: 1`
         - Text: "In the name of God, the Most Gracious, the Most Merciful"

    No footer, no button, no skip (per D-10).

    **Rewrite OnboardingFlow.tsx for 9 steps (per D-07, D-16, RESEARCH Pitfall 1):**

    Read current OnboardingFlow.tsx carefully. Make these changes:

    1. Add import: `import { BismillahMoment } from "./steps/BismillahMoment";`

    2. Replace `TOTAL_STEPS = 8` with `TOTAL_STEPS = 9`.

    3. Replace numeric step names with named constants (per RESEARCH Pitfall 1):
       ```typescript
       const STEP = {
         WELCOME: 0, TILAWAT: 1, HADITH: 2, STARTING_POINT: 3,
         BISMILLAH: 4, LETTER_REVEAL: 5, LETTER_AUDIO: 6,
         LETTER_QUIZ: 7, FINISH: 8,
       } as const;
       ```

    4. Update `STEP_NAMES` array to include `'bismillah'` at index 4:
       ```typescript
       const STEP_NAMES = [
         'welcome', 'tilawat', 'hadith', 'starting_point', 'bismillah',
         'letter_reveal', 'letter_audio', 'letter_quiz', 'finish',
       ] as const;
       ```

    5. Update LetterReveal auto-advance: change `step === 4` to `step === STEP.LETTER_REVEAL`. Change `setStep(5)` to `setStep(STEP.LETTER_AUDIO)`. Change timeout from 3500 to 4500 (accounts for stillness beat per UI-SPEC).

    6. Add Bismillah auto-advance timer:
       ```typescript
       useEffect(() => {
         if (step === STEP.BISMILLAH) {
           // Handled by BismillahMoment component's internal timer
           // No additional timer needed here — goNext is passed as onNext prop
         }
       }, [step]);
       ```
       Actually, the BismillahMoment component handles its own auto-advance via setTimeout(onNext) internally. So NO additional timer in OnboardingFlow for Bismillah. The component calls `goNext` when its timer fires.

    7. Update progress bar visibility: `const showProgressBar = step > STEP.WELCOME && step !== STEP.LETTER_REVEAL && step !== STEP.LETTER_QUIZ && step < STEP.FINISH;`
       Also hide on Bismillah step (it's a full sacred moment): add `&& step !== STEP.BISMILLAH`.

    8. Update FloatingLettersLayer visibility: change `step <= 2` to `step <= STEP.STARTING_POINT` (visible on steps 0-3, per UI-SPEC).

    9. Update render switch to use STEP constants and add Bismillah:
       ```
       {step === STEP.WELCOME && <Welcome onNext={goNext} />}
       {step === STEP.TILAWAT && <Tilawat onNext={goNext} />}
       {step === STEP.HADITH && <Hadith onNext={goNext} />}
       {step === STEP.STARTING_POINT && <StartingPoint ... />}
       {step === STEP.BISMILLAH && <BismillahMoment onNext={goNext} />}
       {step === STEP.LETTER_REVEAL && <LetterReveal />}
       {step === STEP.LETTER_AUDIO && <LetterAudio ... />}
       {step === STEP.LETTER_QUIZ && <LetterQuiz onNext={goNext} />}
       {step === STEP.FINISH && <Finish ... />}
       ```

    10. Export the STEP constant for potential use by tests:
        ```typescript
        export { STEP };
        ```
  </action>
  <verify>
    <automated>cd "C:/Users/Nasif/Desktop/Iqra AI/tila-mobile" && npm run validate</automated>
  </verify>
  <acceptance_criteria>
    - BismillahMoment.tsx exists and exports BismillahMoment component
    - BismillahMoment uses BISMILLAH_DISPLAY_DURATION (not hardcoded 2500)
    - BismillahMoment calls hapticSelection() on mount
    - BismillahMoment has no button or skip mechanism
    - BismillahMoment contains Arabic text بِسْمِ اللّٰهِ الرَّحْمٰنِ الرَّحِيمِ
    - BismillahMoment has accessibilityLabel
    - OnboardingFlow exports STEP constant object with 9 named entries
    - OnboardingFlow TOTAL_STEPS equals 9
    - OnboardingFlow STEP_NAMES has 'bismillah' at index 4
    - OnboardingFlow renders BismillahMoment at step === STEP.BISMILLAH
    - OnboardingFlow LetterReveal auto-advance uses STEP.LETTER_REVEAL (not number 4)
    - OnboardingFlow LetterReveal auto-advance timeout is 4500
    - FloatingLettersLayer visible condition uses step <= STEP.STARTING_POINT
    - Progress bar hidden on STEP.BISMILLAH
    - No raw numeric step comparisons remain (all use STEP.*)
  </acceptance_criteria>
  <done>BismillahMoment renders the sacred breathing pause with Arabic calligraphy, gold glow, and 2.5s auto-advance. OnboardingFlow orchestrates 9 steps using named STEP constants with correct visibility logic and timer adjustments.</done>
</task>

<task type="auto">
  <name>Task 2: Elevate Welcome, Tilawat, and Hadith screens</name>
  <files>src/components/onboarding/steps/Welcome.tsx, src/components/onboarding/steps/Tilawat.tsx, src/components/onboarding/steps/Hadith.tsx</files>
  <read_first>
    - src/components/onboarding/steps/Welcome.tsx
    - src/components/onboarding/steps/Tilawat.tsx
    - src/components/onboarding/steps/Hadith.tsx
    - src/components/onboarding/BrandedLogo.tsx
    - src/components/onboarding/WarmGlow.tsx
  </read_first>
  <action>
    **Welcome.tsx (per D-01, D-02, D-12):**

    1. Remove the entire `LogoMark` function component (lines 16-47 of current file).
    2. Add import: `import { BrandedLogo } from "../BrandedLogo";`
    3. Replace `<LogoMark colors={colors} />` with `<BrandedLogo width={120} height={160} />`.
    4. Change `<WarmGlow size={360} opacity={0.12} />` to `<WarmGlow size={360} animated pulseMin={0.08} pulseMax={0.18} />` — adds ambient pulsing (per UI-SPEC Step 0: Welcome, WarmGlow pulsing between 0.08 and 0.18 opacity).
    5. Remove the import of `Svg, { Path, Circle } from "react-native-svg"` — no longer needed since LogoMark is gone.
    6. All other elements (app name "tila", "READ BEAUTIFULLY", tagline, "Get Started" button) remain exactly as they are.

    **Tilawat.tsx (per D-13):**

    1. Change `<WarmGlow size={300} opacity={0.15} />` to `<WarmGlow size={300} animated pulseMin={0.10} pulseMax={0.20} />`.
    2. Change the Button title from `"Begin"` to `"Begin Reading"` (per UI-SPEC Copywriting Contract).
    3. All other elements remain the same — the Arabic calligraphy, headline with italic "Tilawat", motto are already well-designed.

    **Hadith.tsx (per D-13):**

    1. Change `<WarmGlow size={340} opacity={0.12} />` to `<WarmGlow size={340} animated pulseMin={0.08} pulseMax={0.18} />`.
    2. Change the Button title from `"Continue"` to `"Continue Journey"` (per UI-SPEC Copywriting Contract).
    3. All other elements remain the same — the arch outline, gold diamond, hadith quote, source citation are already well-designed.

    **Critical for ONB-03:** All three screens MUST continue using SPLASH_STAGGER_BASE and SPLASH_STAGGER_DURATION constants from `../animations` for their content entrance stagger timing. Do NOT replace these with different values or hardcoded numbers. The stagger constants must be preserved exactly as they are to maintain smooth step transitions.
  </action>
  <verify>
    <automated>cd "C:/Users/Nasif/Desktop/Iqra AI/tila-mobile" && npm run validate</automated>
  </verify>
  <acceptance_criteria>
    - Welcome.tsx imports BrandedLogo (not Svg/Path/Circle from react-native-svg)
    - Welcome.tsx renders `<BrandedLogo width={120} height={160} />`
    - Welcome.tsx has no LogoMark function
    - Welcome.tsx WarmGlow has animated prop (no opacity prop, uses pulseMin/pulseMax)
    - Tilawat.tsx WarmGlow has animated prop
    - Tilawat.tsx Button title is "Begin Reading"
    - Hadith.tsx WarmGlow has animated prop
    - Hadith.tsx Button title is "Continue Journey"
    - No import of Svg/Path/Circle in Welcome.tsx
    - ONB-03 preserved: All three screens still reference SPLASH_STAGGER_BASE and SPLASH_STAGGER_DURATION from animations.ts for stagger timing (grep confirms no hardcoded stagger values replaced these constants)
  </acceptance_criteria>
  <done>Welcome screen uses real BrandedLogo with animated glow, Tilawat and Hadith screens have animated WarmGlow and updated CTA copy. All three screens feel visually warmer and more intentional. Stagger constants from animations.ts are preserved for ONB-03 compliance.</done>
</task>

</tasks>

<verification>
- `npm run validate` passes (lint + typecheck)
- OnboardingFlow renders exactly 9 steps (count step === STEP.* comparisons)
- No raw numeric step indices remain in OnboardingFlow
- BismillahMoment has no button/touchable elements
- Welcome.tsx no longer contains any SVG path data (LogoMark removed)
- All CTA copy matches UI-SPEC Copywriting Contract
- Stagger constants (SPLASH_STAGGER_BASE, SPLASH_STAGGER_DURATION) still used in Welcome, Tilawat, Hadith screens
</verification>

<success_criteria>
- Complete 9-step onboarding flow with BismillahMoment at step 4
- Bismillah auto-advances in 2.5s with haptic and gold glow
- Welcome screen shows real branded logo with animated ambient glow
- Tilawat says "Begin Reading", Hadith says "Continue Journey"
- All three elevated screens use animated WarmGlow pulsing
- Named STEP constants prevent off-by-one bugs
- Stagger constants preserved after rewrite (ONB-03)
</success_criteria>

<output>
After completion, create `.planning/phases/02-onboarding-wow-factor/02-02-SUMMARY.md`
</output>
