---
phase: 05-celebrations-feedback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/celebration-tiers.test.ts
  - src/__tests__/letter-mastery-celebration.test.ts
  - src/__tests__/phase-complete-celebration.test.ts
  - src/__tests__/islamic-copy.test.ts
  - src/__tests__/mastery-pipeline.test.ts
  - src/engine/engagement.js
  - src/hooks/useProgress.ts
  - src/hooks/useMastery.ts
autonomous: true
requirements:
  - CEL-05
must_haves:
  truths:
    - "Islamic phrases appear in CORRECT_COPY, WRONG_ENCOURAGEMENT, STREAK_COPY, MID_CELEBRATE_COPY, COMPLETION_HEADLINES, COMPLETION_SUBLINES, CONTINUATION_COPY, and UNLOCK_COPY pools"
    - "A LETTER_MASTERY_COPY array exists with {letter} placeholder in engagement.js"
    - "Quiz results update mastery_entities via mergeQuizResultsIntoMastery after lesson completion"
    - "Test stubs exist for all 5 phase requirements"
  artifacts:
    - path: "src/engine/engagement.js"
      provides: "Islamic copy pools + LETTER_MASTERY_COPY"
      contains: "LETTER_MASTERY_COPY"
    - path: "src/hooks/useProgress.ts"
      provides: "Mastery pipeline integration in completeLesson"
      contains: "mergeQuizResultsIntoMastery"
    - path: "src/__tests__/islamic-copy.test.ts"
      provides: "Islamic copy validation tests"
    - path: "src/__tests__/mastery-pipeline.test.ts"
      provides: "Mastery pipeline integration tests"
  key_links:
    - from: "src/hooks/useProgress.ts"
      to: "src/engine/mastery.js"
      via: "mergeQuizResultsIntoMastery import and call"
      pattern: "mergeQuizResultsIntoMastery"
    - from: "src/hooks/useProgress.ts"
      to: "src/hooks/useMastery.ts"
      via: "saveMasteryEntity/Skill/Confusion calls"
      pattern: "saveMastery"
---

<objective>
Wire the mastery tracking pipeline, expand Islamic copy pools, and create Wave 0 test stubs for all Phase 5 requirements.

Purpose: The mastery engine (mergeQuizResultsIntoMastery) exists but is never called from the lesson flow. Without wiring this, letter mastery celebrations (CEL-03) can never trigger. Islamic copy expansion (CEL-05) is independent and can be done in parallel. Test stubs provide verification infrastructure for all subsequent plans.

Output: Working mastery pipeline, Islamic-enriched copy pools, LETTER_MASTERY_COPY array, test stubs for CEL-01 through CEL-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-celebrations-feedback/05-CONTEXT.md
@.planning/phases/05-celebrations-feedback/05-RESEARCH.md

@src/engine/engagement.js
@src/engine/mastery.js
@src/hooks/useProgress.ts
@src/hooks/useMastery.ts
@src/engine/progress.ts
@src/types/quiz.ts
@src/db/provider.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/engine/mastery.js:
```javascript
export function mergeQuizResultsIntoMastery(mastery, quizResults, today)
// Returns { entities, skills, confusions }

export function deriveMasteryState(entry, today)
// Returns "introduced"|"unstable"|"accurate"|"retained"

export function normalizeEntityKey(targetId, question)
// Returns "letter:N" or "combo:X"

export function emptyMastery()
// Returns { entities: {}, skills: {}, confusions: {} }
```

From src/hooks/useMastery.ts:
```typescript
export function useMastery() {
  // Returns { updateEntity, updateSkill, updateConfusion }
  // updateEntity(entityKey: string, state: EntityState): Promise<void>
  // updateSkill(skillKey: string, state: SkillState): Promise<void>
  // updateConfusion(confusionKey: string, state: ConfusionState): Promise<void>
}
```

From src/engine/progress.ts:
```typescript
export function saveMasteryEntity(db, entityKey, state): Promise<void>
export function saveMasterySkill(db, skillKey, state): Promise<void>
export function saveMasteryConfusion(db, confusionKey, state): Promise<void>
export function loadProgress(db): Promise<ProgressState>
// ProgressState includes mastery: { entities, skills, confusions }
```

From src/types/quiz.ts:
```typescript
export interface QuizResultItem {
  targetId: string | number;
  correct: boolean;
  selectedId: string;
  questionType: string | null;
  correctId: string;
  isHarakat: boolean;
  hasAudio: boolean;
  responseTimeMs: number;
}
```

From src/engine/engagement.js:
```javascript
export function pickCopy(arr) // Returns random element
export function getCorrectPool(isHarakat, isSound) // Returns correct copy array
export function getCompletionTier(accuracy, isFirst, isHarakat) // Returns tier string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 test stubs + Islamic copy expansion + LETTER_MASTERY_COPY</name>
  <files>
    src/__tests__/celebration-tiers.test.ts
    src/__tests__/letter-mastery-celebration.test.ts
    src/__tests__/phase-complete-celebration.test.ts
    src/__tests__/islamic-copy.test.ts
    src/__tests__/mastery-pipeline.test.ts
    src/engine/engagement.js
  </files>
  <read_first>
    src/engine/engagement.js
    src/engine/mastery.js
    src/__tests__/mastery.test.js
    src/components/quiz/QuizCelebration.tsx
    src/components/LessonSummary.tsx
    app/phase-complete.tsx
  </read_first>
  <action>
**A) Create 5 test stub files** with it.todo() for forward-looking tests and real assertions for existing exports (per Phase 02 decision on Wave 0 stubs):

1. `src/__tests__/celebration-tiers.test.ts` (CEL-01):
   - it.todo("LetterMasteryCelebration uses hapticMilestone")
   - it.todo("phase-complete uses hapticMilestone")
   - it.todo("QuizCelebration uses hapticMilestone (small tier)")
   - it.todo("LessonSummary uses tiered haptics (existing)")

2. `src/__tests__/letter-mastery-celebration.test.ts` (CEL-03):
   - it.todo("LetterMasteryCelebration renders with WarmGlow")
   - it.todo("LetterMasteryCelebration shows letter name and Arabic character")
   - it.todo("LetterMasteryCelebration displays Islamic mastery message")
   - it.todo("LetterMasteryCelebration handles multiple mastered letters")

3. `src/__tests__/phase-complete-celebration.test.ts` (CEL-04):
   - it.todo("phase-complete.tsx imports WarmGlow")
   - it.todo("phase-complete.tsx calls hapticMilestone")
   - it.todo("phase-complete.tsx has scale animation on Arabic text")

4. `src/__tests__/islamic-copy.test.ts` (CEL-05):
   - Real assertions: verify CORRECT_COPY.recognition contains at least 2 entries matching /MashaAllah|Alhamdulillah|SubhanAllah|Bismillah/i
   - Real assertions: verify CORRECT_COPY.sound contains at least 1 Islamic phrase
   - Real assertions: verify CORRECT_COPY.harakat contains at least 1 Islamic phrase
   - Real assertions: verify WRONG_ENCOURAGEMENT contains at least 1 Islamic phrase
   - Real assertions: verify STREAK_COPY.default contains at least 1 Islamic phrase
   - Real assertions: verify MID_CELEBRATE_COPY.default contains at least 1 Islamic phrase
   - Real assertions: verify COMPLETION_HEADLINES has at least 1 Islamic phrase
   - Real assertions: verify COMPLETION_SUBLINES has at least 1 Islamic phrase
   - Real assertions: verify LETTER_MASTERY_COPY exists with at least 5 entries, all containing {letter} placeholder
   - Real assertions: verify LETTER_MASTERY_COPY entries match /MashaAllah|Alhamdulillah|SubhanAllah/i

5. `src/__tests__/mastery-pipeline.test.ts` (mastery wiring prerequisite):
   - Real test: mergeQuizResultsIntoMastery correctly merges a simple quiz result
   - Real test: deriveMasteryState returns "introduced" for new entity
   - Real test: normalizeEntityKey returns "letter:N" for numeric targetId
   - it.todo("useProgress.completeLesson calls mergeQuizResultsIntoMastery")
   - it.todo("mastery entities are persisted to DB after quiz completion")

**B) Expand Islamic copy in engagement.js** (per D-08, D-09). Mix Islamic phrases into existing pools at ~30-40% ratio (per Research pitfall 4 — avoid overuse):

Add to CORRECT_COPY.recognition (add 3 Islamic variants among existing 9):
- "MashaAllah, that's right."
- "MashaAllah -- you see it clearly."
- "Alhamdulillah, correct."

Add to CORRECT_COPY.sound (add 2 among existing 8):
- "MashaAllah, you hear it."
- "Your ear is blessed -- that's right."

Add to CORRECT_COPY.harakat (add 2 among existing 8):
- "MashaAllah, you read that well."
- "Alhamdulillah -- the marks are clear to you."

Add to WRONG_ENCOURAGEMENT (add 2 among existing 6):
- "In shaa Allah, it will come -- keep trying."
- "Every effort is rewarded -- try again."

Add to STREAK_COPY.default (add 1 among existing 5):
- "Alhamdulillah, flowing beautifully"

Add to MID_CELEBRATE_COPY.default (add 2 among existing 5):
- "MashaAllah, your dedication shows"
- "Alhamdulillah -- real progress"

Add to COMPLETION_HEADLINES (add Islamic variants for perfect and great tiers):
- Change "perfect" from "Flawless." to "MashaAllah!"
- Change "great" from "Well done." to "Alhamdulillah."

Add to COMPLETION_SUBLINES (add Islamic framing):
- Change "perfect" to "MashaAllah -- every answer was precise. That's real learning."
- Change "great" to "Alhamdulillah -- strong understanding. The letters are becoming clear."

Add to CONTINUATION_COPY (add 1 among existing 5):
- "Bismillah -- your next step awaits."

Add to UNLOCK_COPY (add 1 among existing 3):
- "MashaAllah -- a new lesson is open to you."

**C) Add new LETTER_MASTERY_COPY export** to engagement.js:
```javascript
export const LETTER_MASTERY_COPY = [
  "MashaAllah! You've truly learned {letter}.",
  "Alhamdulillah -- {letter} is now part of your reading.",
  "MashaAllah -- you've mastered {letter}.",
  "SubhanAllah, look how far you've come with {letter}.",
  "{letter} is yours now. MashaAllah.",
];
```
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/islamic-copy.test.ts src/__tests__/mastery-pipeline.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "MashaAllah\|Alhamdulillah\|SubhanAllah\|Bismillah" src/engine/engagement.js returns >= 15
    - grep "LETTER_MASTERY_COPY" src/engine/engagement.js returns a match
    - All 5 test stub files exist in src/__tests__/
    - islamic-copy.test.ts and mastery-pipeline.test.ts pass
  </acceptance_criteria>
  <done>Islamic phrases mixed into all copy pools at ~30-40% ratio. LETTER_MASTERY_COPY exported with 5 entries containing {letter} placeholder. All 5 test stub files created. Real assertion tests pass for islamic-copy and mastery-pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Wire mastery pipeline into useProgress.completeLesson</name>
  <files>
    src/hooks/useProgress.ts
  </files>
  <read_first>
    src/hooks/useProgress.ts
    src/hooks/useMastery.ts
    src/engine/mastery.js
    src/engine/progress.ts
    src/types/quiz.ts
    app/lesson/[id].tsx
  </read_first>
  <action>
Wire mergeQuizResultsIntoMastery into the lesson completion flow per Research recommendation: extend useProgress.completeLesson to also call mastery merge + save. This centralizes the pipeline.

**Modify `src/hooks/useProgress.ts`:**

1. Add imports at top:
```typescript
import {
  mergeQuizResultsIntoMastery,
  normalizeEntityKey,
  deriveMasteryState,
} from "../engine/mastery";
import {
  saveMasteryEntity,
  saveMasterySkill,
  saveMasteryConfusion,
} from "../engine/progress";
```

2. Add a new import for QuizResultItem type:
```typescript
import type { QuizResultItem } from "../types/quiz";
```

3. Extend the completeLesson callback signature to accept an optional `quizResultItems` parameter (the raw QuizResultItem[] from the quiz UI, needed for mastery merge). Keep backward compat by making it optional:
```typescript
async (
  lessonId: number,
  accuracy: number,
  passed: boolean,
  questions: QuestionAttempt[],
  quizResultItems?: QuizResultItem[]
) => {
```

4. After `await refresh()` and before `return attemptId`, add mastery merge if quizResultItems provided:
```typescript
// Wire mastery pipeline (Phase 5: CEL-03 prerequisite)
if (quizResultItems && quizResultItems.length > 0) {
  const today = new Date().toISOString().slice(0, 10);
  const currentMastery = state?.mastery ?? { entities: {}, skills: {}, confusions: {} };

  // Enrich quiz results with keys needed by mastery engine
  const enrichedResults = quizResultItems.map(r => ({
    ...r,
    targetKey: normalizeEntityKey(r.targetId, r),
    skillKeys: [], // Skills derived inside mergeQuizResultsIntoMastery via deriveSkillKeysFromQuestion
  }));

  const updatedMastery = mergeQuizResultsIntoMastery(currentMastery, enrichedResults, today);

  // Persist updated entities
  for (const [key, entityState] of Object.entries(updatedMastery.entities)) {
    await saveMasteryEntity(db, key, entityState as any);
  }
  // Persist updated skills
  for (const [key, skillState] of Object.entries(updatedMastery.skills)) {
    await saveMasterySkill(db, key, skillState as any);
  }
  // Persist updated confusions
  for (const [key, confusionState] of Object.entries(updatedMastery.confusions)) {
    await saveMasteryConfusion(db, key, confusionState as any);
  }

  // Refresh again to pick up mastery changes
  await refresh();
}
```

5. Update the dependency array of the useCallback to include `state`:
```typescript
[db, refresh, state]
```

**Important:** Do NOT modify app/lesson/[id].tsx in this task. The lesson screen already passes `results.questions` (QuizResultItem[]) to handleQuizComplete. Plan 02 will wire the quizResultItems parameter through the call in lesson/[id].tsx.

Note: The mastery engine's `mergeQuizResultsIntoMastery` expects results to have `targetKey` and `skillKeys` fields. The `normalizeEntityKey` call enriches each result with `targetKey`. The `skillKeys` are derived internally by the engine via `deriveSkillKeysFromQuestion` which reads from the question object fields (isHarakat, targetId, lessonMode, type, hasAudio, options). Since QuizResultItem has isHarakat, hasAudio, targetId, and questionType, the engine can derive skills from these.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - grep "mergeQuizResultsIntoMastery" src/hooks/useProgress.ts returns a match
    - grep "saveMasteryEntity" src/hooks/useProgress.ts returns a match
    - grep "quizResultItems" src/hooks/useProgress.ts returns a match
    - npm run typecheck passes
  </acceptance_criteria>
  <done>useProgress.completeLesson accepts optional quizResultItems parameter. When provided, it calls mergeQuizResultsIntoMastery, persists updated entities/skills/confusions to DB, and refreshes state. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `npm test -- --run` passes (all existing + new tests)
- `npm run typecheck` passes
- grep confirms Islamic phrases in engagement.js
- grep confirms mergeQuizResultsIntoMastery called from useProgress.ts
- LETTER_MASTERY_COPY exported from engagement.js
- All 5 test stub files exist
</verification>

<success_criteria>
1. Islamic phrases mixed into all engagement.js copy pools at appropriate ratios
2. LETTER_MASTERY_COPY array exported with {letter} placeholder entries
3. Mastery pipeline wired: completeLesson -> mergeQuizResultsIntoMastery -> save to DB
4. All 5 test stub files created with appropriate real assertions and it.todo() stubs
5. All tests pass, TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-celebrations-feedback/05-01-SUMMARY.md`
</output>
