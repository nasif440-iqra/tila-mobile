---
phase: 05-quiz-learning-engine-4-options-question-types-lesson-insight
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/insights.ts
  - src/components/insights/LessonInsights.tsx
  - src/__tests__/lesson-insights.test.ts
autonomous: true
requirements:
  - D-06
  - D-07
  - D-08
  - D-09

must_haves:
  truths:
    - "Post-lesson insights never show spaced-repetition scheduling language like 'Review Ba on Tuesday'"
    - "Insights lead with mastery celebration: 'You mastered Alif!', 'Ba is getting stronger', '3 letters now retained'"
    - "Confusion pairs are shown with encouraging, non-punitive tone: 'You sometimes confuse Ba and Ta -- keep practicing!'"
    - "When no confusion pairs and lesson went well, a simple encouraging message appears instead of empty/technical insights"
  artifacts:
    - path: "src/engine/insights.ts"
      provides: "Rewritten insight generation with mastery celebration + confusion awareness"
      contains: "mastered"
    - path: "src/__tests__/lesson-insights.test.ts"
      provides: "Tests for new insight types and no scheduling language"
      contains: "mastered"
  key_links:
    - from: "src/engine/insights.ts"
      to: "src/components/insights/LessonInsights.tsx"
      via: "LessonInsight type"
      pattern: "LessonInsight"
    - from: "src/components/LessonSummary.tsx"
      to: "src/components/insights/LessonInsights.tsx"
      via: "LessonInsights component import"
      pattern: "LessonInsights"
---

<objective>
Rewrite post-lesson insights to celebrate mastery progress and surface confusion pairs warmly, removing all spaced-repetition scheduling language.

Purpose: Users find scheduling language ("Review Ba on Tuesday") confusing and random. Insights should feel celebratory first, constructive second -- never clinical or robotic.
Output: Rewritten insights engine + updated UI indicator colors + comprehensive tests.
</objective>

<execution_context>
@C:\Users\Nasif\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Nasif\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/engine/insights.ts
@src/components/insights/LessonInsights.tsx
@src/__tests__/lesson-insights.test.ts

<interfaces>
From src/engine/insights.ts:
```typescript
export interface LessonInsight {
  type: 'confusion' | 'review' | 'trend';
  message: string;
}

export function generatePostLessonInsights(
  mastery: MasteryData,
  lessonLetterIds: number[],
  sessionResults: Map<number, { correct: number; total: number }>
): LessonInsight[]
```

From src/components/insights/LessonInsights.tsx:
```typescript
interface LessonInsightsProps {
  insights?: LessonInsight[];
}
export function LessonInsights({ insights }: LessonInsightsProps)
```

From src/components/LessonSummary.tsx (consumer):
```typescript
import type { LessonInsight } from "../engine/insights";
import { LessonInsights } from "./insights/LessonInsights";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite insights engine — mastery celebration + confusion awareness (per D-06, D-07, D-08, D-09)</name>
  <files>src/engine/insights.ts, src/__tests__/lesson-insights.test.ts</files>
  <read_first>src/engine/insights.ts, src/__tests__/lesson-insights.test.ts, src/engine/selectors.ts</read_first>
  <behavior>
    - Test: No insight has type "review" (D-06 — scheduling language removed)
    - Test: No insight message contains day names or "Review X on" pattern (D-06)
    - Test: When a letter reaches "retained" state, insight says "You mastered [name]!" (D-07)
    - Test: When a letter reaches "accurate" state, insight says "[name] is getting stronger" (D-07)
    - Test: When multiple letters are retained, insight says "N letters now retained" (D-07)
    - Test: When confusion pairs exist for lesson letters, insight says "You sometimes confuse [A] and [B] -- keep practicing!" (D-08)
    - Test: When no confusions and session accuracy >= 80%, show encouraging message like "Great focus today!" (D-09)
    - Test: Returns max 3 insights, at most 1 per type
  </behavior>
  <action>
1. In `src/engine/insights.ts`:

**Change LessonInsight type union:**
```typescript
export interface LessonInsight {
  type: 'mastery' | 'confusion' | 'encouragement';
  message: string;
}
```
Remove 'review' and 'trend' types. Add 'mastery' and 'encouragement'.

**Rewrite `generatePostLessonInsights`:**
- Remove call to `buildReviewInsight` entirely (D-06).
- Replace `buildTrendInsight` with `buildMasteryInsight` (D-07):
  - Check each lesson letter's entity state. If state === "retained", message: "You mastered [name]!"
  - If state === "accurate", message: "[name] is getting stronger"
  - If multiple letters are "retained", message: "[N] letters now retained"
  - Priority: retained count message > individual retained > individual accurate
- Keep `buildConfusionInsight` but update message copy (D-08):
  - Change from "Tila noticed you mixed up X and Y -- we'll practice these together" to "You sometimes confuse [A] and [B] -- keep practicing!"
- Add `buildEncouragementInsight` (D-09):
  - Only shows if no confusion insight and no mastery insight
  - Calculate overall session accuracy from sessionResults
  - If accuracy >= 80%: pick from ["Great focus today!", "Masha'Allah, well done!", "You're building something beautiful"]
  - If accuracy >= 50%: pick from ["Keep going -- every lesson makes you stronger", "Practice makes permanent"]
  - If accuracy < 50%: "Every attempt teaches you something. Keep going!"
- Assemble insights: mastery first, then confusion, then encouragement. Max 3.

**Keep all other exports** (`groupReviewsByDay`, `parseConfusionPairs`, `ReviewGroups`, `ConfusionPairDisplay`) unchanged — they serve the progress tab, not post-lesson insights.

2. In `src/__tests__/lesson-insights.test.ts`:
- Rewrite existing tests to match new types/messages.
- Remove "review timing" test.
- Add tests for mastery celebration, confusion awareness, encouragement fallback, and no-scheduling-language assertion.
  </action>
  <verify>
    <automated>cd "C:\Users\Nasif\Desktop\Iqra AI\tila-mobile" && npm test -- --run --reporter=verbose src/__tests__/lesson-insights.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "'review'" src/engine/insights.ts returns 0 (review type removed from insight generation)
    - grep "Review.*on" src/engine/insights.ts returns 0 matches (no scheduling language)
    - grep -c "mastered" src/engine/insights.ts returns >= 1
    - grep -c "encouragement" src/engine/insights.ts returns >= 1
    - All tests pass
  </acceptance_criteria>
  <done>Post-lesson insights celebrate mastery progress first, surface confusion pairs warmly, show encouragement when nothing else applies. No scheduling language anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Update LessonInsights component indicator colors for new insight types</name>
  <files>src/components/insights/LessonInsights.tsx</files>
  <read_first>src/components/insights/LessonInsights.tsx, src/engine/insights.ts</read_first>
  <action>
Update `getIndicatorColor` function to handle the new insight types from the rewritten engine:

```typescript
function getIndicatorColor(type: LessonInsight["type"], colors: any) {
  switch (type) {
    case "mastery":
      return colors.accent;       // gold — celebratory
    case "confusion":
      return colors.primaryLight; // soft green — constructive, not alarming
    case "encouragement":
      return colors.primary;      // deep green — grounding
    default:
      return colors.textMuted;
  }
}
```

Remove old cases for "review" and "trend". Update the section header from "Your Lesson Insights" to "How You Did" for a warmer, less clinical tone.

Run typecheck to ensure LessonInsight type changes propagate cleanly.
  </action>
  <verify>
    <automated>cd "C:\Users\Nasif\Desktop\Iqra AI\tila-mobile" && npm run typecheck 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "mastery" src/components/insights/LessonInsights.tsx returns >= 1
    - grep -c "review" src/components/insights/LessonInsights.tsx returns 0 (old type removed)
    - grep "How You Did" src/components/insights/LessonInsights.tsx returns 1 match
    - npm run typecheck passes with 0 errors
  </acceptance_criteria>
  <done>LessonInsights component handles mastery/confusion/encouragement types with appropriate indicator colors and warmer header copy. No type errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engine insights -> UI component | Insight type must match expected union |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | I (Information Disclosure) | Insight messages | accept | Messages only reference letter names already visible to user. No sensitive data. |
| T-05-04 | T (Tampering) | Mastery state reading | accept | Insights read mastery state; they don't write it. Read-only access, no risk. |
</threat_model>

<verification>
1. `npm test -- --run` — all tests pass
2. `npm run typecheck` — no type errors
3. Verify no insight message contains day names or "Review X on" pattern
4. Verify mastery celebration messages appear for retained/accurate states
5. Verify fallback encouragement appears when no mastery or confusion data
</verification>

<success_criteria>
- No scheduling language in any insight message
- Mastery celebration leads insights when letters reach retained/accurate states
- Confusion pairs shown with warm, actionable tone
- Default encouraging message when lesson goes well and no other insights apply
- Component renders new types with correct indicator colors
- All tests pass, no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-quiz-learning-engine-4-options-question-types-lesson-insight/05-02-SUMMARY.md`
</output>
