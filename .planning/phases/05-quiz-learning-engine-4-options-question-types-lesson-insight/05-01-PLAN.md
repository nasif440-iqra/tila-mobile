---
phase: 05-quiz-learning-engine-4-options-question-types-lesson-insight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/questions/shared.ts
  - src/engine/questions/recognition.ts
  - src/engine/questions/sound.ts
  - src/engine/questions/review.ts
  - src/__tests__/questions.test.js
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05

must_haves:
  truths:
    - "Every question in recognition, sound, and review modes has exactly 4 options"
    - "When fewer than 3 valid distractors exist in the pool, padding pulls from the full alphabet to reach 4"
    - "Recognition mode distributes questions roughly equally across rule, name_to_letter, and letter_to_name types"
    - "letter_to_name questions show Arabic letter as prompt and 4 English name options"
  artifacts:
    - path: "src/engine/questions/shared.ts"
      provides: "4-option padding in makeOpts, makeNameOpts, makeSoundOpts, buildFallbackQuestion"
      contains: "slice(0, 3)"
    - path: "src/engine/questions/recognition.ts"
      provides: "33/33/33 split of rule, name_to_letter, letter_to_name"
      contains: "letter_to_name"
    - path: "src/engine/questions/sound.ts"
      provides: "4-option sound questions"
      contains: "slice(0, 3)"
    - path: "src/engine/questions/review.ts"
      provides: "4-option review questions"
      contains: "getDistractors(t.id, allPool, 3)"
  key_links:
    - from: "src/engine/questions/recognition.ts"
      to: "src/engine/questions/shared.ts"
      via: "makeOpts, makeNameOpts, getDistractors, getRuleDistractors"
      pattern: "makeOpts|makeNameOpts|getDistractors|getRuleDistractors"
    - from: "src/engine/questions/sound.ts"
      to: "src/engine/questions/shared.ts"
      via: "makeOpts, makeSoundOpts, getConfusionDistractors"
      pattern: "makeOpts|makeSoundOpts|getConfusionDistractors"
---

<objective>
Update all question generators to produce exactly 4 options per question and balance recognition question types equally across rule, name_to_letter, and letter_to_name.

Purpose: 4 options increases quiz difficulty appropriately as learners progress, and equal type distribution ensures learners practice visual identification, name recall, and reverse identification equally.
Output: Updated question generators + tests proving 4-option output and type balance.
</objective>

<execution_context>
@C:\Users\Nasif\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Nasif\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/engine/questions/shared.ts
@src/engine/questions/recognition.ts
@src/engine/questions/sound.ts
@src/engine/questions/review.ts
@src/types/question.ts
@src/__tests__/questions.test.js

<interfaces>
From src/engine/questions/shared.ts:
```typescript
export function getDistractors(tid: number, pool: number[], cnt: number): ArabicLetter[]
export function getRuleDistractors(target: ArabicLetter, pool: number[], cnt: number): (ArabicLetter | undefined)[]
export function getConfusionDistractors(tid: number, pool: number[], cnt: number): ArabicLetter[]
export function makeOpts(letters: (ArabicLetter | undefined)[], cid: number): QuestionOption[]
export function makeNameOpts(letters: (ArabicLetter | undefined)[], cid: number): QuestionOption[]
export function makeSoundOpts(letters: (ArabicLetter | undefined)[], cid: number): QuestionOption[]
export function buildFallbackQuestion(targetId: number, pool: number[]): Question | null
```

From src/types/question.ts:
```typescript
export interface QuestionOption {
  id: number | string;
  label: string;
  isCorrect: boolean;
  sublabel?: string;
}
export interface Question {
  type: string;
  targetId: number | string;
  options: QuestionOption[];
  prompt?: string;
  promptSubtext?: string;
  hasAudio?: boolean;
  optionMode?: string;
  isHarakat?: boolean;
  isConfusionQ?: boolean;
  explanation?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update shared helpers and all generators to produce 4 options (per D-01, D-02)</name>
  <files>src/engine/questions/shared.ts, src/engine/questions/sound.ts, src/engine/questions/review.ts, src/__tests__/questions.test.js</files>
  <read_first>src/engine/questions/shared.ts, src/engine/questions/sound.ts, src/engine/questions/review.ts, src/__tests__/questions.test.js</read_first>
  <behavior>
    - Test: makeOpts([target, d1, d2, d3], target.id) returns exactly 4 options
    - Test: makeOpts([target, d1], target.id) pads to 4 options from ARABIC_LETTERS (per D-02)
    - Test: makeNameOpts pads to 4 when given fewer than 4 unique letters
    - Test: makeSoundOpts pads to 4 when given fewer than 4 unique letters
    - Test: buildFallbackQuestion returns a question with 4 options
    - Test: generateSoundQs produces questions with exactly 4 options each
    - Test: generateReviewQs letter questions have 4 options each
  </behavior>
  <action>
1. In `shared.ts`:
   - `makeOpts`: Change minimum padding threshold from `u.length < 2` to `u.length < 4`. Change fallback slice from `slice(0, 2 - u.length)` to `slice(0, 4 - u.length)`. Per D-02, this pulls from ARABIC_LETTERS when pool is small.
   - `makeNameOpts`: Same change — pad to 4 instead of 2.
   - `makeSoundOpts`: Same change — pad to 4 instead of 2.
   - `buildFallbackQuestion`: Change `getDistractors(t.id, ..., 2)` to `getDistractors(t.id, ..., 3)` so fallback questions also have 4 options.

2. In `sound.ts`:
   - All `getConfusionDistractors(t.id, allPool, dCount)` calls: change `dCount` base from 1 to 2 for early lessons, and from 2 to 3 for later lessons (`isLater`). Simplify: always use `dCount = 3` since makeOpts will pad anyway.
   - The contrast_audio section with `teach.length <= 3 ? teach : [t, ...shuffle(others).slice(0, 2)]` — change the slice to `slice(0, 3)` to ensure 4 options.

3. In `review.ts`:
   - Letter review: change `getDistractors(t.id, allPool, 2)` to `getDistractors(t.id, allPool, 3)`.
   - Rule review: change `getRuleDistractors(t, allPool, 2)` to `getRuleDistractors(t, allPool, 3)`.

4. Add/update tests in `questions.test.js`:
   - Add test: "every recognition question has exactly 4 options" — run generateRecognitionQs on a lesson with 1 teachId and verify all returned questions have options.length === 4.
   - Add test: "every sound question has exactly 4 options" — same pattern.
   - Add test: "makeOpts pads to 4 from alphabet when pool is small" — call makeOpts with only [target, oneDistractor] and verify 4 options returned.
   - Add test: "buildFallbackQuestion returns 4 options".
  </action>
  <verify>
    <automated>cd "C:\Users\Nasif\Desktop\Iqra AI\tila-mobile" && npm test -- --run --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "slice(0, 3)" src/engine/questions/shared.ts returns >= 1 (distractor count changed)
    - grep -c "u.length < 4" src/engine/questions/shared.ts returns >= 2 (makeOpts + makeNameOpts padding)
    - grep -c "allPool, 3" src/engine/questions/review.ts returns >= 1
    - All existing tests pass, new 4-option tests pass
  </acceptance_criteria>
  <done>Every question from sound, review, and recognition generators has exactly 4 options. Padding from full alphabet fills gaps when pool is small. All tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Balance recognition types to 33/33/33 rule/name_to_letter/letter_to_name (per D-03, D-04, D-05)</name>
  <files>src/engine/questions/recognition.ts, src/__tests__/questions.test.js</files>
  <read_first>src/engine/questions/recognition.ts, src/__tests__/questions.test.js</read_first>
  <behavior>
    - Test: generateRecognitionQs for a multi-teach lesson produces roughly equal counts of "rule", "name_to_letter", and "letter_to_name" types (within 1 of each other)
    - Test: "letter_to_name" questions have Arabic letter as prompt, English name options (not Arabic letter options)
    - Test: single-teach lessons produce at least one of each type (rule, name_to_letter, letter_to_name)
  </behavior>
  <action>
Per D-03 (keep 50/50 rule vs name-based) and D-04/D-05 (add reverse identification at 33/33/33):

Note: "reverse identification" per D-04 maps to the existing `letter_to_name` type — shows Arabic letter prominently, asks "What is this letter?" with 4 name (text) options. This type already exists in recognition.ts but is underweighted.

**Single-teach branch (teach.length === 1):**
Already produces one of each type (rule, name_to_letter, letter_to_name, tap, find). Update distractor counts from 2 to 3 in all calls:
- `getDistractors(t.id, allPool, 1)` -> `getDistractors(t.id, allPool, 3)`
- `getDistractors(t.id, allPool, 2)` -> `getDistractors(t.id, allPool, 3)`
- `getRuleDistractors(t, allPool, 2)` -> `getRuleDistractors(t, allPool, 3)`

**Multi-teach branch (teach.length >= 2):**
Currently: 1 tap per teach, 2 rule, 2 name_to_letter, 1 letter_to_name. This is unbalanced.
Change to: 1 tap per teach, 2 rule, 2 name_to_letter, 2 letter_to_name.
- Duplicate the letter_to_name block: instead of picking 1 letter (`shuffle([...teach])[0]`), pick 2 (`shuffle([...teach]).slice(0, 2)`), and generate one letter_to_name question per letter.
- Each letter_to_name: `makeNameOpts` with teach letters (if >= 4) or teach + padding from getDistractors to reach 4 options.
- Update distractor counts in multi-teach: all `getDistractors` calls use 3 distractors.

**Review question at the end:** Change `getDistractors(rev.id, allPool, isLater ? 2 : 1)` to `getDistractors(rev.id, allPool, 3)`.

Add tests verifying type balance and 4 options.
  </action>
  <verify>
    <automated>cd "C:\Users\Nasif\Desktop\Iqra AI\tila-mobile" && npm test -- --run --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "letter_to_name" src/engine/questions/recognition.ts returns >= 3 (type used in single + multi branches)
    - grep "slice(0, 2)" src/engine/questions/recognition.ts returns 0 matches (all changed to slice(0, 3) or removed)
    - All tests pass including new type-balance test
  </acceptance_criteria>
  <done>Recognition mode generates rule, name_to_letter, and letter_to_name questions in roughly equal proportion. All questions have 4 options. letter_to_name shows Arabic letter as prompt with English name options.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Question generator output -> quiz UI | Generated questions must be structurally valid |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | D (Denial of Service) | makeOpts padding loop | mitigate | Padding uses ARABIC_LETTERS (28 items, bounded) so no infinite loop possible. Existing dedup via Set prevents unbounded growth. |
| T-05-02 | T (Tampering) | Question option correctness | mitigate | validateQuestion in shared.ts already enforces exactly 1 correct option and no duplicates — no changes needed to validation logic. |
</threat_model>

<verification>
1. `npm test -- --run` — all tests pass
2. `npm run typecheck` — no type errors
3. Run generateRecognitionQs for lesson 1 (single teach) and lesson 3 (multi teach) — verify all questions have options.length === 4
4. Verify letter_to_name questions have name labels (not Arabic letters)
</verification>

<success_criteria>
- Every question from recognition, sound, and review generators has exactly 4 options
- Recognition mode generates roughly equal rule / name_to_letter / letter_to_name questions
- Small distractor pools pad from full ARABIC_LETTERS array
- All existing and new tests pass
- No type errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-quiz-learning-engine-4-options-question-types-lesson-insight/05-01-SUMMARY.md`
</output>
