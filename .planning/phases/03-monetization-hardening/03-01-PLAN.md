---
phase: 03-monetization-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/monetization/hooks.ts
  - src/monetization/paywall.ts
  - src/monetization/analytics.ts
  - src/analytics/events.ts
  - app/(tabs)/progress.tsx
autonomous: true
requirements: [MON-01, MON-02, MON-03]
must_haves:
  truths:
    - "useCanAccessLesson returns true during loading for premium lessons"
    - "Restore purchases button is accessible on Progress tab without paywall"
    - "PAYWALL_RESULT.ERROR fires trackPurchaseFailed and shows Alert"
    - "PAYWALL_RESULT.NOT_PRESENTED fires analytics event"
    - "restore_failed event type exists in events.ts"
    - "No silent failure paths remain for user-initiated purchase/restore actions"
  artifacts:
    - path: "src/monetization/hooks.ts"
      provides: "Loading-state UX fix for premium access"
    - path: "app/(tabs)/progress.tsx"
      provides: "Standalone restore purchases button"
    - path: "src/monetization/paywall.ts"
      provides: "Complete failure analytics and user messaging"
    - path: "src/analytics/events.ts"
      provides: "restore_failed event type + not_presented result"
    - path: "src/monetization/analytics.ts"
      provides: "trackRestoreFailed analytics function"
---

<objective>
Harden monetization flows: fix loading-state false-lock, add standalone restore purchases surface, and complete failure analytics instrumentation.

Purpose: RevenueCat integration works but has three gaps: brief false-lock during loading, no standalone restore surface outside paywall, and incomplete failure analytics. All three create App Store review risk or silent failure paths.

Output: Loading fix in hooks.ts, restore button on Progress tab, complete failure analytics in paywall/events.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Fix loading-state false-lock in useCanAccessLesson</name>
  <files>src/monetization/hooks.ts</files>
  <action>Change useCanAccessLesson to return true during loading for premium lessons instead of false.</action>
  <done>No false-lock flash during subscription loading state.</done>
</task>

<task type="auto">
  <name>Task 2: Complete failure analytics for paywall and restore</name>
  <files>src/analytics/events.ts, src/monetization/analytics.ts, src/monetization/paywall.ts</files>
  <action>Add restore_failed event, wire trackPurchaseFailed in ERROR case, add Alert to ERROR case, instrument NOT_PRESENTED.</action>
  <done>Every failure path has analytics event and user-facing message.</done>
</task>

<task type="auto">
  <name>Task 3: Add standalone restore purchases button to Progress tab</name>
  <files>app/(tabs)/progress.tsx</files>
  <action>Add restore button at bottom of Progress tab, calls Purchases.restorePurchases(), tracks analytics, shows alerts.</action>
  <done>User can restore purchases without hitting paywall.</done>
</task>

</tasks>

<verification>
1. `npm run typecheck` -- no new type errors in modified files
2. Source analysis: useCanAccessLesson returns true during loading
3. Source analysis: restore button exists in progress.tsx
4. Source analysis: PAYWALL_RESULT.ERROR calls trackPurchaseFailed and Alert.alert
5. Source analysis: restore_failed event type exists in events.ts
</verification>

<success_criteria>
- Loading-state UX fix prevents false-lock flash
- Standalone restore button on Progress tab
- Complete failure analytics with no silent paths
- All three MON requirements addressed
</success_criteria>
