---
phase: 02-quiz-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/design/components/ArabicText.tsx
  - src/components/quiz/QuizQuestion.tsx
  - src/__tests__/quiz-letterhero.test.ts
autonomous: true
requirements: [QUIZ-01, QUIZ-05]
must_haves:
  truths:
    - "ArabicText component accepts 'quizOption' as a valid size prop"
    - "quizOption size renders at 52px fontSize with 114px lineHeight (same as arabicQuizHero token)"
    - "LetterHero circle is 160px diameter, not 120px"
    - "WarmGlow renders at 240px size, not 180px"
    - "LetterHero letter uses 'display' size (72px) via ArabicText"
    - "WarmGlow import uses canonical path from src/design/atmosphere/"
    - "WarmGlow has animated={true} for breathing animation on LetterHero"
  artifacts:
    - path: "src/design/components/ArabicText.tsx"
      provides: "quizOption size alias in ArabicSize union and SIZE_MAP"
      contains: "quizOption"
    - path: "src/components/quiz/QuizQuestion.tsx"
      provides: "Enlarged LetterHero circle (160px) and WarmGlow (240px)"
      contains: "width: 160"
    - path: "src/__tests__/quiz-letterhero.test.ts"
      provides: "Tests for LetterHero sizing and quizOption tier"
  key_links:
    - from: "src/design/components/ArabicText.tsx"
      to: "src/design/tokens.ts"
      via: "SIZE_MAP references typography.arabicQuizHero for quizOption"
      pattern: "quizOption.*arabicQuizHero"
    - from: "src/components/quiz/QuizQuestion.tsx"
      to: "src/design/atmosphere/WarmGlow"
      via: "import path for WarmGlow"
      pattern: "design/atmosphere"
---

<objective>
Add the `quizOption` typography tier to ArabicText and enlarge the LetterHero circle to 160px with 240px WarmGlow so the Arabic letter dominates the quiz screen as a living presence.

Purpose: QUIZ-05 requires Arabic text in quiz options at 52px (not 36px). QUIZ-01 requires the letter to be the dominant visual element with breathing glow. This plan lays the foundation that Plan 02 depends on.
Output: ArabicText with quizOption size, enlarged LetterHero, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-quiz-experience/02-CONTEXT.md
@.planning/phases/02-quiz-experience/02-UI-SPEC.md
@.planning/phases/02-quiz-experience/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/design/components/ArabicText.tsx:
```typescript
type ArabicSize = "display" | "quizHero" | "large" | "body";

const SIZE_MAP: Record<ArabicSize, { fontFamily: string; fontSize: number; lineHeight: number }> = {
  display: typography.arabicDisplay,
  quizHero: typography.arabicQuizHero,
  large: typography.arabicLarge,
  body: typography.arabicBody,
};
```

From src/design/tokens.ts:
```typescript
export const typography = {
  arabicDisplay: { fontFamily: "Amiri_400Regular", fontSize: 72, lineHeight: 158 },
  arabicQuizHero: { fontFamily: "Amiri_400Regular", fontSize: 52, lineHeight: 114 },
  arabicLarge: { fontFamily: "Amiri_400Regular", fontSize: 36, lineHeight: 72 },
  arabicBody: { fontFamily: "Amiri_400Regular", fontSize: 24, lineHeight: 48 },
  // ...
};
```

From src/components/quiz/QuizQuestion.tsx (LetterPrompt section):
```typescript
// Current WarmGlow import (uses old shim path):
import { WarmGlow } from "../onboarding/WarmGlow";

// Current circle dimensions:
// letterCircle style: width: 120, height: 120, borderRadius: 60
// WarmGlow size={180}
// ArabicText size="display" (72px) -- already correct for hero
```

From src/design/atmosphere/WarmGlow.tsx (canonical location after Phase 1):
```typescript
// AnimatedWarmGlow props: size, color, pulseMin, pulseMax, animated
// Uses breathing tokens from animations.ts, useReducedMotion built-in
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add quizOption size tier to ArabicText + write tests</name>
  <files>src/design/components/ArabicText.tsx, src/__tests__/quiz-letterhero.test.ts</files>
  <read_first>src/design/components/ArabicText.tsx, src/design/tokens.ts, src/__tests__/arabic-typography.test.ts</read_first>
  <behavior>
    - Test: ArabicText.tsx source contains "quizOption" in ArabicSize type union
    - Test: ArabicText.tsx SIZE_MAP maps quizOption to typography.arabicQuizHero (same 52px/114px token, semantic alias)
    - Test: ArabicText.tsx ArabicSize union contains exactly: display, quizHero, quizOption, large, body
    - Test: QuizQuestion.tsx LetterPrompt circle width is 160 (not 120)
    - Test: QuizQuestion.tsx WarmGlow size is 240 (not 180)
    - Test: QuizQuestion.tsx import references "design/atmosphere" (not "onboarding/WarmGlow")
    - Test: QuizQuestion.tsx WarmGlow has `animated` prop present
  </behavior>
  <action>
Per D-11 and D-12, add a `quizOption` semantic size alias to ArabicText:

1. In `src/design/components/ArabicText.tsx`:
   - Change the ArabicSize type union from:
     `type ArabicSize = "display" | "quizHero" | "large" | "body";`
     to:
     `type ArabicSize = "display" | "quizHero" | "quizOption" | "large" | "body";`
   - Add to SIZE_MAP after quizHero:
     `quizOption: typography.arabicQuizHero,  // semantic alias -- same 52px/114px for quiz option context`

2. Create `src/__tests__/quiz-letterhero.test.ts` with source-audit tests (following project convention of fs.readFileSync pattern):
   - Read ArabicText.tsx source and assert:
     - Contains `"quizOption"` in the type union
     - SIZE_MAP has `quizOption: typography.arabicQuizHero`
   - Read QuizQuestion.tsx source and assert (for Task 2 verification):
     - Circle width is 160 (not 120)
     - WarmGlow size is 240 (not 180)
     - Import path references `design/atmosphere` (not `onboarding/WarmGlow`)
     - ArabicText size is `"display"` inside LetterPrompt
     - WarmGlow has `animated` prop present

Note: The quizOption tests should be written FIRST (red), then implement, then verify (green). The QuizQuestion assertions will fail until Task 2 completes -- this is expected TDD behavior. Write but skip those tests initially (use `test.skip` or `describe.skip`), then un-skip after Task 2.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/quiz-letterhero.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "quizOption" src/design/components/ArabicText.tsx returns at least 2 (type union + SIZE_MAP)
    - grep "arabicQuizHero" src/design/components/ArabicText.tsx matches the quizOption entry
    - The ArabicText quizOption tests pass (QuizQuestion tests may be skipped pending Task 2)
  </acceptance_criteria>
  <done>ArabicText accepts "quizOption" as a size prop and renders at 52px/114px. Test file created with assertions for both ArabicText and QuizQuestion (QuizQuestion assertions un-skipped after Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Enlarge LetterHero circle to 160px with 240px WarmGlow</name>
  <files>src/components/quiz/QuizQuestion.tsx</files>
  <read_first>src/components/quiz/QuizQuestion.tsx, src/design/atmosphere/WarmGlow.tsx, src/__tests__/quiz-letterhero.test.ts</read_first>
  <action>
Per D-01 and D-02, make the LetterHero the dominant visual presence:

1. Update WarmGlow import from shim path to canonical path:
   - Change: `import { WarmGlow } from "../onboarding/WarmGlow";`
   - To: `import { WarmGlow } from "../../design/atmosphere/WarmGlow";`

2. In the LetterPrompt component, update WarmGlow props:
   - Change `size={180}` to `size={240}` (1.5x circle diameter)
   - Keep all other WarmGlow props the same: `animated`, `color="rgba(196,164,100,0.3)"`, `pulseMin={0.1}`, `pulseMax={0.3}`

3. In the StyleSheet, update letterCircle dimensions:
   - Change `width: 120` to `width: 160`
   - Change `height: 120` to `height: 160`
   - Change `borderRadius: 60` to `borderRadius: 80` (half of 160)

4. Un-skip the QuizQuestion assertions in `src/__tests__/quiz-letterhero.test.ts` that were skipped in Task 1 (change `describe.skip` or `test.skip` to `describe`/`test`).

No other changes to QuizQuestion.tsx. The letter already uses `size="display"` (72px) which is correct for the larger circle. The letterCircleWrapper already centers both WarmGlow and circle via alignItems/justifyContent.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/quiz-letterhero.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep "width: 160" src/components/quiz/QuizQuestion.tsx matches
    - grep "height: 160" src/components/quiz/QuizQuestion.tsx matches
    - grep "borderRadius: 80" src/components/quiz/QuizQuestion.tsx matches
    - grep "size={240}" src/components/quiz/QuizQuestion.tsx matches (WarmGlow)
    - grep "design/atmosphere" src/components/quiz/QuizQuestion.tsx matches (canonical import)
    - All quiz-letterhero tests pass (including un-skipped QuizQuestion assertions)
  </acceptance_criteria>
  <done>LetterHero circle is 160px with 240px WarmGlow breathing animation. Letter is the dominant visual element. WarmGlow imported from canonical atmosphere path. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries apply -- this plan modifies only UI presentation (typography tiers, component dimensions). No data handling, authentication, or external communication.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | N/A | N/A | accept | Pure UI changes -- no security surface |
</threat_model>

<verification>
1. `npx vitest run src/__tests__/quiz-letterhero.test.ts --reporter=verbose` -- all tests pass
2. `npm run typecheck` -- no new type errors from quizOption addition
3. `npm test` -- full suite passes (no regressions)
</verification>

<success_criteria>
- ArabicText component has quizOption size tier mapping to 52px/114px
- LetterHero circle is 160px diameter with 240px WarmGlow
- WarmGlow uses canonical import path from design/atmosphere
- WarmGlow animated prop verified by test
- All tests in quiz-letterhero.test.ts pass
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-quiz-experience/02-01-SUMMARY.md`
</output>
