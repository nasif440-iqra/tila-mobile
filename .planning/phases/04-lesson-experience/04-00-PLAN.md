---
phase: 04-lesson-experience
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/lesson-intro.test.ts
  - src/__tests__/quiz-progress.test.ts
  - src/__tests__/quiz-question.test.ts
  - src/__tests__/wrong-answer.test.ts
  - src/__tests__/lesson-summary.test.ts
  - src/__tests__/exercise-haptics.test.ts
autonomous: true
requirements: [LES-01, LES-02, LES-03, LES-04, LES-05, LES-06]

must_haves:
  truths:
    - "Test stubs exist for all 6 phase requirements before implementation begins"
    - "Each test file validates the specific behavior change its requirement demands"
    - "All tests fail initially (RED state) since implementation has not happened yet"
  artifacts:
    - path: "src/__tests__/lesson-intro.test.ts"
      provides: "LES-01 test: WarmGlow integration and staggered entrance in LessonIntro"
    - path: "src/__tests__/quiz-progress.test.ts"
      provides: "LES-02 test: springs.gentle preset and no hardcoded springs in QuizProgress"
    - path: "src/__tests__/quiz-question.test.ts"
      provides: "LES-03 test: correct feedback pill scale entrance in QuizQuestion"
    - path: "src/__tests__/wrong-answer.test.ts"
      provides: "LES-04 test: WRONG_ENCOURAGEMENT prefix in WrongAnswerPanel"
    - path: "src/__tests__/lesson-summary.test.ts"
      provides: "LES-05 test: score-proportional WarmGlow and haptics in LessonSummary"
    - path: "src/__tests__/exercise-haptics.test.ts"
      provides: "LES-06 test: no direct expo-haptics imports in exercise components"
  key_links:
    - from: "src/__tests__/exercise-haptics.test.ts"
      to: "src/components/exercises/*.tsx"
      via: "Import audit reads source files for expo-haptics imports"
      pattern: "expo-haptics"
---

<objective>
Create test stubs for all 6 phase requirements (Nyquist Wave 0). These tests define expected behaviors BEFORE implementation, ensuring every plan in wave 1 has automated verification targets.

Purpose: Satisfy Nyquist rule -- every verify block in wave 1 plans must have a test file to run against. Tests start RED and go GREEN as wave 1 plans execute.
Output: 6 test files in src/__tests__/ covering LES-01 through LES-06.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-lesson-experience/04-CONTEXT.md
@.planning/phases/04-lesson-experience/04-RESEARCH.md

<interfaces>
From src/design/haptics.ts:
```typescript
export function hapticTap(): void;
export function hapticSuccess(): void;
export function hapticError(): void;
export function hapticMilestone(): void;
export function hapticSelection(): void;
```

From src/design/animations.ts:
```typescript
export const springs = {
  press: { stiffness: 400, damping: 20, mass: 0.8 },
  bouncy: { stiffness: 300, damping: 18 },
  gentle: { stiffness: 200, damping: 22 },
  snap: { stiffness: 500, damping: 25 },
} as const;
```

From src/engine/engagement.js:
```javascript
export const WRONG_ENCOURAGEMENT = [...];
export const MID_CELEBRATE_COPY = { default: [...], harakat: [...] };
export function pickCopy(pool: string[]): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for LES-01 through LES-04 (intro, quiz progress, quiz question, wrong answer)</name>
  <files>
    src/__tests__/lesson-intro.test.ts
    src/__tests__/quiz-progress.test.ts
    src/__tests__/quiz-question.test.ts
    src/__tests__/wrong-answer.test.ts
  </files>
  <read_first>
    src/__tests__/
    src/components/LessonIntro.tsx
    src/components/quiz/QuizProgress.tsx
    src/components/quiz/QuizQuestion.tsx
    src/components/quiz/WrongAnswerPanel.tsx
    vitest.config.ts
  </read_first>
  <action>
    Create 4 test files. Each test should use `vi.mock` to mock react-native-reanimated and expo modules. Tests validate the BEHAVIOR defined in requirements, not implementation details.

    **src/__tests__/lesson-intro.test.ts (LES-01):**
    - Test: LessonIntro source code imports WarmGlow (read file as string, check import presence)
    - Test: LessonIntro source code imports springs from design/animations
    - Test: LessonIntro source code uses staggers.fast.delay for staggered entrance

    **src/__tests__/quiz-progress.test.ts (LES-02):**
    - Test: QuizProgress source code imports springs from design/animations
    - Test: QuizProgress source code does NOT contain hardcoded `stiffness: 120`
    - Test: QuizProgress source code contains `interpolateColor` for color transition

    **src/__tests__/quiz-question.test.ts (LES-03):**
    - Test: QuizQuestion source code imports springs from design/animations
    - Test: QuizQuestion source code contains `springs.press` for correct feedback pulse
    - Test: QuizQuestion source code does NOT import from design/haptics (haptics owned by QuizOption)

    **src/__tests__/wrong-answer.test.ts (LES-04):**
    - Test: WrongAnswerPanel source code imports WRONG_ENCOURAGEMENT from engagement
    - Test: WrongAnswerPanel source code imports pickCopy from engagement
    - Test: WrongAnswerPanel source code uses encouragement prefix in explanation text

    Use `fs.readFileSync` to read source files as strings and assert on content patterns. This is a source-audit approach that works without rendering (avoids heavy React Native mocking).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lesson-intro.test.ts src/__tests__/quiz-progress.test.ts src/__tests__/quiz-question.test.ts src/__tests__/wrong-answer.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>4 test files exist and run (expected to FAIL since implementation has not happened). Each validates the specific behavior change for LES-01 through LES-04.</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for LES-05 and LES-06 (lesson summary, exercise haptics)</name>
  <files>
    src/__tests__/lesson-summary.test.ts
    src/__tests__/exercise-haptics.test.ts
  </files>
  <read_first>
    src/components/LessonSummary.tsx
    src/components/exercises/GuidedReveal.tsx
    src/components/exercises/TapInOrder.tsx
    src/components/exercises/BuildUpReader.tsx
    src/components/exercises/FreeReader.tsx
    src/components/exercises/SpotTheBreak.tsx
    src/components/exercises/ComprehensionExercise.tsx
  </read_first>
  <action>
    Create 2 test files using the same source-audit pattern (read file as string, assert on content patterns).

    **src/__tests__/lesson-summary.test.ts (LES-05):**
    - Test: LessonSummary source code imports WarmGlow
    - Test: LessonSummary source code imports hapticMilestone from design/haptics
    - Test: LessonSummary source code contains conditional WarmGlow render (`percentage >= 50`)
    - Test: LessonSummary source code contains score-proportional haptic calls (hapticMilestone, hapticSuccess, hapticTap)

    **src/__tests__/exercise-haptics.test.ts (LES-06):**
    - Test: For each of 6 exercise files, source code does NOT contain `import * as Haptics from "expo-haptics"`
    - Test: For each of 6 exercise files, source code imports from `design/haptics`
    - Test: GuidedReveal source code imports WarmGlow
    - Test: LessonHybrid source code contains `springs.gentle` (not hardcoded stiffness: 120)

    The exercise-haptics test should iterate over all 6 exercise file paths and assert each one individually with descriptive test names.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lesson-summary.test.ts src/__tests__/exercise-haptics.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>2 test files exist and run (expected to FAIL). exercise-haptics.test.ts covers all 6 exercise components plus LessonHybrid. lesson-summary.test.ts covers score-proportional celebration tiers.</done>
</task>

</tasks>

<verification>
- All 6 test files exist in src/__tests__/
- `npx vitest run src/__tests__/lesson-intro.test.ts src/__tests__/quiz-progress.test.ts src/__tests__/quiz-question.test.ts src/__tests__/wrong-answer.test.ts src/__tests__/lesson-summary.test.ts src/__tests__/exercise-haptics.test.ts` runs without parse errors (tests may fail -- that is expected RED state)
- Each test file covers exactly one LES-XX requirement
</verification>

<success_criteria>
- 6 test files created, one per requirement (LES-01 through LES-06)
- Tests run without syntax/parse errors
- Tests are in RED state (failing) since wave 1 implementation has not happened
- Wave 1 plans can reference these tests in their verify blocks
</success_criteria>

<output>
After completion, create `.planning/phases/04-lesson-experience/04-00-SUMMARY.md`
</output>
