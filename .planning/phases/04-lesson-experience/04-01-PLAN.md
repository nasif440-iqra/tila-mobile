---
phase: 04-lesson-experience
plan: 01
type: execute
wave: 1
depends_on: [04-00]
files_modified:
  - src/components/exercises/GuidedReveal.tsx
  - src/components/exercises/TapInOrder.tsx
  - src/components/exercises/BuildUpReader.tsx
  - src/components/exercises/FreeReader.tsx
  - src/components/exercises/SpotTheBreak.tsx
  - src/components/exercises/ComprehensionExercise.tsx
  - src/components/LessonHybrid.tsx
autonomous: true
requirements: [LES-06]

must_haves:
  truths:
    - "All 6 exercise types use centralized haptic utilities instead of direct expo-haptics imports"
    - "LessonHybrid progress bar uses springs.gentle instead of hardcoded spring config"
    - "Exercise transitions feel smooth with design preset timings"
    - "Stage indicator badges use design token colors consistently"
    - "Exercise-to-exercise transitions use smooth opacity fades"
  artifacts:
    - path: "src/components/exercises/GuidedReveal.tsx"
      provides: "Haptic migration + WarmGlow on letter header"
      contains: "hapticTap"
    - path: "src/components/exercises/TapInOrder.tsx"
      provides: "Haptic migration"
      contains: "hapticSuccess"
    - path: "src/components/exercises/BuildUpReader.tsx"
      provides: "Haptic migration"
      contains: "hapticTap"
    - path: "src/components/exercises/FreeReader.tsx"
      provides: "Haptic migration"
      contains: "hapticSuccess"
    - path: "src/components/exercises/SpotTheBreak.tsx"
      provides: "Haptic migration"
      contains: "hapticSuccess"
    - path: "src/components/exercises/ComprehensionExercise.tsx"
      provides: "Haptic migration"
      contains: "hapticSuccess"
    - path: "src/components/LessonHybrid.tsx"
      provides: "Spring preset migration + haptic migration + stage badge tokens + exercise fade transitions"
      contains: "springs.gentle"
  key_links:
    - from: "src/components/exercises/*.tsx"
      to: "src/design/haptics.ts"
      via: "import { hapticTap, hapticSuccess, hapticError }"
      pattern: "from.*design/haptics"
    - from: "src/components/LessonHybrid.tsx"
      to: "src/design/animations.ts"
      via: "import { springs }"
      pattern: "springs\\.gentle"
    - from: "src/components/LessonHybrid.tsx"
      to: "src/design/tokens.ts"
      via: "Stage badge colors from design tokens"
      pattern: "colors\\.(primary|accent|text)"
---

<objective>
Migrate all 6 exercise components and LessonHybrid to use centralized design system utilities (haptics + springs), add WarmGlow to GuidedReveal letter header, add stage indicator badge tokens (D-11), and add exercise transition fades (D-12).

Purpose: Ensure exercise screens feel consistent and polished with standardized haptic feedback, animation timings, token-based badges, and smooth transitions (per D-10, D-11, D-12, D-13, D-14).
Output: 7 modified component files using design system presets instead of direct expo-haptics/hardcoded springs, with stage badges and exercise fades.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-lesson-experience/04-CONTEXT.md
@.planning/phases/04-lesson-experience/04-UI-SPEC.md
@.planning/phases/04-lesson-experience/04-RESEARCH.md

<interfaces>
From src/design/haptics.ts:
```typescript
export function hapticTap(): void;
export function hapticSuccess(): void;
export function hapticError(): void;
export function hapticMilestone(): void;
export function hapticSelection(): void;
```

From src/design/animations.ts:
```typescript
export const springs = {
  press: { stiffness: 400, damping: 20, mass: 0.8 },
  bouncy: { stiffness: 300, damping: 18 },
  gentle: { stiffness: 200, damping: 22 },
  snap: { stiffness: 500, damping: 25 },
} as const;

export const durations = {
  fast: 150, micro: 200, normal: 300, slow: 400, dramatic: 600,
} as const;
```

From src/components/onboarding/WarmGlow.tsx:
```typescript
interface WarmGlowProps {
  size: number;
  animated?: boolean;
  color?: string;
  pulseMin?: number;
  pulseMax?: number;
}
export function WarmGlow(props: WarmGlowProps): JSX.Element;
```

From src/design/tokens.ts:
```typescript
// colors.primary, colors.accent, colors.primarySoft, colors.text, colors.textSecondary
// Used for stage badge backgrounds and text
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate haptics in all 6 exercise components</name>
  <files>
    src/components/exercises/GuidedReveal.tsx
    src/components/exercises/TapInOrder.tsx
    src/components/exercises/BuildUpReader.tsx
    src/components/exercises/FreeReader.tsx
    src/components/exercises/SpotTheBreak.tsx
    src/components/exercises/ComprehensionExercise.tsx
  </files>
  <read_first>
    src/components/exercises/GuidedReveal.tsx
    src/components/exercises/TapInOrder.tsx
    src/components/exercises/BuildUpReader.tsx
    src/components/exercises/FreeReader.tsx
    src/components/exercises/SpotTheBreak.tsx
    src/components/exercises/ComprehensionExercise.tsx
    src/design/haptics.ts
  </read_first>
  <action>
    Per D-14 (haptics on all interactive elements using Phase 1 haptic presets) and D-10 (consistent polished look):

    For each of the 6 exercise files, perform these exact replacements:

    **GuidedReveal.tsx:**
    - Remove: `import * as Haptics from "expo-haptics";` (line ~7)
    - Add: `import { hapticTap } from "../../design/haptics";`
    - Replace all `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light)` with `hapticTap()`
    - Add WarmGlow behind the letter header (per UI spec GuidedReveal Specifics):
      - Import `WarmGlow` from `"../onboarding/WarmGlow"` (path relative to exercises/ is `"../onboarding/WarmGlow"`)
      - Wrap the letter display area with a container that positions WarmGlow absolutely behind the Arabic letter
      - WarmGlow props: size={120}, animated={true}, color="rgba(196,164,100,0.3)", pulseMin={0.04}, pulseMax={0.12}

    **TapInOrder.tsx:**
    - Remove: `import * as Haptics from "expo-haptics";` (line ~11)
    - Add: `import { hapticSuccess, hapticError } from "../../design/haptics";`
    - Replace `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success)` with `hapticSuccess()`
    - Replace `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Error)` with `hapticError()`

    **BuildUpReader.tsx:**
    - Remove: `import * as Haptics from "expo-haptics";` (line ~6)
    - Add: `import { hapticTap } from "../../design/haptics";`
    - Replace `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light)` with `hapticTap()`

    **FreeReader.tsx:**
    - Remove: `import * as Haptics from "expo-haptics";` (line ~6)
    - Add: `import { hapticSuccess, hapticTap } from "../../design/haptics";`
    - Replace `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success)` with `hapticSuccess()`
    - Replace `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light)` with `hapticTap()` (if present, otherwise replace any remaining direct Haptics calls)

    **SpotTheBreak.tsx:**
    - Remove: `import * as Haptics from "expo-haptics";` (line ~11)
    - Add: `import { hapticSuccess, hapticError } from "../../design/haptics";`
    - Replace `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success)` with `hapticSuccess()`
    - Replace `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Error)` with `hapticError()`

    **ComprehensionExercise.tsx:**
    - Remove: `import * as Haptics from "expo-haptics";` (line ~5)
    - Add: `import { hapticSuccess, hapticError } from "../../design/haptics";`
    - Replace `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success)` with `hapticSuccess()`
    - Replace `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Error)` with `hapticError()`

    IMPORTANT: Do NOT refactor any hooks-in-loops patterns in TapInOrder or SpotTheBreak (per Pitfall 4 in Research). Only change haptic imports/calls.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Zero files in src/components/exercises/ contain `import * as Haptics from "expo-haptics"`
    - All 6 exercise files import from `../../design/haptics` instead
    - GuidedReveal.tsx contains `WarmGlow` import and renders WarmGlow with size={120}
    - `npm run typecheck` passes with no new errors
  </acceptance_criteria>
  <done>All 6 exercise components use centralized haptic utilities. GuidedReveal has WarmGlow behind letter header. No direct expo-haptics imports remain in exercises/.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate LessonHybrid spring config and haptics</name>
  <files>src/components/LessonHybrid.tsx</files>
  <read_first>
    src/components/LessonHybrid.tsx
    src/design/animations.ts
    src/design/haptics.ts
  </read_first>
  <action>
    Per D-13 (use Phase 1 shared animation presets) and D-14 (haptics on all interactive elements):

    In LessonHybrid.tsx:

    1. **Remove direct Haptics import:**
       - Remove: `import * as Haptics from "expo-haptics";` (line 10)
       - Add: `import { hapticTap } from "../design/haptics";`

    2. **Add springs import:**
       - Update line 14 from `import { durations } from "../design/animations";` to `import { durations, springs } from "../design/animations";`

    3. **Replace hardcoded spring config (lines 98-100):**
       - Before: `progressWidth.value = withSpring(hybrid.progress, { stiffness: 120, damping: 20 });`
       - After: `progressWidth.value = withSpring(hybrid.progress, springs.gentle);`

    4. **Add hapticTap to CloseButton:**
       - In the CloseButton onPress handler or in handleBack, add `hapticTap()` call before `router.back()`

    Note: The spring values change from {stiffness: 120, damping: 20} to {stiffness: 200, damping: 22}. This is intentional per spec -- snappier, more responsive feel.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - LessonHybrid.tsx contains `import { hapticTap } from "../design/haptics"`
    - LessonHybrid.tsx contains `springs.gentle` (not `stiffness: 120`)
    - LessonHybrid.tsx does NOT contain `import * as Haptics`
    - `npm run typecheck` passes
  </acceptance_criteria>
  <done>LessonHybrid uses design system springs and haptic utilities instead of hardcoded values and direct imports.</done>
</task>

<task type="auto">
  <name>Task 3: Add stage indicator badge tokens (D-11) and exercise transition fades (D-12)</name>
  <files>src/components/LessonHybrid.tsx</files>
  <read_first>
    src/components/LessonHybrid.tsx
    src/design/tokens.ts
    src/design/animations.ts
  </read_first>
  <action>
    Per D-11 (stage indicator badges use design tokens consistently) and D-12 (exercise transitions should be smooth fades):

    **D-11 -- Stage indicator badge tokens:**
    In LessonHybrid.tsx, find where the stage indicator badge is rendered (the badge showing "Learning" | "Building" | "Reading" for each exercise type). Update:
    1. Badge background: use `colors.primarySoft` (from useColors()) instead of any hardcoded color
    2. Badge text: use `colors.primary` for the label text
    3. Badge border radius: use 12 (rounded pill shape)
    4. Badge padding: horizontal 12, vertical 4 (consistent with design token spacing rhythm of 8px base)
    5. Badge font: use the Inter font family at 13px (body small), fontWeight "600"
    6. If there is no explicit badge styling yet (just plain text), wrap the stage label in a styled View+Text to create the badge appearance

    **D-12 -- Exercise transition fades:**
    In LessonHybrid.tsx, find where exercise components are rendered/switched. Add a smooth opacity fade transition between exercises:
    1. Import `FadeIn, FadeOut` from react-native-reanimated (add to existing reanimated import)
    2. Wrap the exercise content area in an `Animated.View` with `entering={FadeIn.duration(durations.normal)}` and `exiting={FadeOut.duration(durations.fast)}`
    3. Use a `key` prop on the Animated.View tied to the current exercise index or step ID, so React re-mounts the wrapper when the exercise changes, triggering the fade transition
    4. `durations.normal` (300ms) for entering and `durations.fast` (150ms) for exiting per design presets

    IMPORTANT: Only modify styling and add layout animations. Do NOT change any exercise logic, hook patterns, or state management.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - LessonHybrid.tsx stage badge uses `colors.primarySoft` for background and `colors.primary` for text
    - LessonHybrid.tsx exercise area uses `FadeIn` and `FadeOut` for transitions
    - LessonHybrid.tsx exercise wrapper has a `key` prop that changes with exercise step
    - `npm run typecheck` passes
  </acceptance_criteria>
  <done>Stage indicator badges use design token colors consistently (D-11). Exercise transitions use smooth opacity fades between exercises (D-12).</done>
</task>

</tasks>

<verification>
- `grep -rn "import \* as Haptics" src/components/exercises/ src/components/LessonHybrid.tsx` returns nothing
- `grep -rn "stiffness: 120" src/components/LessonHybrid.tsx` returns nothing
- `grep -rn "hapticTap\|hapticSuccess\|hapticError" src/components/exercises/` shows all 6 files using design haptics
- `grep -rn "FadeIn\|FadeOut" src/components/LessonHybrid.tsx` shows exercise transition imports
- `grep -rn "primarySoft" src/components/LessonHybrid.tsx` shows stage badge token usage
- `npm run typecheck` passes
- `npm test -- --run` passes
</verification>

<success_criteria>
- All 7 files (6 exercises + LessonHybrid) use centralized design system utilities
- Zero direct expo-haptics imports remain in modified files
- Zero hardcoded spring configs remain in modified files
- GuidedReveal has WarmGlow behind letter header
- Stage indicator badges use design token colors (D-11)
- Exercise transitions fade smoothly between steps (D-12)
- TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-lesson-experience/04-01-SUMMARY.md`
</output>
