---
phase: 04-value-communication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/insights.ts
  - src/__tests__/lesson-insights.test.ts
  - src/__tests__/review-schedule.test.ts
autonomous: true
requirements: [CONV-03, CONV-05]

must_haves:
  truths:
    - "generatePostLessonInsights returns confusion insight with caring teacher tone when confused pairs involve lesson letters"
    - "generatePostLessonInsights returns review timing insight with human-readable day name"
    - "generatePostLessonInsights returns accuracy trend insight when improvement detected"
    - "generatePostLessonInsights returns empty array when no interesting data exists (D-04)"
    - "groupReviewsByDay correctly buckets entities into today/tomorrow/thisWeek groups"
    - "parseConfusionPairs returns letter names and Arabic characters for display"
  artifacts:
    - path: "src/engine/insights.ts"
      provides: "Pure engine logic for post-lesson insights and progress tab data"
      exports: ["generatePostLessonInsights", "groupReviewsByDay", "parseConfusionPairs", "LessonInsight", "ReviewGroups", "ConfusionPairDisplay"]
    - path: "src/__tests__/lesson-insights.test.ts"
      provides: "Unit tests for generatePostLessonInsights"
      min_lines: 50
    - path: "src/__tests__/review-schedule.test.ts"
      provides: "Unit tests for groupReviewsByDay and parseConfusionPairs"
      min_lines: 30
  key_links:
    - from: "src/engine/insights.ts"
      to: "src/engine/selectors.js"
      via: "import getTopConfusions"
      pattern: "getTopConfusions"
    - from: "src/engine/insights.ts"
      to: "src/data/letters.js"
      via: "import getLetter for name lookups"
      pattern: "getLetter"
---

<objective>
Create the pure engine logic layer for value communication: post-lesson insights and progress tab data computation.

Purpose: Build testable, React-free functions that generate insight messages (confusion detection, review timing, accuracy trends) and group review schedules for the progress tab. This is the data foundation that Plan 02 will wire into UI components.

Output: `src/engine/insights.ts` with exported functions and types, plus comprehensive unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-value-communication/04-CONTEXT.md
@.planning/phases/04-value-communication/04-RESEARCH.md

@src/engine/selectors.js
@src/engine/mastery.js
@src/data/letters.js

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/engine/selectors.js (line 140):
```javascript
export function getTopConfusions(confusions, limit = 5) {
  // Returns [{ key, count, lastSeen }]
  // key format: "recognition:2->3" or "sound:7->8" or "harakat:ba-fatha->ba-kasra"
}
```

From src/engine/mastery.js (line 107):
```javascript
const DEFAULT_ENTITY = {
  correct: 0,
  attempts: 0,
  lastSeen: null,
  nextReview: null,
  intervalDays: 1,
  sessionStreak: 0,
  lastLatencyMs: null,
};
```

From src/data/letters.js (line 47):
```javascript
export function getLetter(id) {
  // Returns { id, letter, name, ... } or undefined
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/engine/insights.ts with post-lesson and progress tab logic</name>
  <files>src/engine/insights.ts, src/__tests__/lesson-insights.test.ts, src/__tests__/review-schedule.test.ts</files>
  <read_first>
    - src/engine/selectors.js (lines 139-146 for getTopConfusions signature and return shape)
    - src/engine/mastery.js (lines 107-115 for DEFAULT_ENTITY shape, lines 280-318 for deriveMasteryState)
    - src/data/letters.js (lines 47-55 for getLetter signature)
    - .planning/phases/04-value-communication/04-CONTEXT.md (D-03 for insight types, D-04 for empty behavior, D-10/D-11/D-12 for tone)
  </read_first>
  <behavior>
    - Test: generatePostLessonInsights with confusion data involving lesson letters returns insight with type 'confusion' and message containing "Tila noticed you mixed up" and letter names (not IDs)
    - Test: generatePostLessonInsights with entity having nextReview returns insight with type 'review' and message containing day name (e.g., "Monday") not ISO date
    - Test: generatePostLessonInsights with accuracy improvement (entity.correct/attempts minus session results shows prior was lower) returns insight with type 'trend' and message containing "getting stronger"
    - Test: generatePostLessonInsights with empty mastery ({entities:{}, confusions:{}}) returns empty array
    - Test: generatePostLessonInsights with confusions not involving lesson letters returns empty (no confusion insight)
    - Test: generatePostLessonInsights returns max 1 insight per type, max 3 total
    - Test: groupReviewsByDay correctly puts entity with nextReview === today in 'today' bucket
    - Test: groupReviewsByDay correctly puts entity with nextReview === tomorrow in 'tomorrow' bucket
    - Test: groupReviewsByDay correctly puts entity with nextReview 3 days from now in 'thisWeek' bucket
    - Test: groupReviewsByDay ignores entities with null nextReview
    - Test: parseConfusionPairs parses "recognition:2->3" into letter names and Arabic characters
    - Test: parseConfusionPairs skips harakat confusion keys (prefix === 'harakat')
  </behavior>
  <action>
    **Write tests first (RED), then implement (GREEN).**

    Create `src/engine/insights.ts` as a pure TypeScript module with zero React dependencies.

    **Exported types:**
    ```typescript
    export interface LessonInsight {
      type: 'confusion' | 'review' | 'trend';
      message: string;
    }

    export interface ReviewGroups {
      today: Array<{ entityKey: string; letterName: string; letterChar: string }>;
      tomorrow: Array<{ entityKey: string; letterName: string; letterChar: string }>;
      thisWeek: Array<{ entityKey: string; letterName: string; letterChar: string }>;
    }

    export interface ConfusionPairDisplay {
      letter1Name: string;
      letter1Char: string;
      letter2Name: string;
      letter2Char: string;
      count: number;
    }
    ```

    **Exported functions:**

    1. `generatePostLessonInsights(mastery, lessonLetterIds, sessionResults)` -> `LessonInsight[]`
       - `mastery`: `{ entities: Record<string, any>; confusions: Record<string, any> }`
       - `lessonLetterIds`: `number[]` — letter IDs from lesson.teachIds + lesson.reviewIds
       - `sessionResults`: `Map<number, { correct: number; total: number }>` — per-letter accuracy from just-completed session
       - Returns max 3 insights (1 per type), empty array if no interesting data (per D-04)
       - **Confusion (D-03.1):** Call `getTopConfusions(mastery.confusions, 10)`, find first pair involving a lesson letter. Parse key format "recognition:2->3" by splitting on ":" then "->". Skip harakat keys. Use `getLetter(id)` for names. Message: `"Tila noticed you mixed up ${name1} and ${name2} — we'll practice these together"` (per D-12)
       - **Review (D-03.2):** Check `mastery.entities[letter:${id}].nextReview` for lesson letters. Use `new Date(nextReview).toLocaleDateString('en-US', { weekday: 'long' })` for day name. Message: `"Review ${letterName} on ${dayName}"` (per D-12)
       - **Trend (D-03.3):** For each lesson letter with sessionResults, compute previous accuracy as `(entity.correct - sessionCorrect) / (entity.attempts - sessionTotal)`. If current session accuracy > previous by 10+ percentage points AND entity.attempts > sessionTotal (has prior data), show trend. Message: `"You're getting stronger with ${letterName} (${prevPct}% → ${currPct}%)"` (per D-12). Use right arrow character (→).

    2. `groupReviewsByDay(entities, today)` -> `ReviewGroups`
       - `entities`: `Record<string, { nextReview: string | null; ... }>`
       - `today`: `string` YYYY-MM-DD
       - Groups entities by nextReview relative to today. Overdue (nextReview <= today) goes in 'today'. Compute tomorrow as today + 1 day, weekEnd as today + 7 days. Use `parseEntityKey` to extract letter ID, `getLetter` for name/char.
       - Only include letter-type entities (skip skills/combos).

    3. `parseConfusionPairs(confusions, limit)` -> `ConfusionPairDisplay[]`
       - Wraps `getTopConfusions(confusions, limit)` and enriches with letter names/chars via `getLetter()`.
       - Skips harakat confusion keys.
       - Returns display-ready objects sorted by count descending.

    **Import from existing modules (JS imports work in TS):**
    ```typescript
    import { getTopConfusions } from './selectors';
    import { parseEntityKey } from './mastery.js';
    import { getLetter } from '../data/letters.js';
    ```

    **Test files:**
    - `src/__tests__/lesson-insights.test.ts` — Tests for `generatePostLessonInsights`
    - `src/__tests__/review-schedule.test.ts` — Tests for `groupReviewsByDay` and `parseConfusionPairs`

    Mock `getLetter` to return test data (e.g., `{ id: 2, letter: 'ب', name: 'Ba' }`). Mock `getTopConfusions` if needed, or construct test confusion objects directly.

    Use Vitest `vi.mock()` for mocking. Test file pattern: `src/__tests__/*.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lesson-insights.test.ts src/__tests__/review-schedule.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - File src/engine/insights.ts exists and exports: generatePostLessonInsights, groupReviewsByDay, parseConfusionPairs, LessonInsight, ReviewGroups, ConfusionPairDisplay
    - grep "Tila noticed you mixed up" src/engine/insights.ts returns a match (D-12 caring tone)
    - grep "getting stronger" src/engine/insights.ts returns a match (D-12 trend message)
    - grep "Review .* on" src/engine/insights.ts returns a match (D-12 review message)
    - grep "return \[\]" src/engine/insights.ts confirms empty array return for no-data case (D-04)
    - Tests in src/__tests__/lesson-insights.test.ts cover: confusion insight, review insight, trend insight, empty mastery returns [], confusions not involving lesson letters returns no confusion insight
    - Tests in src/__tests__/review-schedule.test.ts cover: today/tomorrow/thisWeek grouping, null nextReview skipped, parseConfusionPairs skips harakat
    - npx vitest run src/__tests__/lesson-insights.test.ts src/__tests__/review-schedule.test.ts -x passes with 0 failures
    - npm run typecheck passes with 0 errors
  </acceptance_criteria>
  <done>
    Pure engine module src/engine/insights.ts exports all three functions with correct types. All unit tests pass. No React dependencies in the module. Messages follow D-12 caring teacher tone. Empty mastery returns empty array per D-04.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/lesson-insights.test.ts src/__tests__/review-schedule.test.ts -x` — all tests pass
- `npm run typecheck` — zero errors
- `npm run validate` — lint + typecheck clean
</verification>

<success_criteria>
- src/engine/insights.ts exists with generatePostLessonInsights, groupReviewsByDay, parseConfusionPairs exported
- All unit tests pass covering confusion/review/trend insights, empty states, review grouping, confusion pair parsing
- No React dependencies in the engine module
- typecheck passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-value-communication/04-01-SUMMARY.md`
</output>
