---
phase: 06-progress-screen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/progress-mastery-grid.test.ts
  - src/__tests__/progress-phase-bars.test.ts
  - src/components/progress/LetterMasteryGrid.tsx
  - src/components/progress/PhasePanel.tsx
autonomous: true
requirements: [PROG-02, PROG-03]

must_haves:
  truths:
    - "Letter mastery grid shows 5 immediately distinguishable visual states"
    - "Retained state is visually distinct from accurate state (not just same green)"
    - "Phase progress bars animate smoothly from 0 to actual percentage"
    - "Progress bars use springs.gentle from shared animation presets"
  artifacts:
    - path: "src/components/progress/LetterMasteryGrid.tsx"
      provides: "5-state mastery visualization with color + opacity + border differentiation"
      contains: "retained.*accurate"
    - path: "src/components/progress/PhasePanel.tsx"
      provides: "Spring-animated progress bar fill"
      contains: "springs.gentle"
    - path: "src/__tests__/progress-mastery-grid.test.ts"
      provides: "Source-audit tests for 5 distinct mastery states"
    - path: "src/__tests__/progress-phase-bars.test.ts"
      provides: "Source-audit tests for animated progress bar with springs.gentle"
  key_links:
    - from: "src/components/progress/PhasePanel.tsx"
      to: "src/design/animations.ts"
      via: "springs.gentle import"
      pattern: "springs\\.gentle"
    - from: "src/components/progress/LetterMasteryGrid.tsx"
      to: "src/design/tokens.ts"
      via: "color tokens for 5 states"
      pattern: "primaryDark|primarySoft|accentLight|shadows"
---

<objective>
Polish LetterMasteryGrid with 5 immediately distinguishable mastery states and PhasePanel with spring-animated progress bars.

Purpose: The two most data-heavy components on the progress screen need visual differentiation (mastery grid) and life (progress bars). Currently retained and accurate look identical, and progress bars are static widths.
Output: Two polished components with source-audit tests validating the visual requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-progress-screen/06-CONTEXT.md
@.planning/phases/06-progress-screen/06-RESEARCH.md

@src/design/animations.ts
@src/design/tokens.ts
@src/components/progress/LetterMasteryGrid.tsx
@src/components/progress/PhasePanel.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/design/animations.ts:
```typescript
export const springs = {
  gentle: { stiffness: 200, damping: 22 },
} as const;

export const durations = {
  normal: 300,
} as const;

export const easings = {
  contentReveal: Easing.out(Easing.cubic),
} as const;
```

From src/design/tokens.ts:
```typescript
export const lightColors = {
  primaryDark: "#0F2419",
  primarySoft: "#E8F0EB",
  primary: "#163323",
  accentLight: "#F5EDDB",
  accent: "#C4A464",
  bgCard: "#FFFFFF",
  border: "#EBE6DC",
  textMuted: "#6B6760",
  textSoft: "#52545C",
};

export const shadows = {
  card: { shadowColor: "#000", shadowOffset: { width: 0, height: 2 }, shadowOpacity: 0.08, shadowRadius: 8, elevation: 3 },
  cardLifted: { shadowColor: "#000", shadowOffset: { width: 0, height: 4 }, shadowOpacity: 0.12, shadowRadius: 12, elevation: 5 },
};
```

From src/components/progress/LetterMasteryGrid.tsx:
```typescript
export interface LetterMasteryGridProps {
  entities: Record<string, EntityState>;
  learnedIds: number[];
  today: string;
}
```

From src/components/progress/PhasePanel.tsx:
```typescript
export interface PhasePanelProps {
  label: string;
  done: number;
  total: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 source-audit test stubs for PROG-02 and PROG-03</name>
  <files>src/__tests__/progress-mastery-grid.test.ts, src/__tests__/progress-phase-bars.test.ts</files>
  <action>
Create source-audit tests following the established Phase 4 pattern (read source file as string, assert with regex).

**progress-mastery-grid.test.ts** (PROG-02, per D-03 and D-04):
- Read `src/components/progress/LetterMasteryGrid.tsx` as a string using `fs.readFileSync`
- Assert the source contains all 5 mastery state case branches: "not_started", "introduced", "unstable", "accurate", "retained"
- Assert retained state has a DIFFERENT visual treatment from accurate state (they must not be identical blocks). Check that the retained case references at least one distinguishing property not in accurate (e.g., `shadows`, `cardLifted`, `primaryDark`, or `elevation`)
- Assert the source uses color tokens from design/tokens (imports from `../../design/tokens` or uses `colors.primarySoft`, `colors.accentLight`, etc.)
- Assert border differentiation exists (the word "border" appears in mastery styling logic)

**progress-phase-bars.test.ts** (PROG-03, per D-05):
- Read `src/components/progress/PhasePanel.tsx` as a string using `fs.readFileSync`
- Assert the source imports from `react-native-reanimated`
- Assert the source imports or references `springs.gentle` or `springs` from the animations module
- Assert the source contains `useSharedValue` or `useAnimatedStyle` (Reanimated primitives)
- Assert the source contains `withSpring` (for animated progress bar fill)
- Assert no hardcoded spring config objects exist (no raw `stiffness:` outside of imports -- per D-09 no new magic numbers)

Use `it.todo()` for any tests that describe behavior not yet implemented (the component changes come in Task 2). But write the assertions now so they act as red-light tests -- they should FAIL against the current source until Task 2 is complete.
  </action>
  <verify>
    <automated>cd "C:/Users/Nasif/Desktop/Iqra AI/tila-mobile" && npx vitest run src/__tests__/progress-mastery-grid.test.ts src/__tests__/progress-phase-bars.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist with real assertions. Tests for mastery grid currently fail (retained === accurate). Tests for phase bars currently fail (no Reanimated imports). This is expected RED state.</done>
</task>

<task type="auto">
  <name>Task 2: Polish LetterMasteryGrid 5-state visuals and PhasePanel animated progress bars</name>
  <files>src/components/progress/LetterMasteryGrid.tsx, src/components/progress/PhasePanel.tsx</files>
  <action>
**LetterMasteryGrid.tsx** — 5 distinct mastery states (per D-03, D-04):

Update the `getMasteryStyle` function to return visually distinct styles for ALL 5 states. The key fix: retained and accurate are currently identical. Make them different.

State mapping (per D-03, D-04 — color + opacity + border, not just color alone):
- **not_started**: bg=colors.bgCard, border="transparent", textColor=colors.textMuted, opacity 0.35. Dimmed, barely visible.
- **introduced**: bg=colors.bgCard, border=colors.border (2px), textColor=colors.textSoft, opacity 1.0. Present but neutral.
- **unstable**: bg=colors.accentLight, border=colors.accent (2px), textColor=colors.text, opacity 1.0. Amber warmth.
- **accurate**: bg=colors.primarySoft, border=colors.primary (2px), textColor=colors.primaryDark, opacity 1.0. Green, solid.
- **retained**: bg=colors.primaryDark, border=colors.primary (2px), textColor=colors.primarySoft (light text on dark bg), opacity 1.0, PLUS apply `shadows.cardLifted` for a lifted glow effect. This is the key differentiator -- retained uses a DARK green background (inverted from accurate) with shadow lift.

Update the return type of `getMasteryStyle` to include an optional `shadow` property (the shadows object from tokens.ts) and an `opacity` number. Apply these in the render.

Import `shadows` from `../../design/tokens`.

Do NOT add per-cell animations (28 cells would be expensive per Research anti-pattern). The grid stays static; visual distinction comes from color/shadow only.

**PhasePanel.tsx** — Animated progress bar (per D-05, D-06, D-09):

Add Reanimated imports: `useSharedValue`, `useAnimatedStyle`, `withSpring`, `withDelay` from `react-native-reanimated`, and `Animated` default import.

Import `springs` from `../../design/animations`.

Replace the static `<View style={{ width: \`${pct}%\` }}>` progress fill with an `<Animated.View>` whose width is driven by a shared value:
1. Create `const progress = useSharedValue(0);`
2. In a `useEffect` keyed on `[done, total]`, set `progress.value = withSpring(pct / 100, springs.gentle);`
3. Create animated style: `useAnimatedStyle(() => ({ width: \`${progress.value * 100}%\` }))`
4. Apply animated style to the progress fill View (change it to Animated.View)

Also increase the progress bar height from 4 to 6 for better visibility, and set borderRadius to 3 accordingly.

Keep the existing phaseHeader, status dot, count display, and Card wrapper unchanged. Only the progress bar fill changes.

No new magic numbers -- all timing from animations.ts (D-09).
  </action>
  <verify>
    <automated>cd "C:/Users/Nasif/Desktop/Iqra AI/tila-mobile" && npx vitest run src/__tests__/progress-mastery-grid.test.ts src/__tests__/progress-phase-bars.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All source-audit tests pass GREEN. LetterMasteryGrid has 5 visually distinct states with retained clearly different from accurate (dark bg + shadow). PhasePanel progress bars use Reanimated withSpring + springs.gentle. No new magic numbers.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/__tests__/progress-mastery-grid.test.ts` -- all assertions pass
2. `npx vitest run src/__tests__/progress-phase-bars.test.ts` -- all assertions pass
3. `npm run typecheck` -- no type errors in modified files
4. `npm run validate` -- full lint + typecheck clean
</verification>

<success_criteria>
- LetterMasteryGrid has 5 distinct visual states that differ by color + opacity + border (per D-03, D-04)
- Retained state is immediately distinguishable from accurate state (dark bg + shadow lift vs light bg)
- PhasePanel progress bars animate from 0 to actual value using springs.gentle (per D-05)
- All animation values come from src/design/animations.ts, no magic numbers (per D-09)
- Source-audit tests validate all of the above
</success_criteria>

<output>
After completion, create `.planning/phases/06-progress-screen/06-01-SUMMARY.md`
</output>
