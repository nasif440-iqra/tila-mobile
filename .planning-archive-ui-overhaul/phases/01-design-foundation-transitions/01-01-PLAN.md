---
phase: 01-design-foundation-transitions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/design/animations.ts
  - src/design/haptics.ts
  - src/__tests__/animations.test.ts
  - src/__tests__/haptics.test.ts
autonomous: true
requirements: [DES-03, DES-04]
must_haves:
  truths:
    - "Animation presets are centralized in a single shared module"
    - "Haptic feedback has named presets matching the 3-tier system"
    - "Both modules are tested and exported correctly"
  artifacts:
    - path: "src/design/animations.ts"
      provides: "Centralized animation presets (springs, durations, staggers, easings, screenTransitions, pressScale)"
      exports: ["springs", "durations", "staggers", "easings", "screenTransitions", "pressScale"]
    - path: "src/design/haptics.ts"
      provides: "Haptic feedback utility with named presets"
      exports: ["hapticTap", "hapticSuccess", "hapticError", "hapticMilestone", "hapticSelection"]
    - path: "src/__tests__/animations.test.ts"
      provides: "Unit tests for animation presets module"
    - path: "src/__tests__/haptics.test.ts"
      provides: "Unit tests for haptics utility module"
  key_links:
    - from: "src/design/animations.ts"
      to: "react-native-reanimated"
      via: "Easing import"
      pattern: "import.*Easing.*from.*react-native-reanimated"
    - from: "src/design/haptics.ts"
      to: "expo-haptics"
      via: "Haptics import"
      pattern: "import.*expo-haptics"
---

<objective>
Create the centralized animation presets module and haptic feedback utility that form the foundation for all Phase 1 component polish and transition work.

Purpose: Every subsequent plan in this phase (and all future phases) imports from these two modules instead of hardcoding animation values or calling expo-haptics directly. This is the single source of truth for motion and tactile feedback.

Output: Two new utility modules (animations.ts, haptics.ts) with full unit test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-foundation-transitions/01-CONTEXT.md
@.planning/phases/01-design-foundation-transitions/01-RESEARCH.md

@src/design/tokens.ts
@src/components/onboarding/animations.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create animation presets module with tests</name>
  <files>src/design/animations.ts, src/__tests__/animations.test.ts</files>
  <read_first>
    - src/design/tokens.ts (see existing token structure and export patterns)
    - src/components/onboarding/animations.ts (see existing animation constants to supersede)
    - src/design/components/Button.tsx (see current hardcoded spring values: stiffness 400, damping 25)
    - src/design/components/QuizOption.tsx (see current hardcoded spring and timing values)
  </read_first>
  <behavior>
    - Test: `springs` export has keys: press, bouncy, gentle, snap — each with stiffness and damping properties
    - Test: `springs.press` has stiffness 400, damping 20, mass 0.8 (per D-06 snappier range)
    - Test: `springs.gentle` has stiffness 200, damping 22 (per D-06 gentle range)
    - Test: `durations` export has keys: fast (150), micro (200), normal (300), slow (400), dramatic (600) (per D-05)
    - Test: `staggers` export has keys: fast, normal, dramatic — each with delay and duration
    - Test: `staggers.fast.delay` is 50 (per D-05 stagger range)
    - Test: `easings` export has keys: contentReveal, entrance, exit, smooth
    - Test: `screenTransitions` export has keys: slideUp (400), fade (300), push (350), feedback (200) (per D-11)
    - Test: `pressScale` export has keys: normal (0.97), subtle (0.98), bouncy (0.95) (per D-02)
  </behavior>
  <action>
    Create `src/__tests__/animations.test.ts` first with failing tests for all exports listed in behavior.

    Then create `src/design/animations.ts` with these exact exports (per D-04 through D-07):

    ```typescript
    import { Easing } from "react-native-reanimated";

    export const springs = {
      press: { stiffness: 400, damping: 20, mass: 0.8 },
      bouncy: { stiffness: 300, damping: 18 },
      gentle: { stiffness: 200, damping: 22 },
      snap: { stiffness: 500, damping: 25 },
    } as const;

    export const durations = {
      fast: 150,
      micro: 200,
      normal: 300,
      slow: 400,
      dramatic: 600,
    } as const;

    export const staggers = {
      fast: { delay: 50, duration: 300 },
      normal: { delay: 80, duration: 400 },
      dramatic: { delay: 120, duration: 600 },
    } as const;

    export const easings = {
      contentReveal: Easing.out(Easing.cubic),
      entrance: Easing.out(Easing.exp),
      exit: Easing.in(Easing.cubic),
      smooth: Easing.inOut(Easing.ease),
    } as const;

    export const screenTransitions = {
      slideUp: 400,
      fade: 300,
      push: 350,
      feedback: 200,
    } as const;

    export const pressScale = {
      normal: 0.97,
      subtle: 0.98,
      bouncy: 0.95,
    } as const;
    ```

    Use `as const` on every export for maximum type narrowing. Follow existing token.ts export pattern (named exports, no default).
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/animations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/design/animations.ts` exists and exports 6 named constants
    - `grep "export const springs" src/design/animations.ts` matches
    - `grep "export const durations" src/design/animations.ts` matches
    - `grep "export const staggers" src/design/animations.ts` matches
    - `grep "export const easings" src/design/animations.ts` matches
    - `grep "export const screenTransitions" src/design/animations.ts` matches
    - `grep "export const pressScale" src/design/animations.ts` matches
    - `grep "stiffness: 400" src/design/animations.ts` matches (press spring)
    - `grep "stiffness: 200" src/design/animations.ts` matches (gentle spring)
    - `grep "normal: 300" src/design/animations.ts` matches (duration)
    - `grep "slideUp: 400" src/design/animations.ts` matches (screen transition)
    - `grep "normal: 0.97" src/design/animations.ts` matches (press scale)
    - All tests in `src/__tests__/animations.test.ts` pass
    - `npm run validate` passes (typecheck + lint)
  </acceptance_criteria>
  <done>animations.ts exports all 6 preset categories with correct values per D-04 through D-07, all tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create haptics utility module with tests</name>
  <files>src/design/haptics.ts, src/__tests__/haptics.test.ts</files>
  <read_first>
    - src/design/components/Button.tsx (see current direct expo-haptics usage: Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light))
    - src/design/components/QuizOption.tsx (see current haptic calls for correct/wrong states)
    - app/(tabs)/_layout.tsx (see current tab press haptic)
  </read_first>
  <behavior>
    - Test: module exports functions: hapticTap, hapticSuccess, hapticError, hapticMilestone, hapticSelection
    - Test: each export is typeof "function"
    - Test: hapticTap calls Haptics.impactAsync with ImpactFeedbackStyle.Light
    - Test: hapticSuccess calls Haptics.notificationAsync with NotificationFeedbackType.Success
    - Test: hapticError calls Haptics.notificationAsync with NotificationFeedbackType.Error
    - Test: hapticMilestone calls Haptics.impactAsync with ImpactFeedbackStyle.Heavy
    - Test: hapticSelection calls Haptics.selectionAsync
  </behavior>
  <action>
    Create `src/__tests__/haptics.test.ts` first. Mock `expo-haptics` module and verify each function calls the correct underlying API.

    Then create `src/design/haptics.ts` (per D-08, D-09):

    ```typescript
    import * as Haptics from "expo-haptics";

    /** Tier 1: Light tap for interactive presses (buttons, options, cards) */
    export function hapticTap() {
      Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light);
    }

    /** Tier 2: Success notification (correct answers, lesson complete) */
    export function hapticSuccess() {
      Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success);
    }

    /** Tier 3: Error notification (wrong answers) */
    export function hapticError() {
      Haptics.notificationAsync(Haptics.NotificationFeedbackType.Error);
    }

    /** Milestone: Heavy impact for significant achievements */
    export function hapticMilestone() {
      Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Heavy);
    }

    /** Selection change: subtle feedback for picker/selection changes */
    export function hapticSelection() {
      Haptics.selectionAsync();
    }
    ```

    This is a plain utility module — NOT a React hook. No React dependencies. Fire-and-forget calls.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/haptics.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/design/haptics.ts` exists
    - `grep "export function hapticTap" src/design/haptics.ts` matches
    - `grep "export function hapticSuccess" src/design/haptics.ts` matches
    - `grep "export function hapticError" src/design/haptics.ts` matches
    - `grep "export function hapticMilestone" src/design/haptics.ts` matches
    - `grep "export function hapticSelection" src/design/haptics.ts` matches
    - `grep "ImpactFeedbackStyle.Light" src/design/haptics.ts` matches (tap = light)
    - `grep "ImpactFeedbackStyle.Heavy" src/design/haptics.ts` matches (milestone = heavy)
    - `grep "NotificationFeedbackType.Success" src/design/haptics.ts` matches
    - `grep "NotificationFeedbackType.Error" src/design/haptics.ts` matches
    - No React imports in haptics.ts (it is a plain utility)
    - All tests in `src/__tests__/haptics.test.ts` pass
    - `npm run validate` passes
  </acceptance_criteria>
  <done>haptics.ts exports all 5 named preset functions with correct expo-haptics calls per D-08/D-09, all tests green</done>
</task>

</tasks>

<verification>
- `npm test -- --run` — all tests pass including new animation and haptics tests
- `npm run validate` — typecheck and lint pass
- `grep -r "export const" src/design/animations.ts | wc -l` returns 6
- `grep -r "export function" src/design/haptics.ts | wc -l` returns 5
</verification>

<success_criteria>
- Two new modules exist: src/design/animations.ts and src/design/haptics.ts
- Both have full unit test coverage
- Animation presets match values from D-04 through D-07
- Haptic presets match the 3-tier system from D-08/D-09
- npm test and npm run validate both pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-foundation-transitions/01-01-SUMMARY.md`
</output>
