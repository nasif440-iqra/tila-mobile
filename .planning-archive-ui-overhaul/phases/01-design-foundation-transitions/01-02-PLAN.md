---
phase: 01-design-foundation-transitions
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/design/components/Button.tsx
  - src/design/components/Card.tsx
  - src/design/components/HearButton.tsx
  - src/design/components/QuizOption.tsx
  - app/(tabs)/_layout.tsx
autonomous: true
requirements: [DES-01, DES-02, DES-04]
must_haves:
  truths:
    - "All 5 design system components use shared animation presets instead of hardcoded values"
    - "All interactive components use the haptics utility instead of direct expo-haptics imports"
    - "Button press animation uses spring presets and feels satisfying with bounce"
    - "Card has interactive mode with press animation for pressable cards"
    - "HearButton has press animation (currently has none)"
    - "QuizOption uses shared presets for press, correct, and wrong state animations"
  artifacts:
    - path: "src/design/components/Button.tsx"
      provides: "Polished button with shared animation presets"
      contains: "springs.press"
    - path: "src/design/components/Card.tsx"
      provides: "Card with optional interactive press state"
      contains: "interactive"
    - path: "src/design/components/HearButton.tsx"
      provides: "Audio button with press animation and haptics utility"
      contains: "hapticTap"
    - path: "src/design/components/QuizOption.tsx"
      provides: "Quiz option using shared animation presets and haptics"
      contains: "springs.press"
  key_links:
    - from: "src/design/components/Button.tsx"
      to: "src/design/animations.ts"
      via: "named import"
      pattern: "from.*../animations"
    - from: "src/design/components/Button.tsx"
      to: "src/design/haptics.ts"
      via: "named import"
      pattern: "from.*../haptics"
    - from: "src/design/components/QuizOption.tsx"
      to: "src/design/animations.ts"
      via: "named import"
      pattern: "from.*../animations"
    - from: "src/design/components/QuizOption.tsx"
      to: "src/design/haptics.ts"
      via: "named import"
      pattern: "from.*../haptics"
---

<objective>
Polish the 5 design system components (Button, Card, ArabicText, HearButton, QuizOption) to use shared animation presets and haptic utility, replacing all hardcoded animation values and direct expo-haptics imports.

Purpose: Components become the premium, consistent building blocks that every screen uses. No more scattered magic numbers or inconsistent haptic calls. ArabicText needs no changes (no animations or haptics).

Output: 4 modified component files + 1 modified tab layout, all using centralized presets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-foundation-transitions/01-CONTEXT.md
@.planning/phases/01-design-foundation-transitions/01-RESEARCH.md
@.planning/phases/01-design-foundation-transitions/01-01-SUMMARY.md

<interfaces>
<!-- From src/design/animations.ts (created in Plan 01): -->
export const springs = {
  press: { stiffness: 400, damping: 20, mass: 0.8 },
  bouncy: { stiffness: 300, damping: 18 },
  gentle: { stiffness: 200, damping: 22 },
  snap: { stiffness: 500, damping: 25 },
} as const;

export const durations = {
  fast: 150, micro: 200, normal: 300, slow: 400, dramatic: 600,
} as const;

export const pressScale = {
  normal: 0.97, subtle: 0.98, bouncy: 0.95,
} as const;

<!-- From src/design/haptics.ts (created in Plan 01): -->
export function hapticTap(): void;
export function hapticSuccess(): void;
export function hapticError(): void;
export function hapticMilestone(): void;
export function hapticSelection(): void;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish Button and Card components with shared presets</name>
  <files>src/design/components/Button.tsx, src/design/components/Card.tsx</files>
  <read_first>
    - src/design/components/Button.tsx (current state: hardcoded stiffness 400/damping 25, direct Haptics import)
    - src/design/components/Card.tsx (current state: static View, no press interaction)
    - src/design/animations.ts (the presets to import)
    - src/design/haptics.ts (the haptic utility to import)
    - src/design/tokens.ts (shadow tokens: shadows.card, shadows.cardLifted)
  </read_first>
  <action>
    **Button.tsx changes (per D-01, D-02):**
    1. Replace `import * as Haptics from "expo-haptics"` with `import { hapticTap } from "../haptics"`
    2. Add `import { springs, pressScale } from "../animations"`
    3. In `handlePressIn`: change `withSpring(0.97, { stiffness: 400, damping: 25 })` to `withSpring(pressScale.normal, springs.press)`
    4. In `handlePressOut`: change `withSpring(1, { stiffness: 400, damping: 25 })` to `withSpring(1, springs.press)`
    5. In `handlePress`: change `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light)` to `hapticTap()`
    6. Remove the `import * as Haptics from "expo-haptics"` line entirely

    **Card.tsx changes (per D-03):**
    1. Add imports: `import Animated, { useSharedValue, useAnimatedStyle, withSpring } from "react-native-reanimated"` and `import { Pressable } from "react-native"`
    2. Add `import { springs, pressScale } from "../animations"` and `import { hapticTap } from "../haptics"`
    3. Add `interactive?: boolean` and `onPress?: () => void` to CardProps interface
    4. When `interactive` is true AND `onPress` is provided, wrap content in `Animated.createAnimatedComponent(Pressable)` with:
       - `onPressIn`: `scale.value = withSpring(pressScale.subtle, springs.press)` (subtle = 0.98, gentler than button)
       - `onPressOut`: `scale.value = withSpring(1, springs.press)`
       - `onPress`: calls `hapticTap()` then `onPress()`
    5. When `interactive` is false (default), render as plain `View` exactly as before — no behavior change
    6. Keep existing `elevated` prop behavior unchanged
  </action>
  <verify>
    <automated>npm run validate</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from \"expo-haptics\"" src/design/components/Button.tsx` returns NO matches (direct import removed)
    - `grep "hapticTap" src/design/components/Button.tsx` matches (using utility)
    - `grep "springs.press" src/design/components/Button.tsx` matches (using preset)
    - `grep "pressScale.normal" src/design/components/Button.tsx` matches (using preset)
    - `grep "stiffness.*400.*damping.*25" src/design/components/Button.tsx` returns NO matches (hardcoded removed)
    - `grep "interactive" src/design/components/Card.tsx` matches (new prop added)
    - `grep "onPress" src/design/components/Card.tsx` matches (new prop added)
    - `grep "springs.press" src/design/components/Card.tsx` matches (using preset for interactive)
    - `grep "pressScale.subtle" src/design/components/Card.tsx` matches (subtle scale for cards per D-03)
    - `npm run validate` passes
  </acceptance_criteria>
  <done>Button uses shared presets and haptic utility with no direct expo-haptics import. Card supports interactive press state with gentle scale animation per D-03.</done>
</task>

<task type="auto">
  <name>Task 2: Polish HearButton and QuizOption, migrate tab layout haptics</name>
  <files>src/design/components/HearButton.tsx, src/design/components/QuizOption.tsx, app/(tabs)/_layout.tsx</files>
  <read_first>
    - src/design/components/HearButton.tsx (current: plain Pressable with no animation, direct Haptics import)
    - src/design/components/QuizOption.tsx (current: hardcoded spring values, hardcoded shake timing, direct Haptics imports)
    - app/(tabs)/_layout.tsx (current: direct Haptics import for tab press)
    - src/design/animations.ts (presets to use)
    - src/design/haptics.ts (utility to use)
  </read_first>
  <action>
    **HearButton.tsx changes (per D-01, D-02):**
    1. Replace `import * as Haptics from "expo-haptics"` with `import { hapticTap } from "../haptics"`
    2. Add `import Animated, { useSharedValue, useAnimatedStyle, withSpring } from "react-native-reanimated"`
    3. Add `import { springs, pressScale } from "../animations"`
    4. Create `const AnimatedPressable = Animated.createAnimatedComponent(Pressable)`
    5. Add shared value `scale = useSharedValue(1)` and animated style for transform scale
    6. Add `onPressIn` handler: `scale.value = withSpring(pressScale.subtle, springs.press)`
    7. Add `onPressOut` handler: `scale.value = withSpring(1, springs.press)`
    8. Replace `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light)` in handlePress with `hapticTap()`
    9. Replace `<Pressable>` with `<AnimatedPressable>` and apply animated style
    10. Remove the direct expo-haptics import

    **QuizOption.tsx changes (per D-02, D-04):**
    1. Replace `import * as Haptics from "expo-haptics"` with `import { hapticTap, hapticSuccess, hapticError } from "../haptics"`
    2. Add `import { springs, pressScale, durations } from "../animations"`
    3. In `handlePressIn`: change `withSpring(0.97, { stiffness: 400, damping: 25 })` to `withSpring(pressScale.normal, springs.press)`
    4. In `handlePressOut`: change `withSpring(1, { stiffness: 400, damping: 25 })` to `withSpring(1, springs.press)`
    5. In correct state effect: replace `withTiming(1.04, { duration: 150 })` with `withTiming(1.04, { duration: durations.fast })` and `withTiming(1, { duration: 150 })` with `withTiming(1, { duration: durations.fast })`
    6. In wrong state shake effect: replace all `{ duration: 50 }` with `{ duration: 50 }` (keep at 50ms — shake segments must be very fast, this is below the `durations.fast` threshold and is a deliberate sub-preset value for shake segments)
    7. Replace `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success)` with `hapticSuccess()`
    8. Replace `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Error)` with `hapticError()`
    9. Replace `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light)` in handlePress with `hapticTap()`
    10. Remove the direct expo-haptics import

    **Tab layout changes:**
    1. Replace `import * as Haptics from "expo-haptics"` with `import { hapticTap } from "../../src/design/haptics"`
    2. Replace `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light)` with `hapticTap()`
    3. Remove the direct expo-haptics import
  </action>
  <verify>
    <automated>npm run validate</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from \"expo-haptics\"" src/design/components/HearButton.tsx` returns NO matches
    - `grep "from \"expo-haptics\"" src/design/components/QuizOption.tsx` returns NO matches
    - `grep "from \"expo-haptics\"" app/\(tabs\)/_layout.tsx` returns NO matches
    - `grep "hapticTap" src/design/components/HearButton.tsx` matches
    - `grep "hapticTap" src/design/components/QuizOption.tsx` matches
    - `grep "hapticSuccess" src/design/components/QuizOption.tsx` matches
    - `grep "hapticError" src/design/components/QuizOption.tsx` matches
    - `grep "springs.press" src/design/components/QuizOption.tsx` matches
    - `grep "pressScale.normal" src/design/components/QuizOption.tsx` matches
    - `grep "durations.fast" src/design/components/QuizOption.tsx` matches
    - `grep "AnimatedPressable" src/design/components/HearButton.tsx` matches (press animation added)
    - `grep "hapticTap" app/\(tabs\)/_layout.tsx` matches
    - `grep "stiffness.*400.*damping.*25" src/design/components/QuizOption.tsx` returns NO matches
    - `npm run validate` passes
    - `npm test -- --run` passes
  </acceptance_criteria>
  <done>HearButton has press animation and uses haptic utility. QuizOption uses all shared presets. Tab layout uses haptic utility. Zero direct expo-haptics imports remain in design system components.</done>
</task>

</tasks>

<verification>
- `npm run validate` passes
- `npm test -- --run` passes
- `grep -r "from \"expo-haptics\"" src/design/components/` returns NO matches (all 5 components clean)
- `grep -r "stiffness.*400.*damping.*25" src/design/components/` returns NO matches (all hardcoded springs removed)
- `grep -r "springs\.\|pressScale\.\|durations\." src/design/components/` shows presets used across components
</verification>

<success_criteria>
- Button, Card, HearButton, QuizOption all import from animations.ts and haptics.ts
- No direct expo-haptics imports remain in any design system component
- No hardcoded spring values remain in any design system component
- Card supports interactive prop with press animation
- HearButton has press animation (new behavior)
- All existing functionality preserved (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-foundation-transitions/01-02-SUMMARY.md`
</output>
