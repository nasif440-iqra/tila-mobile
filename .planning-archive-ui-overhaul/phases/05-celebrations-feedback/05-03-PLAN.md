---
phase: 05-celebrations-feedback
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/phase-complete.tsx
  - src/__tests__/phase-complete-celebration.test.ts
  - src/__tests__/celebration-tiers.test.ts
autonomous: true
requirements:
  - CEL-02
  - CEL-04
must_haves:
  truths:
    - "Phase completion screen has WarmGlow behind the Arabic centerpiece"
    - "Phase completion screen fires hapticMilestone on mount"
    - "Phase completion screen has scale entrance animation on Arabic text"
    - "The four celebration tiers are visually distinct: micro (QuizOption sparkle), small (LessonSummary WarmGlow), big (LetterMasteryCelebration), milestone (phase-complete WarmGlow + haptic)"
    - "Lesson completion celebration (CEL-02) is confirmed working from Phase 4 (LessonSummary tiered WarmGlow)"
  artifacts:
    - path: "app/phase-complete.tsx"
      provides: "Milestone-tier phase completion celebration"
      contains: "WarmGlow"
    - path: "src/__tests__/phase-complete-celebration.test.ts"
      provides: "Source audit tests for phase-complete enhancements"
    - path: "src/__tests__/celebration-tiers.test.ts"
      provides: "Source audit tests validating 4-tier system"
  key_links:
    - from: "app/phase-complete.tsx"
      to: "src/components/onboarding/WarmGlow.tsx"
      via: "import and render"
      pattern: "WarmGlow"
    - from: "app/phase-complete.tsx"
      to: "src/design/haptics.ts"
      via: "hapticMilestone on mount"
      pattern: "hapticMilestone"
---

<objective>
Enhance the phase completion screen with milestone-tier celebration visuals and validate the complete 4-tier celebration system.

Purpose: Phase completion is the peak achievement moment (per D-06) — completing all lessons in a phase. The existing screen has basic FadeIn stagger but lacks the visual warmth and haptic impact of a milestone celebration. CEL-02 (lesson completion celebration) was already delivered in Phase 4 via LessonSummary tiered WarmGlow — this plan validates it and elevates phase completion to milestone tier.

Output: Enhanced phase-complete.tsx with WarmGlow + hapticMilestone + scale animation. Source audit tests validating the complete 4-tier system.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-celebrations-feedback/05-CONTEXT.md
@.planning/phases/05-celebrations-feedback/05-RESEARCH.md
@.planning/phases/05-celebrations-feedback/05-01-SUMMARY.md

@app/phase-complete.tsx
@src/components/onboarding/WarmGlow.tsx
@src/components/LessonSummary.tsx
@src/components/quiz/QuizCelebration.tsx
@src/design/components/QuizOption.tsx
@src/design/animations.ts
@src/design/haptics.ts
@src/design/tokens.ts

<interfaces>
From src/components/onboarding/WarmGlow.tsx:
```typescript
export function WarmGlow({
  size?: number,        // default 340
  opacity?: number,     // default 0.12
  animated?: boolean,   // default false
  color?: string,
  pulseMin?: number,    // default 0.08
  pulseMax?: number,    // default 0.25
}: WarmGlowProps)
```

From src/design/haptics.ts:
```typescript
export function hapticMilestone() // Heavy impact for significant achievements
```

From src/design/animations.ts:
```typescript
export const springs = {
  gentle: { stiffness: 200, damping: 22 },
  bouncy: { stiffness: 300, damping: 18 },
}
export const durations = { normal: 300, slow: 400, dramatic: 600 }
```

Existing phase-complete.tsx structure:
- SafeAreaView with colors.primary background
- Animated.View with FadeIn.duration(800) for arabicWrap containing ArabicText
- FadeInDown stagger at 200, 350, 500, 650, 800, 1000ms delays
- PHASE_INFO record with title + affirmation per phase
- NEXT_PHASE record with next phase title
- Button to router.replace("/(tabs)")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance phase-complete.tsx with milestone-tier celebration</name>
  <files>
    app/phase-complete.tsx
  </files>
  <read_first>
    app/phase-complete.tsx
    src/components/onboarding/WarmGlow.tsx
    src/design/animations.ts
    src/design/haptics.ts
  </read_first>
  <action>
Enhance `app/phase-complete.tsx` with milestone-tier visuals per D-07 and Research phase-complete enhancement pattern. Keep existing structure — just elevate it.

**1. Add imports:**
```typescript
import { useSharedValue, useAnimatedStyle, withSpring } from "react-native-reanimated";
import { WarmGlow } from "../src/components/onboarding/WarmGlow";
import { hapticMilestone } from "../src/design/haptics";
import { springs } from "../src/design/animations";
```

(Note: Animated is already imported from reanimated. Keep the existing FadeIn, FadeInDown imports.)

**2. Add hapticMilestone on mount** inside the component, in a new useEffect:
```typescript
useEffect(() => {
  hapticMilestone();
}, []);
```

**3. Add scale entrance animation** for the Arabic centerpiece:
```typescript
const arabicScale = useSharedValue(0.92);

useEffect(() => {
  arabicScale.value = withSpring(1, springs.gentle);
}, []);

const arabicScaleStyle = useAnimatedStyle(() => ({
  transform: [{ scale: arabicScale.value }],
}));
```

**4. Wrap the arabicWrap section** with WarmGlow behind the ArabicText. Replace the existing arabicWrap Animated.View content:

Before (existing):
```jsx
<Animated.View entering={FadeIn.duration(800)} style={styles.arabicWrap}>
  <ArabicText size="display" color={colors.accent} style={{ fontSize: 72 }}>
    {"\u0627\u0644\u062D\u0645\u062F \u0644\u0644\u0647"}
  </ArabicText>
</Animated.View>
```

After:
```jsx
<Animated.View entering={FadeIn.duration(800)} style={[styles.arabicWrap, { alignItems: "center", justifyContent: "center" }]}>
  <Animated.View style={arabicScaleStyle}>
    <View style={{ alignItems: "center", justifyContent: "center" }}>
      <WarmGlow
        size={200}
        animated
        color="rgba(196,164,100,0.25)"
        pulseMin={0.06}
        pulseMax={0.18}
      />
      <ArabicText size="display" color={colors.accent} style={{ fontSize: 72 }}>
        {"\u0627\u0644\u062D\u0645\u062F \u0644\u0644\u0647"}
      </ArabicText>
    </View>
  </Animated.View>
</Animated.View>
```

**5. Increase stagger delays slightly** for more dramatic pacing (per Research recommendation):
- "Phase Complete" label: delay 300 (was 200)
- Phase title: delay 500 (was 350)
- Affirmation: delay 700 (was 500)
- Wird badge: delay 900 (was 650)
- Unlock card: delay 1100 (was 800)
- Continue button: delay 1300 (was 1000)

**6. Add View import** if not already present (it is — View is already imported).

**Do NOT change:**
- The existing PHASE_INFO or NEXT_PHASE records
- The analytics tracking in useEffect
- The router.replace navigation
- The overall layout structure
- Any existing styles (just add alignment to arabicWrap inline)
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - grep "WarmGlow" app/phase-complete.tsx returns a match
    - grep "hapticMilestone" app/phase-complete.tsx returns a match
    - grep "arabicScale\|springs.gentle" app/phase-complete.tsx returns a match
    - grep "size={200}" app/phase-complete.tsx returns a match (WarmGlow size)
    - npm run typecheck passes
  </acceptance_criteria>
  <done>Phase completion screen has WarmGlow (size 200) behind Arabic centerpiece, hapticMilestone on mount, springs.gentle scale entrance from 0.92 to 1.0, and increased stagger delays for dramatic pacing. Existing structure preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Source audit tests for phase-complete and 4-tier celebration system</name>
  <files>
    src/__tests__/phase-complete-celebration.test.ts
    src/__tests__/celebration-tiers.test.ts
  </files>
  <read_first>
    src/__tests__/phase-complete-celebration.test.ts
    src/__tests__/celebration-tiers.test.ts
    app/phase-complete.tsx
    src/components/LessonSummary.tsx
    src/components/quiz/QuizCelebration.tsx
    src/design/components/QuizOption.tsx
  </read_first>
  <action>
Replace the it.todo() stubs from Plan 01 with real source-audit assertions (per Phase 04 pattern: read source as string + regex assertions).

**1. Update `src/__tests__/phase-complete-celebration.test.ts`** (CEL-04):

```typescript
import { describe, it, expect } from "vitest";
import * as fs from "fs";
import * as path from "path";

describe("CEL-04: Phase completion milestone celebration", () => {
  const source = fs.readFileSync(
    path.resolve(__dirname, "../../app/phase-complete.tsx"),
    "utf-8"
  );

  it("imports WarmGlow component", () => {
    expect(source).toMatch(/import.*WarmGlow.*from/);
  });

  it("renders WarmGlow with animated prop and size 200", () => {
    expect(source).toMatch(/WarmGlow/);
    expect(source).toMatch(/size=\{200\}/);
    expect(source).toMatch(/animated/);
  });

  it("calls hapticMilestone on mount", () => {
    expect(source).toMatch(/import.*hapticMilestone.*from/);
    expect(source).toMatch(/hapticMilestone\(\)/);
  });

  it("uses springs.gentle for scale animation", () => {
    expect(source).toMatch(/springs\.gentle/);
  });

  it("has scale animation on Arabic centerpiece", () => {
    expect(source).toMatch(/arabicScale/);
    expect(source).toMatch(/withSpring/);
  });
});
```

**2. Update `src/__tests__/celebration-tiers.test.ts`** (CEL-01):

Source-audit tests that validate all 4 tiers exist and use the correct haptic level:

```typescript
import { describe, it, expect } from "vitest";
import * as fs from "fs";
import * as path from "path";

describe("CEL-01: Tiered celebration system", () => {
  describe("Micro tier: QuizOption correct answer", () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, "../design/components/QuizOption.tsx"),
      "utf-8"
    );

    it("QuizOption uses hapticTap or hapticSuccess for correct answers", () => {
      expect(source).toMatch(/haptic(Tap|Success)/);
    });
  });

  describe("Small tier: LessonSummary completion", () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, "../components/LessonSummary.tsx"),
      "utf-8"
    );

    it("LessonSummary imports WarmGlow", () => {
      expect(source).toMatch(/import.*WarmGlow.*from/);
    });

    it("LessonSummary uses tiered haptics (milestone for high scores, success for medium, tap for low)", () => {
      expect(source).toMatch(/hapticMilestone/);
      expect(source).toMatch(/hapticSuccess/);
      expect(source).toMatch(/hapticTap/);
    });
  });

  describe("Big tier: LetterMasteryCelebration", () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, "../components/celebrations/LetterMasteryCelebration.tsx"),
      "utf-8"
    );

    it("LetterMasteryCelebration uses hapticMilestone", () => {
      expect(source).toMatch(/hapticMilestone/);
    });

    it("LetterMasteryCelebration uses WarmGlow", () => {
      expect(source).toMatch(/WarmGlow/);
    });

    it("LetterMasteryCelebration uses LETTER_MASTERY_COPY for Islamic messages", () => {
      expect(source).toMatch(/LETTER_MASTERY_COPY/);
    });
  });

  describe("Milestone tier: Phase completion", () => {
    const source = fs.readFileSync(
      path.resolve(__dirname, "../../app/phase-complete.tsx"),
      "utf-8"
    );

    it("phase-complete uses hapticMilestone", () => {
      expect(source).toMatch(/hapticMilestone/);
    });

    it("phase-complete uses WarmGlow", () => {
      expect(source).toMatch(/WarmGlow/);
    });
  });

  describe("Tier escalation", () => {
    it("each tier uses progressively stronger haptic or visual treatment", () => {
      // Validate the hierarchy exists by checking imports across files
      const quizOption = fs.readFileSync(
        path.resolve(__dirname, "../design/components/QuizOption.tsx"),
        "utf-8"
      );
      const lessonSummary = fs.readFileSync(
        path.resolve(__dirname, "../components/LessonSummary.tsx"),
        "utf-8"
      );
      const letterMastery = fs.readFileSync(
        path.resolve(__dirname, "../components/celebrations/LetterMasteryCelebration.tsx"),
        "utf-8"
      );
      const phaseComplete = fs.readFileSync(
        path.resolve(__dirname, "../../app/phase-complete.tsx"),
        "utf-8"
      );

      // Micro tier: no WarmGlow (just sparkle/haptic)
      expect(quizOption).not.toMatch(/WarmGlow/);

      // Small tier: has WarmGlow
      expect(lessonSummary).toMatch(/WarmGlow/);

      // Big tier: has WarmGlow + hapticMilestone
      expect(letterMastery).toMatch(/WarmGlow/);
      expect(letterMastery).toMatch(/hapticMilestone/);

      // Milestone tier: has WarmGlow + hapticMilestone + scale animation
      expect(phaseComplete).toMatch(/WarmGlow/);
      expect(phaseComplete).toMatch(/hapticMilestone/);
      expect(phaseComplete).toMatch(/withSpring/);
    });
  });
});
```
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/phase-complete-celebration.test.ts src/__tests__/celebration-tiers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/__tests__/phase-complete-celebration.test.ts has real assertions (no it.todo)
    - src/__tests__/celebration-tiers.test.ts has real assertions covering all 4 tiers
    - Both test files pass with npm test
  </acceptance_criteria>
  <done>Phase-complete source audit tests validate WarmGlow, hapticMilestone, springs.gentle, and scale animation. Celebration-tiers tests validate all 4 tiers (micro, small, big, milestone) with escalating visual/haptic intensity. All tests pass.</done>
</task>

</tasks>

<verification>
- `npm test -- --run` passes (all existing + new tests)
- `npm run typecheck` passes
- Phase completion screen visually enhanced with WarmGlow + haptic + scale
- 4-tier system validated via source audit tests
- CEL-02 confirmed by LessonSummary source audit (already has WarmGlow from Phase 4)
</verification>

<success_criteria>
1. Phase completion screen has WarmGlow (size 200, animated) behind Arabic centerpiece
2. Phase completion screen fires hapticMilestone on mount
3. Arabic text has springs.gentle scale entrance from 0.92 to 1.0
4. Stagger delays increased for dramatic pacing
5. Source audit tests validate all 4 celebration tiers with escalating intensity
6. CEL-02 (lesson completion) confirmed working via LessonSummary source audit
7. All tests pass, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/05-celebrations-feedback/05-03-SUMMARY.md`
</output>
