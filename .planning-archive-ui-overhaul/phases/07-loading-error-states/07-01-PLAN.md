---
phase: 07-loading-error-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/app-loading.test.ts
  - src/__tests__/empty-state.test.ts
  - src/__tests__/error-boundary.test.ts
  - src/components/feedback/AppLoadingScreen.tsx
  - src/components/feedback/ErrorFallback.tsx
  - src/components/feedback/EmptyState.tsx
autonomous: true
requirements: [STATE-01, STATE-02, STATE-03]

must_haves:
  truths:
    - "AppLoadingScreen renders BrandedLogo and WarmGlow with warm cream background"
    - "ErrorFallback shows branded error message with retry button"
    - "EmptyState renders title, subtitle, optional icon, and optional action button"
  artifacts:
    - path: "src/components/feedback/AppLoadingScreen.tsx"
      provides: "Branded loading screen component"
      exports: ["AppLoadingScreen"]
    - path: "src/components/feedback/ErrorFallback.tsx"
      provides: "Error boundary fallback UI"
      exports: ["ErrorFallback"]
    - path: "src/components/feedback/EmptyState.tsx"
      provides: "Reusable empty state component"
      exports: ["EmptyState"]
    - path: "src/__tests__/app-loading.test.ts"
      provides: "Source-audit tests for STATE-01"
    - path: "src/__tests__/empty-state.test.ts"
      provides: "Source-audit tests for STATE-02"
    - path: "src/__tests__/error-boundary.test.ts"
      provides: "Source-audit tests for STATE-03"
  key_links:
    - from: "src/components/feedback/AppLoadingScreen.tsx"
      to: "src/components/onboarding/BrandedLogo.tsx"
      via: "import BrandedLogo"
      pattern: "import.*BrandedLogo"
    - from: "src/components/feedback/AppLoadingScreen.tsx"
      to: "src/components/onboarding/WarmGlow.tsx"
      via: "import WarmGlow"
      pattern: "import.*WarmGlow"
---

<objective>
Create the three feedback components (AppLoadingScreen, ErrorFallback, EmptyState) and their Wave 0 source-audit test stubs.

Purpose: Build all UI building blocks for Phase 7 so Plan 02 only needs to wire them into the app tree.
Output: Three tested components in src/components/feedback/, three test files passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-loading-error-states/07-RESEARCH.md

@src/components/onboarding/BrandedLogo.tsx
@src/components/onboarding/WarmGlow.tsx
@src/design/tokens.ts
@src/design/theme.ts
</context>

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/components/onboarding/BrandedLogo.tsx:
```typescript
interface BrandedLogoProps {
  width?: number;  // default 120
  height?: number; // default 160
}
export function BrandedLogo({ width, height }: BrandedLogoProps)
```

From src/components/onboarding/WarmGlow.tsx:
```typescript
interface WarmGlowProps {
  size?: number;
  opacity?: number;
  animated?: boolean;
  color?: string;
  pulseMin?: number;
  pulseMax?: number;
}
export function WarmGlow({ size, opacity, animated, color, pulseMin, pulseMax }: WarmGlowProps)
```

From src/design/tokens.ts:
```typescript
export const typography = { body, bodyLarge, heading2, pageTitle, ... }
export const spacing = { xs, sm, md, lg, xl, xxl, xxxl, xxxxl }
export const radii = { sm, md, lg, xl, full }
```

From src/design/theme.ts:
```typescript
export function useColors(): ColorTokens  // { bg, text, textMuted, textSoft, primary, accent, white, brown, brownLight, ... }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test stubs for all STATE requirements</name>
  <files>src/__tests__/app-loading.test.ts, src/__tests__/empty-state.test.ts, src/__tests__/error-boundary.test.ts</files>
  <read_first>src/__tests__/celebrations.test.ts (for source-audit test pattern reference)</read_first>
  <behavior>
    - app-loading.test.ts: AppLoadingScreen source imports BrandedLogo, imports WarmGlow, uses useColors or design tokens, has flex:1 container
    - empty-state.test.ts: EmptyState source accepts title+subtitle props, has optional actionLabel+onAction, uses design tokens
    - error-boundary.test.ts: ErrorFallback source accepts onRetry prop, renders retry button text, uses design tokens for branded look
  </behavior>
  <action>
Create three source-audit test files following the established Phase 4+ pattern (read source as string, regex assertions). Use the same `fs.readFileSync` + `describe/it` structure seen in other phase test files.

For `src/__tests__/app-loading.test.ts` (STATE-01):
- Read `src/components/feedback/AppLoadingScreen.tsx` as source string
- Assert: imports BrandedLogo from onboarding
- Assert: imports WarmGlow from onboarding
- Assert: calls useColors() for themed background
- Assert: has "Preparing your lesson" or similar encouraging text

For `src/__tests__/empty-state.test.ts` (STATE-02):
- Read `src/components/feedback/EmptyState.tsx` as source string
- Assert: accepts title prop (in interface or destructuring)
- Assert: accepts subtitle prop
- Assert: has optional actionLabel and onAction
- Assert: uses typography from design tokens

For `src/__tests__/error-boundary.test.ts` (STATE-03):
- Read `src/components/feedback/ErrorFallback.tsx` as source string
- Assert: accepts onRetry prop
- Assert: contains "Try Again" or retry button text
- Assert: uses useColors() for branded appearance
- Assert: has encouraging copy ("progress is saved" or similar)
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/app-loading.test.ts src/__tests__/empty-state.test.ts src/__tests__/error-boundary.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>All three test files exist and fail (RED) because the source component files don't exist yet.</done>
</task>

<task type="auto">
  <name>Task 2: Create AppLoadingScreen, ErrorFallback, and EmptyState components</name>
  <files>src/components/feedback/AppLoadingScreen.tsx, src/components/feedback/ErrorFallback.tsx, src/components/feedback/EmptyState.tsx</files>
  <read_first>src/components/onboarding/BrandedLogo.tsx (interface), src/components/onboarding/WarmGlow.tsx (interface), src/design/tokens.ts (exports)</read_first>
  <action>
Create `src/components/feedback/` directory and three components:

**AppLoadingScreen.tsx** (per D-01, D-02 -- branded, feels like part of the app):
- Full-screen View with `flex: 1`, `backgroundColor: colors.bg` (warm cream)
- Centered WarmGlow behind BrandedLogo: `WarmGlow size={280} animated pulseMin={0.06} pulseMax={0.18}`
- BrandedLogo positioned absolutely centered on top of WarmGlow: `width={100} height={130}`
- Tagline text below: "Preparing your lesson..." in `typography.body`, `color: colors.textMuted`
- Use `marginTop: spacing.xl` between logo area and tagline
- Import useColors from `../../design/theme`, typography/spacing from `../../design/tokens`

**ErrorFallback.tsx** (per D-05, D-06 -- branded, warm, clear recovery):
- Props: `{ onRetry: () => void }`
- Full-screen View with `flex: 1`, centered, `backgroundColor: colors.bg`, `padding: spacing.xl`
- Heading: "Something went wrong" in `typography.heading2`, `color: colors.text`
- Message: "Don't worry -- your progress is saved. Tap below to try again." in `typography.body`, `color: colors.textSoft`, centered, `marginTop: spacing.md`, `marginBottom: spacing.xxl`
- Retry button: Pressable with `backgroundColor: colors.primary`, `paddingHorizontal: spacing.xxl`, `paddingVertical: spacing.lg`, `borderRadius: radii.md`
- Button text: "Try Again" in `typography.bodyLarge`, `color: colors.white`

**EmptyState.tsx** (per D-03, D-04 -- encouraging, warm):
- Props interface: `{ title: string; subtitle: string; icon?: React.ReactNode; actionLabel?: string; onAction?: () => void }`
- Container: centered View with `padding: spacing.xxl`
- Icon area: render `{icon}` if provided, with `marginBottom: spacing.lg`
- Title: in `typography.heading2`, `color: colors.text`, centered
- Subtitle: in `typography.body`, `color: colors.textSoft`, centered, `marginTop: spacing.sm`
- Optional action button: if actionLabel and onAction provided, render Pressable with `backgroundColor: colors.accent` (gold), `paddingHorizontal: spacing.xl`, `paddingVertical: spacing.md`, `borderRadius: radii.md`, `marginTop: spacing.xxl`
- Button text in `typography.bodyLarge`, `color: colors.white`
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/app-loading.test.ts src/__tests__/empty-state.test.ts src/__tests__/error-boundary.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>All three test files pass (GREEN). Components export correctly and match source-audit expectations.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/app-loading.test.ts` -- passes
- `npx vitest run src/__tests__/empty-state.test.ts` -- passes
- `npx vitest run src/__tests__/error-boundary.test.ts` -- passes
- `npm run typecheck` -- no new type errors
- All three component files exist in src/components/feedback/
</verification>

<success_criteria>
Three feedback components created with branded design using existing BrandedLogo, WarmGlow, and design tokens. All source-audit tests passing. No type errors introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/07-loading-error-states/07-01-SUMMARY.md`
</output>
